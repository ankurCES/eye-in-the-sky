# godSeye — verified gap register (audit wf_effd1b75-fc1, 150 agents)
Adversarially verified: each gap was re-checked by an agent told to REFUTE it.

## Orchestrator's own RUNTIME findings (proven against the live stack, not in the static audit)

| # | Finding | Proof |
|---|---|---|
| R1 | `sim_spawn_target` places EVERY target at the home origin (87-579 m error). Root cause: msgpack-rpc delivers dict keys as **bytes**, so `pose.get("position", {zeros})` in fake_airsim.py:491 always takes the zero default. | in-process round-trip: requested 33.7240,51.7240 -> stored 33.7200,51.7200 |
| R2 | Same bytes-key bug at fake_airsim.py:502 (`simSetObjectPose`), :520 (`simSetWind`), :411/:422/:424 (`simGetImages` request fields). `simSetWind` over RPC is a **no-op** - M15 wind never reaches the sim. | `c.simSetWind(Vector3r(12,5,0))` -> `sim._wind` stays (0,0,0) |
| R3 | MCP `uav_get_telemetry.alt_hae` returns MSL verbatim; bridge `/snapshot.alt_hae` is geoid-corrected. Same drone, same instant, **17.422 m apart**. | MCP 1589.510 vs bridge 1572.088 (= bridge alt_msl exactly) |
| R4 | `/mission-overlay` stays `{"features":[]}` throughout a live grid-search mission; `/snapshot` has no `missions[]`/`contacts[]`; `/events` returns 404. | live mission probe, t+3..t+12 s |
| R5 | `fuel_pct` stays 100.0 across a whole flight - `FuelModel.tick()` has zero production call sites. | live probe + `grep -rn '\.tick('` hits tests only |
| R6 | Catalog reality: **19 tools, 0 resources, 0 prompts** vs plan's ~33 tools + 8 `uav://` resources. | MCP `tools/list` over Streamable HTTP |
| R7 | `demo_mission.py` bypasses MCP entirely (`srv.mcp._tool_manager._tools`), so it proves nothing about the harness path. | scripts/demo_mission.py:75 |

## BLOCKERS (49)
### [tools-flight-sensors] Add uav_capture_image — the harness has no MCP path to any imagery
- plan: `PLAN.md §4.2 (uav_capture_image: image resource + geo pose + sun angle)` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: nothing as an MCP tool — `grep uav_capture_image mcp/` = 0 hits; server.py registers 19 tools (server.py:317,321,325,329,338,347,351,358,367,372,401,416,428,434,440,450,473,506,538) and none captures an image. UavBackend (server.py:40-191) has no simGetImages wrapper. Imagery exists ONLY on the bridge REST side: bridge.py:144-160 camera_png() and bridge.py:384-395 GET /camera/{veh}, which the MCP harness does not speak.
- required: An `uav_capture_image(vehicle, camera, type(scene/depth/segmentation/infrared), jpeg_quality)` MCP tool returning the image plus the geo pose of the frame and the sun angle. Note fake_airsim.py:407-434 already answers simGetImages but returns camera_position=(0,0,0) (fake_airsim.py:431) and identity camera_orientation (fake_airsim.py:432), so the fake must also publish the real vehicle NED/attitude or the returned geo pose will be meaningless UE-free. No sun-angle computation exists anywhere (only `sun_azimuth_deg` as a caller-supplied param at missions.py:108).
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py

### [tools-flight-sensors] Add uav_set_fov — M7 wide→narrow cross-cue is impossible
- plan: `PLAN.md §4.1 (uav_set_fov: vehicle, camera, fov_deg — wide↔narrow cross-cue, M7); REVIEW_MILITARY.md:27 (M7 CRITICAL)` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: nothing — `grep uav_set_fov mcp/` = 0 hits; `grep simSetCameraFov` = 0 hits in the whole repo. fake_airsim.py's RPC handler set (fake_airsim.py:257-531) has no simSetCameraFov method. The only FOV number in the system is the hard-coded planning constant DEFAULT_HFOV_DEG = 60.0 (missions.py:22), used for grid spacing math only (missions.py:31-43), never pushed to a camera.
- required: `uav_set_fov(vehicle, camera, fov_deg)` calling simSetCameraFov, plus a simSetCameraFov handler in fake_airsim.py that stores per-camera FOV and (ideally) feeds it back into the detection/footprint math so a narrow-FOV cue actually changes what the sensor sees. Without it the detect-on-wide → identify-on-narrow workflow that M7 calls the core ISR loop cannot be executed at all — uav_identify_target (server.py:416-426) admits this by deferring cross-cue to the mission planner, which also does not do it.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py

### [tools-flight-sensors] uav_get_telemetry fuel_pct is a constant 100% — the fuel integrator is never ticked
- plan: `PLAN.md §4.2 (uav_get_telemetry → fuel_pct, bingo_fuel_pct); §4.5 fuel integrator T5 / BINGO doctrine M4` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: The field is returned (server.py:362 `tele["fuel_pct"] = round(fm.fuel_pct, 2)`) but the integrator that would move it is dead code: FuelModel.tick() (safety.py:198-206) has zero call sites in mcp/ — `grep -rn '\.tick(' mcp/` returns nothing outside safety.py's own definition, only tests/test_safety.py:76-84 call it. No telemetry loop drives it (bridge.py:175-185 loop A caches snapshots and never touches FuelModel; bridge.py:46 hard-codes fuel_pct=100.0 in VehicleSnapshot). Store.log_fuel (store.py:76-77) is likewise never called from mcp/. Consequence: fuel_pct reads 100.0 forever, the M4 pre-flight gate at server.py:298-307 always evaluates against 100% available fuel (safety.py:263 `ok = required <= self.fuel_pct`), and the promised force-RTB at BINGO can never fire — `grep -i 'force_rtb\|rtb' mcp/` matches only two comment lines (safety.py:12, safety.py:243).
- required: Drive FuelModel.tick(speed_mps, vz_mps, landed, headwind_mps) from a telemetry cadence (bridge loop A or a server-side ticker), persist via Store.log_fuel, and feed the measured wind in as headwind_mps — server.py:363 and server.py:301 both call bingo_fuel_pct/preflight_gate with the default headwind_mps=0.0 even after sim_set_environment sets wind. Until then every fuel and BINGO number the harness reads is fiction, which directly defeats 'realistic' missions. (Root cause is shared with the safety/fuel dimension; reported here because it silently corrupts a §4.2 return contract.)
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/safety.py
- scope correction: Severity and blocking status stand; only the framing needs a nuance. The gap is "return contract present, physics unwired" rather than "capability partially built": the integrator (safety.py:198-206), phase classifier (safety.py:171-190), wind penalty (safety.py:192-196) and route dry-run (safety.py:208-240) are all written and unit-tested. Remaining work is wiring, in four parts: (a) call FuelModel.tick(speed_mps, vz_mps, landed, headwind_mps) on a telemetry cadence — cheapest place is server.py's uav_get_telemetry plus a background ticker so fuel burns even when the harness is not polling, or bridge.py telemetry_loop (bridge.py:175-185) if fuel is to appear in /snapshot (bridge.py:46 and bridge.py:123-138 also need fuel_pct actually populated); (b) call Store.log_fuel (store.py:76-77) from that tick; (c) persist the wind set by sim_set_environment (server.py:509-536 currently keeps it only in a local dict) on the server and pass it as headwind_mps at server.py:301 and server.py:363; (d) add the BINGO->force-RTB transition, which does not exist at all (only comments at safety.py:12 and safety.py:243).

### [tools-missions] Remove the silent 12-lane grid cap and report the spacing actually flown
- plan: `PLAN §4.3 mission_grid_search / M1` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: mcp/godseye_uav/missions.py:28 MAX_GRID_LANES=12; missions.py:90-93 lanes=min(ceil(span/spacing)+1, 12) then dlat=(lat_max-lat_min)/(lanes-1); server.py:397-398 returns handle['sweep_spacing_m']=plan.meta['sweep_spacing_m'] which is missions.py:160 grid_sweep_spacing_m(alt) — the UNCAPPED derived value
- required: M1: lane spacing must equal swath*(1-overlap) with swath=2*alt*tan(HFOV/2), and the returned footprint/lane spacing must describe the plan actually flown. Measured on the repo's own AO fixture (tests/test_missions.py:22) at alt 60 m: swath 69.3 m, derived spacing 55.4 m, but the cap forces 12 lanes over a 3339 m span = 303.6 m actual lane spacing (4.4x the swath, ~77% of the box unimaged) while the tool still reports sweep_spacing_m 55.4. The harness is told it has full-overlap coverage it does not have. Either reject the plan on the BINGO gate (as M4 intends) or return actual_spacing_m + coverage_pct and a 'coverage thinned' flag.
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py, godseye/tests/test_missions.py

### [tools-missions] Add distance-triggered image capture to mission_recon_route (forward_overlap_pct)
- plan: `PLAN §4.3 mission_recon_route / M2` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: missions.py:147-152 recon_route returns the caller's waypoints verbatim with meta {'doctrine':'route-recon'} — no overlap param, no capture plan; server.py:390-392 submits it as a plain uav_fly_route. No capture tool exists in the catalog (server.py:317-544 registers no uav_capture_image) and simGetImages is never called from server.py or missions.py (only the bridge's subscriber-gated PIP loop, bridge.py:152).
- required: M2: captures triggered by distance travelled (every N m where N = along-track footprint * (1 - forward_overlap_pct)), with the time interval used only as a max-rate clamp. Requires a forward_overlap_pct param, a server-side capture scheduler driven by telemetry distance, and captured frames recorded against the mission. As shipped, a 'recon route' mission images nothing — it is a waypoint flight, not ISR.
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py
- scope correction: Verdict stands as CONFIRMED; one scoping refinement for the implementer: the along-track geometry is already available and should be reused rather than rewritten — missions.py:31-36 footprint_m returns (w, h) where h = 2*alt*tan(VFOV/2) is the along-track swath, and missions.py:24 defines DEFAULT_OVERLAP = 0.20. So the trigger distance itself is a one-liner, h * (1 - forward_overlap_pct). What genuinely remains: (a) add forward_overlap_pct to the recon_route branch at missions.py:147-152 and surface capture_every_m in plan.meta (mirroring how grid_search surfaces sweep_spacing_m at missions.py:159-160 and server.py:397-398); (b) add a capture tool (PLAN.md:136 uav_capture_image) to server.py's catalog wrapping simGetImages through the backend, alongside the existing get_detections wrapper at server.py:170-174; (c) add a server-side scheduler that integrates along-track distance from telemetry during an executing mission and fires captures every N m with the time interval as a max-rate clamp — nothing of this exists, the only image loop (bridge.py:196-206) is time-based and UI-subscriber-gated, so it cannot be reused as-is; (d) record captured frames against the mission — store.py:67-83 has no captures collection, so a log_capture/frame record plus a retrieval path is new work.

### [tools-missions] Derive track_target standoff server-side from the threat ring and verify it with LOS
- plan: `PLAN §4.3 mission_track_target / M5` | state: **partial** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: missions.py:161-169 — 'orbit_poi' and 'track_target' share one branch: it requires kw['lat']/kw['lon'], takes radius_m from the caller (default 80.0 m), and emits a one-shot 12-point ring. No track_id param, no reference to threat._OOB envelopes, no pixel-density math, no LOS check (simTestLineOfSightToPoint appears only in the fake sim, fake_airsim.py:530 — no MCP tool or backend method calls it), no re-path loop.
- required: M5: params are (vehicle, track_id, alt_m) with standoff DERIVED by the server from the track's threat ring (threat.py:20-30 already holds envelope_m) and from narrow-FOV pixel-density-on-target, then verified against a LOS check before acceptance; plus a server re-path loop so a moving track stays centred. Today the agent picks its own standoff (exactly the 'agent learns bad doctrine' failure PLAN §9 says M1-M5 exist to prevent), must resolve track_id->lat/lon itself, and the ring is computed once so a moving contact is lost.
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/threat.py
- scope correction: No correction needed — the gap stands as written. For planning precision, the real remaining work is five distinct pieces, four of which are state (a) MISSING and one state (b) PARTIAL: (a) a `track_id` param on the track_target branch that resolves via self.tracks.get() (targets.py:125-126 already provides the lookup, and Track carries lat/lon/heading_deg/speed_mps at targets.py:38-52); (a) a narrow-FOV pixel-density/GSD term to floor the standoff — nothing like it exists; (a) a `uav_los_check` MCP tool plus a `UavBackend.los_check()` that actually calls simTestLineOfSightToPoint (the sim-side stub at fake_airsim.py:530 exists but is dead code, so only the wiring is needed against the fake; real AirSim behavior is unverified); (a) a server re-path loop so a moving contact stays centred; (b) the threat-ring derivation itself is the one partial — the formula `envelope_m * 1.1` already exists at threat.py:149 and can be lifted into missions.py, and while doing so the hardcoded `radius_m=150.0` at server.py:495 should be replaced with the derived value so the M10 handoff path stops flying a magic number too.

### [tools-missions] Implement wide→narrow FOV cross-cue for mission_identify_target
- plan: `PLAN §4.3 mission_identify_target / M7` | state: **partial** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: missions.py:170-176 kind 'assess' = fixed 6-point ring at radius_m 50, alt 45, meta {'doctrine':'assess'} — no FOV change, no slant-range reduction, no orbit_first param, no track_id. server.py:416-426 uav_identify_target(track_id) only reformats an existing track via targets.salute_report (targets.py:129-145) — it does not fly, capture, or cross-cue, and its docstring defers cross-cue to uav_mission assess, which does not do it either. simSetCameraFov is called nowhere in the repo and no uav_set_fov tool is registered (server.py:317-544).
- required: M7: wide-FOV detect, then cross-cue narrow FOV at reduced slant range, returning {classification, confidence, geo_point, track_history, key_images}. salute_report returns no confidence, no track_history, and no key_images (targets.py:131-145). Without any FOV control primitive the cross-cue is not achievable by the mission layer or by the harness composing raw tools.
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/targets.py

### [tools-missions] Tick the fuel integrator in flight and enforce the in-flight BINGO force-RTB
- plan: `PLAN §4.5 BINGO doctrine (M4) / fuel integrator (T5)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: safety.py:198-206 FuelModel.tick() is fully implemented but has ZERO production callers — grep across mcp/ finds it only in tests/test_safety.py:76-84. store.log_fuel (store.py:76) is likewise never called outside tests. So fuel_pct stays 100.0 for the life of the process (safety.py:174). No BINGO monitor, no force-RTB transition exists anywhere (grep for rtb/force_rtb in mcp/ returns only the uav_return_to_home tool, server.py:325-327).
- required: M4/T5: each telemetry tick recomputes phase from measured climb/cruise and decrements fuel, persisted to fuel.jsonl; reaching BINGO triggers an un-cancellable force-RTB and flags the mission 'incomplete — fuel'. As shipped, the gate at server.py:298-307 always compares plan cost against a constant 100%, so the second, third and tenth mission of a session all pass a gate that is doctrinally a no-op, and a drone can run to empty with no safety transition.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/safety.py
- scope correction: Gap stands as written. Only nuance worth recording: the fuel MATH is complete and unit-tested (tick, classify_phase, estimate_route, bingo_fuel_pct, preflight_gate in safety.py:181-273), so the remaining work is wiring rather than modeling — (1) call fm.tick(speed_mps, vz_mps, landed, headwind) from a per-vehicle telemetry loop (server.py _wait_arrival:149-161 or a new monitor task) and persist via store.log_fuel; (2) compare fuel_pct against fm.bingo_fuel_pct() each tick and, on breach, abort the queue and submit an un-cancellable uav_return_to_home; (3) plumb headwind from sim_set_environment (server.py:509-519) into tick/preflight_gate, which are already parameterized for it. Item (2) additionally needs a mission-state record to flag 'incomplete — fuel', since store.log_mission (store.py:70) is currently unused and no mission_status tool exists.

### [tools-missions] Derive mission progress from telemetry and emit progress notifications
- plan: `PLAN §4 intro (task_id + notifications/progress, T4a; progress from telemetry, T2) / §4.3 mission_status` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: tasking.py:48 progress_pct: float = 0.0 and tasking.py:58 echoes it into handle() — no code anywhere assigns to it (grep 'progress' over mcp/ returns only these two lines plus a store helper). No progressToken, report_progress, or notifications/progress machinery exists in mcp/godseye_uav at all. The only telemetry-derived signal is the arrival poll inside UavBackend._wait_arrival (server.py:149-161), which is per-leg command-ack and is never turned into a percentage.
- required: T2/§4.3: mission_status must return progress_pct derived from distance-to-target vs plan, plus current waypoint, eta_s, fuel and bingo_fuel_pct. Today uav_task_status (server.py:538-544) returns queue state with progress_pct hard 0.0 until the task flips to done, so the PLAN §5 workflow step 'monitor progress notifications' and the §3.1 HUD progress panel have no data source.
- files: godseye/mcp/godseye_uav/tasking.py, godseye/mcp/godseye_uav/server.py

### [tools-sim-admin] Fix sim_spawn_target default altitude — targets spawn underground and are undetectable
- plan: `PLAN §4.4 sim_spawn_target; Phase 4 exit "Agent identifies spawned target"` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: True
- exists: godseye/mcp/godseye_uav/server.py:443 `sim_spawn_target(name, mesh, lat, lon, alt_m=0.0)` -> server.py:185-191 `spawn_object()` calls `self.llh_to_ned(lat, lon, alt_m)`, i.e. alt_m is treated as ABSOLUTE geodetic altitude (geo.py:117-119 geodetic_to_ned), not AGL/ground. Fake detection filter radius is 500 m (fake_airsim.py:448).
- required: A spawn with no altitude must land the target on the ground near home so `uav_scan_targets` can see it. With start.sh's default theater `iran-isfahan` (start.sh:18) whose home altitude is 1550.0 m (launch.py:35), a default `sim_spawn_target(name=..., mesh=..., lat=..., lon=...)` produces NED z ≈ +1550, i.e. 1550 m below the drone — outside the 500 m detection radius, so `uav_scan_targets` (server.py:404) returns `detections: 0` with no error and no track is ever created. Same for indo-pak-loc (home 1600, launch.py:33). Tests pass only because they hand-pick `alt_m=93.0` against home 122.0 (tests/test_targets.py:112-113, :130-131). Fix: default alt to home altitude / accept AGL, or reject out-of-envelope spawns loudly.
- files: godseye/mcp/godseye_uav/server.py, godseye/tests/test_targets.py
- scope correction: sim_spawn_target IS broken, so the dimension is not clean — but not in the way the finding states, and the stated fix would accomplish nothing. Rewrite the gap as:

REAL DEFECT: fake_airsim.py:491 (and the identical fake_airsim.py:500-505 simSetObjectPose) read the msgpack pose map with str keys while the wire delivers bytes keys, so the position default {0,0,0} is always used and every spawned target lands at home lat/lon/home-altitude regardless of the lat, lon, and alt_m the agent passed. Targets are not "undetectable" — they are all silently stacked on top of the drone's home point.

REAL FIX (small, ~2 lines): normalise the map keys in fake_airsim.py:491 and :503 before reading them (extend the existing _s() at fake_airsim.py:558-562 to a key-decoding helper, or construct the fake's Server with an unpacker using raw=False / strict_map_key handling so keys arrive as str). Then add a regression test that spawns a target at a known offset (e.g. ~120 m east of home) and asserts the resulting track's reported lat/lon actually match the requested position within a few metres — no current test does this, which is why 106 tests pass over the bug.

SEPARATE, SMALLER, GENUINELY OPEN ITEM (keep, but demote): once positions work, alt_m at server.py:443-445 is still absolute-geodetic, and its 0.0 default is wrong for every non-sea-level theater (iran-isfahan 1550, indo-pak-loc 1600, ukraine-donbas 200 at launch.py:33-40). Route spawn through the already-existing AGL helper server.py:63-67 llh_agl_to_ned, or default alt_m to the envelope home altitude. This is a one-line change and only becomes observable after the key-decoding fix lands.

ALSO NOTE (out of scope of this gap but adjacent): PLAN.md:157 sim_move_target is not registered as a tool at all (see the tool list at server.py:317-538), and its would-be backing call simSetObjectPose carries the same bytes-key defect.

### [tools-sim-admin] Add sim_move_target and make spawned targets actually move
- plan: `PLAN §4.4 sim_move_target(target_id, waypoints, speed); Phase 6 "moving convoys"` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: nothing. The full tool list is godseye/mcp/godseye_uav/server.py:317-544 (19 tools; only sim_spawn_target:440 and sim_set_environment:506 are sim-admin). `UavBackend` has only `spawn_object` (server.py:185-191) — no pose/move wrapper. The fake DOES implement the underlying RPC `simSetObjectPose` (fake_airsim.py:500-507), so the wire surface exists but is unreachable.
- required: A `sim_move_target(target_id, waypoints, speed)` tool plus continuous target motion in the zero-GPU backend. The fake's physics integrator iterates only `self._vehicles` (fake_airsim.py:138 `for v in self._vehicles.values()`); `self._objects` (fake_airsim.py:86) is a static dict of GeoPoints that is never advanced, so even a teleport-only tool would not give a convoy. Without motion, `Track.update()` never sets speed/heading (targets.py:55-74), SALUTE activity degrades to the literal string "observed" (targets.py:148-153), the intent half of the M13 threat pipeline has no movement to score, and pattern-of-life (M12) has no signal.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py

### [tools-sim-admin] Wire the wind vector into the fuel model — M15 coupling is dead code
- plan: `PLAN §4.4 sim_set_weather "wind vector (feeds fuel model, M15)"; §4.5 fuel integrator T5` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: Wind plumbing works end-to-end as far as the sim: server.py:509-519 `sim_set_environment` -> `client.simSetWind(Vector3r(...))` -> fake handler fake_airsim.py:518-522 -> `set_wind` fake_airsim.py:212-215 -> applied to airborne vehicles in `_integrate` fake_airsim.py:141-148. The fuel model accepts wind: `FuelModel._burn(phase, dt, headwind_mps)` safety.py:192-196, `tick` :198, `estimate_route` :208, `bingo_fuel_pct` :242, `preflight_gate` :249.
- required: The wind set by the tool must reach the fuel math. It does not: `sim_set_environment` stores wind only in the audit log (server.py:535) — nothing on the server object — and every fuel call site omits the argument, so `headwind_mps` stays at its 0.0 default: `self._gate` calls `fm.preflight_gate(waypoints, start, speed)` (server.py:301) and `uav_get_telemetry` calls `fm.bingo_fuel_pct((lat, lon), alt_hae)` (server.py:363). Worse, `FuelModel.tick()` (safety.py:198) has NO production caller anywhere in mcp/godseye_uav — only tests/test_safety.py:76-84 — so the live integrator never runs and `fuel_pct` is pinned at its 100.0 initial value (safety.py:174) for the whole mission. Setting wind therefore changes drift only; it can never move fuel, BINGO, or a gate decision.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/safety.py, godseye/mcp/godseye_uav/bridge.py

### [tools-sim-admin] Add sim_set_link_state (degraded|lost, duration_s) and the lost-link model
- plan: `PLAN §4.4 sim_set_link_state; §4.5 M9 lost_link_plan; §3.1 "≥3 alarm types" incl. lost-link` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: nothing. No tool (server.py:317-544). A repo-wide grep for `sim_set_link|link_state|lost_link|lostlink` across *.py/*.sh/*.js returns zero hits. The fake's RPC dispatch (fake_airsim.py:252-533) has no link/comms concept at all.
- required: A tool to force a degraded/lost datalink for a duration, plus the server-side per-mission `lost_link_plan` (hold-orbit / climb-for-LOS / RTB / continue) that PLAN §4.5 says the server executes autonomously on link loss, with LOAL events logged to INTREP. None of it exists, so the Phase-5 deliverable and the §3.1 command-center lost-link alarm cannot be demonstrated, and the harness has no way to practise the degraded-comms doctrine the skill is supposed to teach.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py, godseye/mcp/godseye_uav/safety.py, godseye/mcp/godseye_uav/missions.py

### [safety-fuel] Wire the fuel integrator into a telemetry tick loop
- plan: `PLAN.md §4.5 Fuel integrator (T5); REVIEW_TECHNICAL.md T5` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: mcp/godseye_uav/safety.py:198 `FuelModel.tick()` is implemented and correct (phase recomputed from MEASURED speed/vz at safety.py:181-190), but it has ZERO production callers — the only callers are tests/test_safety.py:76,77,84. The single per-tick loop in the system, mcp/godseye_uav/bridge.py:175-185 `telemetry_loop`, holds no FuelModel reference; server.py has no background loop (only `_wait_arrival`'s 4 Hz poll at server.py:149-161, which does not tick fuel). launch.py:74-87 constructs the bridge from an AirSimAdapter only, so bridge and GodseyeUavServer share no fuel state at all.
- required: Per telemetry tick: recompute phase from measured climb/cruise and apply `fuel -= rate(phase, wind)*dt`, as PLAN.md:167 states. A shared FuelModel per vehicle must be driven by the telemetry loop.
- files: mcp/godseye_uav/bridge.py, mcp/godseye_uav/server.py, mcp/godseye_uav/launch.py, mcp/godseye_uav/safety.py

### [safety-fuel] Force RTB at BINGO as an un-cancellable safety transition
- plan: `PLAN.md §4.5 BINGO doctrine (M4) + T5; REVIEW_MILITARY.md:16-17` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: Nothing. Grep for rtb/force/incomplete/cancellable across mcp/godseye_uav/*.py returns only docstrings: safety.py:12 ("Reaching BINGO in flight = force-RTB (un-cancellable safety transition)") and safety.py:243. No code compares live `fuel_pct` against `bingo_fuel_pct`; no code pre-empts or clears the VehicleQueue for a safety transition (tasking.py:180-204 `cancel_current`/`abort` are harness-initiated only and have no privileged/un-cancellable mode); no mission is ever flagged "incomplete — fuel" (no such string anywhere). `uav_return_to_home` (server.py:325-327) is an ordinary harness-submitted FIFO task that `uav_abort` (server.py:351-356) can cancel like any other.
- required: Server-side monitor: on reaching BINGO, pre-empt and clear the queue, submit an RTB that harness cancel/abort cannot cancel (operator-override only), and mark the mission "incomplete — fuel".
- files: mcp/godseye_uav/server.py, mcp/godseye_uav/tasking.py, mcp/godseye_uav/safety.py
- scope correction: Gap is real and slightly UNDERSTATED by the prior auditor. Required work is the claimed monitor plus a prerequisite the claim omitted: (a) call FuelModel.tick() from a live telemetry path (no production caller exists today, so fuel_pct is pinned at 100.0 and BINGO can never trigger); (b) persist ticks via the already-dead store.log_fuel (store.py:76); (c) add a server-side monitor comparing fm.fuel_pct against fm.bingo_fuel_pct(current_pos, alt); (d) add a privileged/safety flag to Task (tasking.py:37-61) and make cancel_current/abort (tasking.py:180-204) and uav_abort (server.py:351-356) refuse to cancel it (operator-override only); (e) pre-empt and drain the VehicleQueue, then submit the privileged uav_return_to_home; (f) introduce a mission record at all — store.log_mission (store.py:70) is never called and uav_mission (server.py:378-399) persists nothing — so that a mission can be flagged "incomplete — fuel"; and emit the bingo audit event that only tests/test_store.py:39 currently fabricates.

### [safety-fuel] Implement the lost-link plan (M9) end to end
- plan: `PLAN.md §4.5 Lost-link (M9), §4.4 `sim_set_link_state`, §4.5 "Harness disconnect → lost_link_plan"` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: Nothing in code. Grep for lost_link/LOAL/link_state across all .py/.js files matches only PLAN.md:160,168,169,176,180 and REVIEW_MILITARY.md:33-34. No `lost_link_plan` parameter on any mission (missions.py:143-177 `plan_mission` accepts none; MissionPlan at missions.py:130-140 has no such field); no `sim_set_link_state` tool (the 19 registered tools are listed at server.py:317-538); no link model in the fake sim (fake_airsim.py exposes only set_wind:212, set_gps_denied:217, set_gps_noise:222, set_detection_realism:227); no LOAL events in targets.py INTREP; no harness-disconnect hook anywhere.
- required: Per-mission `lost_link_plan` (hold-orbit / climb-for-LOS / RTB / continue) executed autonomously by the server on link loss, a `sim_set_link_state` tool + fake-sim link model to induce it, LOAL events logged into the INTREP, and harness disconnect as a trigger.
- files: mcp/godseye_uav/server.py, mcp/godseye_uav/missions.py, mcp/godseye_uav/fake_airsim.py, mcp/godseye_uav/targets.py, mcp/godseye_uav/store.py

### [safety-fuel] Persist fuel integrals to JSONL
- plan: `PLAN.md §4.5 ("persist (JSONL)") and §4.8; REVIEW_TECHNICAL.md T5/T4c` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: mcp/godseye_uav/store.py:76-77 `Store.log_fuel()` and the `fuel.jsonl` journal (store.py:65) are implemented, but have zero production callers — the only caller is tests/test_store.py:40. No fuel record is ever written during a mission, so restart-replay (PLAN.md:180) cannot recover the fuel clock.
- required: Each integrator tick (and each BINGO/safety transition) appends a fuel record; restart replays it to restore fuel state before deciding resume-or-RTH.
- files: mcp/godseye_uav/server.py, mcp/godseye_uav/bridge.py, mcp/godseye_uav/store.py
- scope correction: The gap is real and is UNDERSTATED, not overstated. Fixing it requires more than adding a log_fuel call: (1) there is no production caller of FuelModel.tick() at all (safety.py:198), so a telemetry/integrator loop must first be created and wired (server.py has only request-scoped tool handlers, no background loop; launch.py:76-97 starts no ticker); (2) bridge.py:46 must source fuel_pct from that shared FuelModel instead of the hardcoded 100.0; (3) only then can each tick and each BINGO/safety transition append to Store.log_fuel (store.py:76); (4) a restart-replay/restore step must be added — nothing in launch.py:76-77 or GodseyeUavServer.__init__ (server.py:197-223) reads any journal back. Note also that no BINGO runtime monitor or force-RTB safety transition exists (only the pre-flight gate at server.py:298-307), so the "each BINGO transition appends a record" half of the requirement has no transition to hook.

### [bridge-command-center] Populate /mission-overlay with real mission GeoJSON (route, waypoints, grid, geofence, threat rings, target markers)
- plan: `PLAN §3.1 feed table row 2 + §3 'Mission overlays' (T6)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: mcp/godseye_uav/bridge.py:172 initializes `self.mission_overlay = {"type":"FeatureCollection","features":[]}` and bridge.py:275-277 serves it verbatim. A repo-wide grep for `mission_overlay` returns exactly three hits (bridge.py:172, 276, 277) plus tests/test_bridge.py:94-97, which only asserts `type == 'FeatureCollection'` — there is NO writer anywhere, no POST/PUT to publish overlay geometry, and create_app (bridge.py:226-229) receives no mission/plan handle. The planned geometry is computed in missions.py:143-177 (plan_mission) and then discarded: server.py:390 converts it to `waypoints` for uav_fly_route and keeps only kind/doctrine/waypoint_count in the handle.
- required: /mission-overlay must emit GeoJSON features for the planned route polyline, waypoints, grid/lawnmower pattern, geofence polygon, threat rings and target markers carrying track IDs, refreshed on mission-state change, so the GEV CustomDataSource can render the mission as the harness flies it.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/missions.py, gods-eye-view/src/ui/uavMissionPanel.js

### [bridge-command-center] Add missions[] section to /snapshot (phase, active tool, progress %, fuel vs BINGO, safety state) at 1 Hz
- plan: `PLAN §3.1 feed table row 3 + 'Implementation notes' bullet 1` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: nothing. mcp/godseye_uav/bridge.py:258-266 returns only {sim_state, observedAtMs, count, vehicles}; there is no `missions` key in the bridge (grep for 'missions' in mcp/ hits only store.py journals and targets.py). The closest upstream state, tasking.TaskingService.status() (tasking.py:292-299), is reachable from the bridge only through the loopback MCP proxy, and its progress figure is fake: Task.progress_pct is declared at tasking.py:48 and emitted at tasking.py:58 but a repo-wide grep for an assignment to `.progress_pct` returns zero hits, so every handle reports 0.0.
- required: /snapshot.missions[] with per-mission phase, active tool, task progress %, fuel % vs bingo_fuel_pct, and safety/geofence state, updated at ~1 Hz — plus an actual progress computation (distance-to-target vs plan from telemetry, per T2/§4) rather than a constant 0.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/tasking.py, godseye/mcp/godseye_uav/server.py, godseye/tests/test_bridge.py

### [geo-datum] Repair the EGM96 geoid path — pyproj pipeline always fails and silently falls back to a latitude-only fudge
- plan: `PLAN.md:83 (§3 Datum, T1) 'hae = alt_msl + N(φ,λ) using egm96 evaluated per-sample'; REVIEW_TECHNICAL.md:10 (T1)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: godseye/mcp/godseye_uav/geo.py:128-161. geoid_undulation() builds a pyproj pipeline with a BARE grid filename (geo.py:144-146: '+grids=us_nga_egm96_15.tif') and calls pyproj.datadir.set_data_dir(grid_dir) on a directory that contains no proj.db. Verified at runtime: PROJ raises 'Error 1029 (File not found or invalid): vgridshift: could not find required grid(s)'. That exception is swallowed by the bare `except Exception: pass` at geo.py:151-152, so EVERY call silently returns _coarse_undulation() (geo.py:157-161), a latitude-only expression -30+20cos(2φ)+5sin(3φ) with ZERO longitude dependence. Confirmed empirically: geoid_undulation() == _coarse_undulation() for every point tested. pyproj 3.8.0 IS installed and the real grid IS shipped at mcp/godseye_uav/data/us_nga_egm96_15.tif (2.7 MB) — both unused. Using an absolute path in +grids makes the pipeline work immediately.
- required: Per-sample EGM96 undulation accurate enough for the plan's ±10 m vertical gate. Measured error of the shipped fallback vs true EGM96 at the theaters launch.py actually ships: indo-pak-loc +15.5 m, iran-isfahan -19.0 m, taiwan-strait -26.4 m, ukraine-donbas -44.1 m, red-sea-hormuz +17.2 m — 5 of 6 FAIL the ±10 m gate; only Seattle passes (-6.6 m), because the fudge constants were fitted to Seattle. Fix: absolute path in +grids, drop the set_data_dir call, and make a geoid failure raise rather than silently degrade (geo.py:159's claim 'never used for the geo gate' is false — it is the only path that ever runs).
- files: godseye/mcp/godseye_uav/geo.py, godseye/tests/test_geo.py, godseye/tests/test_bridge.py
- scope correction: NOT overstated - scope is LARGER than the prior auditor stated. Their proposed fix ("absolute path in +grids, drop set_data_dir, raise on failure") is necessary but INSUFFICIENT, because it leaves geo.py:150's sign flip in place. I computed what their literal fix produces: indo-pak err +66.38 FAIL, iran -3.16 PASS, taiwan -28.96 FAIL, ukraine -28.77 FAIL, hormuz +60.62 FAIL, Seattle +44.42 FAIL - 5 of 6 fail, and it is WORSE than the current fallback at Seattle (44.4 m vs 6.6 m) and Hormuz (60.6 m vs 17.2 m). Complete fix: (a) geo.py:144-146 use os.path.join(grid_dir,'us_nga_egm96_15.tif') absolute path in +grids; (b) delete the pyproj.datadir.set_data_dir(grid_dir) call at geo.py:142 (it repoints PROJ away from proj.db); (c) geo.py:150 return float(h_egm96), NOT -float(h_egm96), so N is positive where the geoid is above the ellipsoid, matching PLAN.md:83 / geo.py:165 / gods-eye-view/src/data/geoid.js:68; (d) fix the contradictory docstring at geo.py:9 ('altHae = altMSL - geoid_undulation' should be '+'); (e) replace the bare `except Exception: pass` at geo.py:151-152 with a raise (or an explicit opt-in degraded flag) so a missing grid cannot silently degrade; (f) re-baseline tests/test_geo.py:109-113, which asserts abs(n-(-28.8))<3.0 - that is the FALLBACK's Seattle value, not EGM96's (-22.21), so this test is fitted to the broken path and will fail once the grid works (|-22.21+28.8|=6.59 > 3.0); it must be changed to expect ~-22.2. (g) tests/test_geo_gate.py:5 claims it 'Runs offline against the bundled EGM96 grid' but the file (lines 39-58) never calls any geoid function - the +/-10 m vertical gate is a pure NED round-trip and does not actually exercise the datum at all, so the gate must be extended to cover the geoid before it can be trusted as the T7 hard gate.

### [geo-datum] Collapse the two altHae conversion points — MCP server and bridge disagree by 28.8 m on the same drone
- plan: `PLAN.md:83 (§3 Datum, T1) 'ONE conversion point in the bridge — never per-call-site'; PLAN.md:137 (§4.2 uav_get_telemetry returns altHae)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: TWO independent conversions. (1) Bridge: godseye/mcp/godseye_uav/bridge.py:117-120 does ned_to_geodetic then alt_hae = msl_to_hae(alt_msl, lat, lon) — geoid applied. (2) MCP server: godseye/mcp/godseye_uav/server.py:69-71 ned_to_llh() returns geo.altitude raw, and server.py:113 publishes it under the key "alt_hae", with server.py:358 advertising the tool as returning 'lat/lon/altHae'. No geoid is ever applied on the server path — geo.py's msl_to_hae is not imported by server.py (server.py:22 imports only GeoPoint, HomeGeoPoint, NedPoint, geodetic_to_ned, ned_to_geodetic). Verified numerically for a drone at NED z=-100 from the default home: uav_get_telemetry alt_hae = 222.000, bridge /snapshot alt_hae = 193.173 — a 28.827 m divergence on the same vehicle at the same instant.
- required: The harness reads uav_get_telemetry and the operator watches /snapshot in GEV; those two must be the same number or the skill's altitude reasoning silently disagrees with the C2 display by ~29 m (more at other theaters). Either route server.py's telemetry through the bridge's single conversion, or move the conversion into geo.py and have both call it. Note the mislabel is the dangerous part: server.py:113 names a non-geoid-corrected value 'alt_hae', so it is not a benign rename — it is a wrong value under the canonical name. That mislabeled value is then consumed as if canonical at server.py:363 (bingo_fuel_pct) and server.py:458 (threat observer alt_m).
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/geo.py
- scope correction: Core gap and prescribed fix stand exactly as claimed (route server.py telemetry through the bridge's single conversion, or move it into geo.py and have both call it). ONE consequence sub-claim is overstated: the two named downstream consumers are inert today — safety.py:242 `bingo_fuel_pct(self, at, alt_m, ...)` never reads alt_m in its body (lines 243-247 route to estimate_route with alt_m 0.0), and threat.py:41-88 assess_track reads only observer["lat"]/["lon"], never observer["alt_m"]. So server.py:363 and server.py:458 pass a wrong value that is currently discarded; the mislabel is a landmine for the next consumer, not live corruption of fuel or threat math. This does not shrink the required work.

### [geo-datum] Fix the latent sign inversion in the EGM96 branch that will inject a 44 m error the moment the geoid is repaired
- plan: `PLAN.md:83 'hae = alt_msl + N(φ,λ)'` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: godseye/mcp/godseye_uav/geo.py:150 returns `-float(h_egm96)`. PROJ's vgridshift forward transform of z=0 (MSL) returns exactly N: verified at Seattle it returns -22.208, the true EGM96 undulation. So geo.py:150 returns -N, and msl_to_hae (geo.py:165) then computes alt_msl + (-N) = alt_msl - N — the opposite of the plan. The module docstring at geo.py:9 and the comment at geo.py:124 both state 'altHae = altMSL - N', contradicting geo.py:165 and PLAN.md:83. The coarse fallback (geo.py:157-161) happens to return N with the CORRECT sign, which is why the bug is invisible today: the only live path is the fallback.
- required: altHae = altMSL + N with a single consistent sign convention, and the docstring/comment corrected to match. Verified consequence if someone fixes the pyproj path (finding above) without also fixing this line: msl_to_hae(122.0, Seattle) returns 144.208 instead of the correct 99.792 — a 2·|N| = 44.4 m error, and larger elsewhere. This is a trap for whoever repairs the geoid, so it must be fixed in the same change.
- files: godseye/mcp/godseye_uav/geo.py
- scope correction: Verdict is CONFIRMED — this is a scope ADDITION, not a reduction. The real fix is slightly LARGER than the prior auditor described, because a third file must change in the same commit:

1. geo.py:150 — change `return -float(h_egm96)` to `return float(h_egm96)`.
2. geo.py:9 and geo.py:124 — correct the prose to `altHae = altMSL + N` / `altMSL = altHae - N` so it matches geo.py:165/169, PLAN.md:83, and gods-eye-view/src/data/geoid.js:99.
3. NEW (missed by the prior auditor): tests/test_geo.py:112-113 asserts `abs(n - (-28.8)) < 3.0`. The -28.8 is the COARSE FALLBACK value (geo.py:157-161), not the true EGM96 value. `pytest.importorskip("pyproj")` at tests/test_geo.py:110 does NOT skip — pyproj 3.8.0 is installed in .venv — so this test runs today and passes ONLY because the pyproj branch throws. Verified by running it: 3 passed. After a correct repair (grid path fixed AND sign fixed) n becomes -22.21, and |-22.21 - (-28.8)| = 6.59 > 3.0, so THE TEST FAILS ON THE CORRECT FIX. Its expectation must be retargeted to approximately -22.2 (tolerance ~1.0) in the same change, otherwise the test actively punishes the right answer and rewards nothing — it cannot currently distinguish a correct fix from the sign bug, since both make it fail.

### [geo-datum] Make the Phase-0 T7 geo-registration gate an actual registration test — it currently tests nothing but its own arithmetic
- plan: `PLAN.md:196 (Phase 0) 'geo-registration gate test (T7): scripted marker round-trip ≤5 m horiz / ≤10 m vert'; REVIEW_TECHNICAL.md:38` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: godseye/tests/test_geo_gate.py:39-58. test_marker_roundtrip_within_gate() is named for a marker but spawns none. It computes ned_to_geodetic -> geodetic_to_ned -> ned_to_geodetic purely in-process (lines 42-44) and asserts the two geodetic results agree. No sim object, no simSpawnObject/simGetObjectPose, no FakeAirSim, no bridge, no /snapshot in the loop. Critically, the altitude assertion at line 46 compares gp.altitude to gp2.altitude — both are home.altitude - z from the same function, so the vertical tolerance is satisfied by construction and NO geoid, MSL, or HAE conversion is exercised anywhere in the gate. The only real failure mode it can catch is an ellipsoidal/spherical mismatch between two functions it already knows about.
- required: A round-trip that crosses the system boundary the gate exists to protect: place a marker at a known geodetic point in the sim (FakeAirSim already supports it — simSpawnObject stores objects via ned_to_geodetic at fake_airsim.py:505, and simGetObjectPose round-trips at fake_airsim.py:493), read it back through the bridge's published geo, and assert ≤5 m horizontal / ≤10 m vertical INCLUDING the MSL->HAE conversion. Without the altitude datum in the loop, the gate cannot detect any of the three geoid defects above — all of which are currently green.
- files: godseye/tests/test_geo_gate.py, godseye/mcp/godseye_uav/fake_airsim.py
- scope correction: Verdict is CONFIRMED — scope is NOT overstated. But two errors in the prior auditor's rationale would misdirect whoever implements the fix, and one sub-claim is too strong:

1. FACTUAL ERROR: "simGetObjectPose round-trips at fake_airsim.py:493" — simGetObjectPose does not exist anywhere in the repo (grep over mcp/ and tests/ returns zero matches). FakeAirSim exposes simGetDetections (fake_airsim.py:437), simSpawnObject (:488), simSetObjectPose (:500) — there is no pose getter. The read-back leg must go through simGetDetections (as REVIEW_TECHNICAL.md:35 actually prescribes), or a simGetObjectPose must be added to the fake first. Also, the cited :505 is inside simSetObjectPose; spawn stores its geodetic at :494.

2. OVERSTATED SUB-CLAIM: "the vertical tolerance is satisfied by construction ... both are home.altitude - z from the same function". Not tautological. gp.altitude uses the input v.z, while gp2.altitude uses back.z recovered through the ellipsoidal ECEF path (geo.py:84-119), a genuinely different computation. The assertion is weak and geoid-free, but it is not vacuous — it would catch a sign or rotation error in ecef_to_ned's down component. The correct statement is narrower: the gate never touches EGM96, so it cannot catch any MSL/HAE datum defect.

3. PARTIAL CREDIT NOT GIVEN: the MSL->HAE conversion is exercised, on the vehicle path, at tests/test_bridge.py:53-58 — that satisfies the PLAN's separate T1 datum test, not T7.

Real remaining work (same size as claimed): one test that spawns a marker at a known WGS84 point through the MCP/adapter boundary (server.py:190 spawn_object), reads it back via simGetDetections through the bridge's published geo, and asserts <=5 m horizontal / <=10 m vertical with the geoid in the loop. Doing so will fail until server.py:113 and the detection path apply msl_to_hae like bridge.py:120 does — which is the point of the gate.

### [tasking-persistence-auth] Raise the task watchdog off total duration and add hover-recovery on timeout
- plan: `PLAN §4.5 Watchdog (T2); §4.3 mission primitives` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: tasking.py:158 `result = await asyncio.wait_for(runner, timeout=self.watchdog_s)` wraps the WHOLE task, not a no-progress interval; server.py:204 `watchdog_s: float = 120.0`; launch.py:77 constructs GodseyeUavServer without overriding it. server.py:149-150 `_wait_arrival(..., timeout_s: float = 180.0)` is a second, also-too-short cap. On timeout tasking.py:166-170 sets FAILED and cancels the runner; server.py:274-282 then only calls `backend.cancel_last(v)` — nothing ever commands hover.
- required: PLAN §4.5: 'future not resolved in timeout -> fail handle -> hover-recovery (T2)'. The watchdog must bound *absence of telemetry progress*, not wall-clock mission duration, and must leave the vehicle hovering. Arithmetic on the shipped default theater (start.sh THEATER=iran-isfahan; launch.py:35-36 AO 51.66-51.78 lon at lat 33.72 = ~11.1 km wide): one `uav_goto_gps` crossing at the 10 m/s default (server.py:330) takes ~1110 s, and a `grid_search` at the 8 m/s default (missions.py:158) with up to 12 lanes (missions.py:28) takes hours. FakeAirSim flies in real time at <=20 m/s (fake_airsim.py:27,171-172), so every one of these dies at t=120 s with `watchdog timeout after 120.0s` and the drone is left mid-air under no command.
- files: godseye/mcp/godseye_uav/tasking.py, godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/launch.py
- scope correction: Gap is real; the fix is smaller than a watchdog re-architecture. Minimum work: (1) plumb a per-task deadline instead of the fixed 120 s — the pre-flight gate already computes `est_time_s` (safety.py:271, surfaced at server.py:331/340), so pass it (with a margin, e.g. 2x + 60 s) into `TaskingService.submit`/`Task` and have tasking.py:158 use `task.deadline_s or self.watchdog_s`; alternatively reset the deadline on telemetry progress if a true no-progress watchdog is wanted. (2) add hover-recovery on the timeout path: `await self.backend.hover(v)` after `cancel_last` in server.py:274-280 (or in tasking.py:166-170 via a recovery hook), mirroring the existing uav_abort cancel+hover at server.py:268-271. (3) raise/derive `_wait_arrival`'s 180 s cap (server.py:150) from the same leg estimate so it stops being dead code. (4) expose a `--watchdog` flag in launch.py:48-58 and start.sh for operator override. A regression test belongs next to tests/test_tasking.py:67.

### [tasking-persistence-auth] Return 'busy' + the current handle when a vehicle is already executing
- plan: `PLAN §1 Hard rules line 50 (T2); §4.1` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: True
- exists: `VehicleBusyError` is defined at tasking.py:67-70 and never raised anywhere in the repo. `VehicleQueue.submit` (tasking.py:116-125) unconditionally enqueues; `GodseyeUavServer._submit` (server.py:309-312) returns that fresh handle. Verified at runtime: submitting a second command while one executes returns `{'task_id': '0298d7d834d0', 'state': 'queued', ...}` — a brand-new id, no `busy` flag, no reference to the in-flight task.
- required: PLAN line 50: 'One in-flight command per vehicle... Busy commands return `busy` + current handle — harness decides.' A harness currently cannot distinguish 'my command is flying now' from 'my command is parked behind someone else's', and has no pointer to the blocking task to cancel or abort it. Every flight tool at server.py:317-356 and uav_mission at server.py:378 is affected.
- files: godseye/mcp/godseye_uav/tasking.py, godseye/mcp/godseye_uav/server.py
- scope correction: The gap is real but is ~10 lines at one choke point, not a rework of the tasking layer or of nine tools. Everything the fix needs already exists and works: the one-in-flight state machine (tasking.py:142-178), the `current` handle accessor (tasking.py:292-299), the `VehicleBusyError` class itself (tasking.py:67-70), the three tools that already surface the blocking handle (server.py:365, 367-370, 538-544), and a vehicle-scoped abort that needs no task_id (server.py:351-356).

What is actually left:
1. tasking.py:116 `VehicleQueue.submit` — add a guard before `put_nowait`: if `self._current is not None and self._current.state not in TERMINAL`, raise `VehicleBusyError(self._current)` (the class is already written for exactly this) instead of enqueuing. Decide FIFO-vs-reject deliberately: PLAN.md:50 names both a "FIFO queue" and a busy return, REVIEW_TECHNICAL.md:14 says reject — reject is the resolving intent.
2. server.py:309-312 `_submit` — wrap in try/except VehicleBusyError and return `{"busy": True, "current": exc.current.handle(), "rejected_tool": tool}`. All nine flight tools plus `uav_mission` (server.py:378-398) and `uav_handoff_target` (server.py:500) inherit it for free; no per-tool edits.
3. tasking.py:209-215 `VehicleQueue.get` — also scan `self._queue._queue` so a queued task_id resolves instead of returning None (fixes the false "unknown task_id" from uav_task_status).
4. One contract test in tests/test_tasking.py asserting submit-while-executing returns busy + the in-flight task_id (REVIEW_TECHNICAL.md:38 lists "queue-busy rejection" as a required P2 contract test; no such test exists in tests/test_tasking.py today).

Impact on the user's goal is moderate, not blocking: missions do fly correctly and are never run concurrently. The hazard is an agentic harness that re-tasks a vehicle mid-mission believing it superseded the old command, when in fact both execute in sequence.

### [tasking-persistence-auth] Make a queued task_id pollable before it starts executing
- plan: `PLAN §4 (task_handle contract, T4a); §4.3 mission_status` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: `VehicleQueue.get` (tasking.py:209-215) searches only `self._current` and `self._history` — it never looks in `self._queue`. `uav_task_status` (server.py:538-544) therefore returns `{"error": "unknown task_id"}` for any task that is still pending. Verified at runtime: `q.get(t2.id)` returned `None` for a task whose handle had just been returned to the caller.
- required: A task_id handed back by a tool must be resolvable for the whole task lifetime. Today the server hands the harness an identifier and then denies knowing it — the harness's monitor loop sees 'unknown task_id' and cannot tell that from an invalid id or a lost task. This compounds gap 2: the second command both silently queues and becomes unpollable.
- files: godseye/mcp/godseye_uav/tasking.py
- scope correction: Gap stands as written, with a widened blast radius. Fix is small — add `self._by_id: dict[str, Task] = {}` to `VehicleQueue.__init__` (tasking.py:85-97), populate it in `submit()` alongside the `put_nowait` at tasking.py:120-123, and have `get()` (tasking.py:209) consult it (or scan `self._queue._queue`, the underlying deque). Note that the correct scope covers the first submitted task too, not only a second queued one: any poll issued before the background worker dequeues the task returns "unknown task_id". A regression test must assert `q.get(handle["task_id"])` is non-None IMMEDIATELY after submit with no sleep — the existing loops at tests/test_server.py:58-62 and 76-79 sleep first and tolerate None, so they would not catch a reintroduction.

### [tasking-persistence-auth] Emit notifications/progress with progressToken and actually compute progress_pct
- plan: `PLAN §4 intro (T4a): 'Long ops (>2 s) return task_id immediately + notifications/progress (progressToken, T4a). Progress derived server-side from telemetry'` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: Half of T4a is present: `_submit` (server.py:309-312) returns a handle without blocking. The progress half does not exist. `Task.progress_pct` is declared at tasking.py:48 and serialized at tasking.py:58 and is never assigned anywhere — grep for `progress` across mcp/ returns only those two lines. No tool signature in server.py:317-544 takes a `Context` parameter, and there is no `report_progress` / `progressToken` / `send_progress_notification` call anywhere in the repo. Runtime check on an executing task printed `progress_pct: 0.0`. The installed SDK supports it (.venv/.../mcp/server/mcpserver/context.py:113 `async def report_progress`).
- required: Per PLAN §4 and §5 ('monitor progress notifications') and §3.1 ('task progress %' in the GEV HUD), the server must derive progress from telemetry (distance-to-target vs plan — the data is already in `_wait_arrival`, server.py:149-161) and push it over the MCP progress channel. Today the harness has exactly two observable states, 'executing' and 'done', for a mission that may legitimately run for an hour; it cannot tell a stalled drone from a flying one. The GEV panel already reflects this — src/ui/uavMissionPanel.js:368 can only print 'queued · task <id>'.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/tasking.py

### [tasking-persistence-auth] Create a mission registry so missions have an id, status and cancel
- plan: `PLAN §4.3 mission_status / mission_cancel; §4.8 uav://mission/{id}, uav://reports/{id}` | state: **missing** | verdict: OVERSTATED | effort: M | blocks_goal: True
- exists: `uav_mission` (server.py:378-399) calls `plan_mission(...)`, uses `plan.to_route()` for the gate and the submit, copies three fields onto the task handle, and then discards the MissionPlan — it is a local variable that goes out of scope. No mission id is ever minted anywhere in the repo; `store.log_mission` (store.py:70-71) has zero production callers. The nearest thing is `uav_task_status` (server.py:538-544), which returns only the task handle (task_id, tool, vehicle, state, progress_pct=0.0, submitted_at, error).
- required: PLAN §4.3 specifies `mission_status`/`mission_cancel` keyed by mission_handle returning `state, progress_pct, waypoint, eta_s, fuel, bingo_fuel_pct`. None of waypoint index, eta_s, fuel or bingo_fuel_pct is available from any status call, and there is no mission-level identity at all — so §4.8's `uav://mission/{id}` and `uav://reports/{id}` have no key to hang off, and the GEV `/snapshot.missions[]` field required by PLAN §3.1 has no source. (This is a genuine absence, not a rename: no tool anywhere returns mission-scoped state.)
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/store.py
- scope correction: Not "create a mission registry" from zero — populate and expose what exists. Actual remaining work:

(1) Retain the plan: add `self.missions: dict[str, MissionPlan] = {}` next to `self.tracks` (server.py:212-213) and store `self.missions[handle["task_id"]] = plan` at server.py:392 instead of discarding the local `plan` (server.py:383). That single dict is the registry; `task_id` is the mission id — no new id scheme needed.

(2) Make progress real: `tasking.py:48 progress_pct` is never written. The fly_route executor already enumerates waypoints at server.py:261-263 — set `task.progress_pct = 100.0*(i+1)/n` and record the leg index there, then add `waypoint` to `Task.handle()` (tasking.py:52-61). This is the only field a harness truly cannot get today.

(3) Fill out the status return: carry `gate["est_time_s"]` (safety.py:271, already computed at server.py:387) onto the handle as `eta_s`, and have `uav_task_status` (server.py:538-544) also emit `fuel`/`bingo_fuel_pct` from `self.fuel_for(vehicle)` (the same two lines as server.py:362-363). Optionally alias it as `mission_status`/`mission_cancel` (cancel delegating to the existing `uav_abort` path, server.py:351-356) so a harness reading PLAN §4.3 finds the documented names.

(4) Fix the lookup hole found while there: `VehicleQueue.get()` searches only `_current` and `_history` (tasking.py:209-215), never the pending `_queue`, so a task_id returned by `uav_mission` yields `{"error": "unknown task_id"}` (server.py:543) during the QUEUED window before it starts.

(5) Wire the already-written, already-tested persistence: call `store.log_mission` (store.py:70) on submit/complete alongside the existing `log_task` calls (server.py:237, 287, 311).

Separately, `uav://mission/{id}` / `uav://reports/{id}` (PLAN §4.8) and `/snapshot.missions[]` (PLAN §3.1) are genuinely absent, but those are the MCP-resources and GEV-serialization gaps respectively — the registry above is their prerequisite, not the same item. Scope them there rather than double-counting them here.

### [tasking-persistence-auth] Expose the safety envelope (geofence, ceiling, speed, min AGL, home) to the harness
- plan: `PLAN §4.8 uav://safety/geofence; §4.5 'skill ROE may only be stricter'` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: The data exists and is enforced: `SafetyEnvelope` (safety.py:136-168) holds geofence polygon, ceiling_m_agl, max_speed_mps, min_agl_m, home, and is populated from the theater preset at launch.py:75. But none of the 19 registered tools (server.py:317-544; grep 'geofence' in server.py hits only three description strings at lines 329, 338, 375) returns it, and no resource is registered. The only feedback path is `_gate` (server.py:298-307), which returns `envelope_violations` strings like 'wp3:geofence' — and only on rejection. The bridge's `/mission-overlay` (bridge.py:275-277) could carry it but `state.mission_overlay` is initialised to an empty FeatureCollection at bridge.py:172 and never written.
- required: PLAN §4.5 requires the skill's ROE to be a strict subset of the server envelope — which is impossible if the skill cannot read the envelope. A harness planning a grid search must guess the AO polygon, then discover it was wrong via an opaque rejection string. This is the single highest-value missing resource for the user's goal.
- files: godseye/mcp/godseye_uav/server.py

### [intel-tracks-threat] No confidence level (confirmed/probable/possible) anywhere in the intel chain
- plan: `PLAN.md:173 §4.6(c) / M13(c); REVIEW_MILITARY.md:40 M11 fix` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: nothing. `assess_track` returns only track_id, category, threat_score, threat_level, in_envelope, envelope_m, intent, rationale, recommendation (godseye/mcp/godseye_uav/threat.py:81-92). The `Track` dataclass has no confidence field (godseye/mcp/godseye_uav/targets.py:38-53) and `salute_report` emits none (targets.py:129-145). A repo-wide grep for 'confirmed|probable|possible|confidence' hits zero lines under mcp/.
- required: PLAN §4.6(c): 'confidence = confirmed/probable/possible with cited evidence per element'. M11's fix also requires track state to carry classification confidence. A harness has no way to distinguish a single-frame contact from a 40-sighting multi-aspect ID; PLAN.md:98 also expects the GEV contact roster to show 'persistent track IDs, class, confidence'.
- files: godseye/mcp/godseye_uav/threat.py, godseye/mcp/godseye_uav/targets.py
- scope correction: Gap stands as MISSING. Only correction: the derivation input already exists, so the fix is smaller than "build from nothing" — Track.sightings (targets.py:49,71) plus first_seen/last_seen give observation count and track age for free. Remaining work: (1) add a confidence field to Track (targets.py:38-53) plus a deterministic derive function mapping sightings/track-age/category-known-vs-unknown to confirmed|probable|possible; (2) emit it in salute_report (targets.py:131-145) and in intrep_summary (targets.py:162-169); (3) add confidence + per-element evidence refs to assess_track's return (threat.py:81-92) so PLAN §4.6(c) is satisfied alongside the existing (a)(b)(d); (4) require confidence >= probable for custody transfer in plan_handoff (threat.py:141-159) per M10's "positive-ID confirmation"; (5) surface it in the GEV contact roster — blocked behind the separately-missing /snapshot.contacts[] feed (bridge.py has no contacts field at all). Also drop the prior finding's false claim that sightings is invisible to a harness.

### [intel-tracks-threat] Pattern-of-life store (M12) does not exist
- plan: `PLAN.md:23, 162, 179-180, 201 / REVIEW_MILITARY.md:42-43 M12` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: Only a comment and a breadcrumb list: godseye/mcp/godseye_uav/targets.py:52-53 ('# pattern-of-life: recent (t, lat, lon) breadcrumbs (M12)', `history: list`), appended and truncated to 200 points in memory at targets.py:72-74. godseye/mcp/godseye_uav/store.py:62-65 opens exactly four journals (tasks, missions, audit, fuel) — no pattern-of-life journal, no POI keying, no load API. Repo-wide grep for 'pattern_of_life|pattern-of-life|deviation|baseline' under mcp/ hits only that one comment.
- required: PLAN §4.4/§4.8 + M12: a persistent per-POI pattern-of-life store (activity by time-of-day, routes, dwell times, deviations), seeded from scenario runs, surviving restart, exposed at `uav://pattern-of-life/{poi}`, and feeding the M13 intent stage. None of activity-by-hour, dwell, route, deviation, persistence or the resource exists. Track.history is per-track (not per-POI), unbounded-truncated, and read by nobody.
- files: godseye/mcp/godseye_uav/targets.py, godseye/mcp/godseye_uav/store.py, godseye/mcp/godseye_uav/threat.py, godseye/mcp/godseye_uav/server.py

### [intel-tracks-threat] Track store is process-memory only — IDs do not survive restart and are reused across runs
- plan: `PLAN.md:180 §4.8 / REVIEW_MILITARY.md:40 M11` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: godseye/mcp/godseye_uav/server.py:212-213 constructs `TrackManager()` empty on every server start with no store argument and no reload. godseye/mcp/godseye_uav/targets.py:80-83 holds `self._tracks: dict` and `self._seq = 0` purely in memory; IDs are minted `f"TRK-{self._seq:03d}"` (targets.py:111). store.py:62-65 has no tracks journal; the only trace written is a flat audit line, `store.log_audit("target_track", ...)` (server.py:409-411), which nothing ever reads back — store.py exposes only `mission_events` and `audit_tail` (store.py:79-84).
- required: PLAN §4.8: 'Track store + pattern-of-life persist across sim_reset (M12)' and 'restart = replay'. Today tracks do persist across missions inside one process (that half works), but a restart loses every track and restarts numbering at TRK-001, so TRK-001 in today's INTREP is a different object from TRK-001 in yesterday's log — handoff continuity (M10), cross-mission SALUTE coherence (M8) and any longitudinal pattern-of-life all break. Needs a tracks journal plus load-on-start and a monotonic sequence restored from it.
- files: godseye/mcp/godseye_uav/store.py, godseye/mcp/godseye_uav/targets.py, godseye/mcp/godseye_uav/server.py, godseye/tests/test_store.py

### [intel-tracks-threat] INTREP is a track count roll-up, not the plan's template (no mission summary, coverage %, sensor conditions, LOAL, gaps)
- plan: `PLAN.md:176 §4.7 / REVIEW_MILITARY.md:30 M8` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: godseye/mcp/godseye_uav/targets.py:156-169 `intrep_summary(tracks, since)` returns exactly {format, as_of, total_tracks, by_category, new_contacts, tracks[]}. Exposed as tool `uav_target_report(since)` (godseye/mcp/godseye_uav/server.py:428-432).
- required: PLAN §4.7 names six required INTREP sections: mission summary, coverage %, tracks with IDs, sensor conditions, LOAL events, gaps. Only 'tracks with IDs' is present. Coverage % has no producer anywhere (missions.py computes lane spacing at missions.py:39-43 but never flown-coverage). Sensor conditions (time of day, weather, wind, GPS quality) are settable via sim_set_environment (server.py:506) but never captured into a report. LOAL cannot exist: grep for 'lost_link|LOAL|link_state|degraded' across mcp/godseye_uav/*.py returns zero matches, so M9 has no producer. 'Gaps' has no field.
- files: godseye/mcp/godseye_uav/targets.py, godseye/mcp/godseye_uav/server.py

### [gev-ui] Feed mission overlays from /mission-overlay into a dedicated CustomDataSource
- plan: `PLAN.md §3 'Mission overlays' (L81) + §3.1 acceptance 3 & 4 (L104-105); REVIEW_TECHNICAL.md T6` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: Bridge endpoint is a stub: godseye/mcp/godseye_uav/bridge.py:172 initializes `self.mission_overlay = {"type":"FeatureCollection","features":[]}` and bridge.py:275-277 returns it verbatim; grep over the whole godseye tree finds NO writer to `mission_overlay` outside that constructor, so the endpoint is permanently empty. On the browser side, grep for 'mission-overlay' across gods-eye-view/src returns zero hits — nothing fetches it. The one UAV CustomDataSource in the UI (gods-eye-view/src/ui/uavMissionPanel.js:396 'uav-tactical') is fed from /snapshot and only ever holds a target pin, a projected path-ahead line and a ground track (uavMissionPanel.js:402-535). No route polyline, no grid/lawnmower pattern, no waypoints, no geofence, no threat rings, no OB markers, no handoff route. The AO polygon is defined per theater (uavMissionPanel.js:15-20) but never drawn.
- required: MCP mission primitives must publish GeoJSON (route polyline, waypoints, derived grid lanes, geofence polygon, threat rings, target markers carrying track IDs, handoff route for the second vehicle) into bridge state.mission_overlay on every mission-state change, and a new GEV module must poll/refresh /mission-overlay into its own Cesium.CustomDataSource separate from the entity snapshot, per T6. This single gap is what makes §3.1 acceptance items 3 and 4 unachievable.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py, gods-eye-view/src/layers/uav/missionOverlay.js, gods-eye-view/src/layers/uav/index.js, gods-eye-view/src/app/layers/uav.js
- scope correction: Scope note only (gap itself is real and blocking): drop "target markers carrying track IDs" from the required work — those already exist and render via src/layers/uav/index.js:97-129 fed from bridge /tracks. Remaining work is the other six overlay feature types (route polyline, waypoints, derived grid lanes, geofence polygon, threat rings, handoff route for the second vehicle) plus both halves of the pipeline that are 100% absent: (1) a writer that serializes mission geometry into BridgeState.mission_overlay on every mission-state change — which additionally requires a new MCP-to-bridge publish path, since launch.py:87-90 gives the bridge its own state and bridge.py:292-318 is one-way, and (2) a new GEV module that polls /mission-overlay into its own Cesium.CustomDataSource separate from the 'uav' entity source, per T6.

### [gev-ui] Install a click-to-track handler and register the uav pick owner
- plan: `PLAN.md §3 (L75 tracking.js/lifecycle.js `registerPickOwner('uav', …)`, L79 'Free wins once tracked: click-to-track') + §3.1 acceptance 2 (L103)` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: gods-eye-view/src/layers/uav/index.js has no Cesium.ScreenSpaceEventHandler and never imports src/data/pickRegistry.js; `track(reference)` (index.js:189-197) is only callable programmatically. The reference layer does both: gods-eye-view/src/layers/military/lifecycle.js:138 `registerPickOwner('military', …)` and src/layers/military/tracking.js:1041 `_installClickHandler`. The only path to a tracked drone today is the mission panel's Cockpit button, hardcoded to vehicle 'Drone1' (gods-eye-view/src/ui/uavMissionPanel.js:558 `onEnterCockpit?.('Drone1')` → src/app/controls.js:67-89).
- required: Clicking a drone on the globe must track it and open the cockpit for THAT vehicle (multi-vehicle handoff in acceptance item 4 needs per-entity selection, not a fixed 'Drone1'). A pick-ownership predicate for ids matching `uav:*` / `uav-target:*` must also be registered — without it, src/layers/military/tracking.js:1124-1137 classifies a UAV pick as empty space and CLEARS the operator's existing military track while tracking no drone, and the same holds for src/layers/flights/lifecycle.js:159.
- files: gods-eye-view/src/layers/uav/tracking.js, gods-eye-view/src/layers/uav/lifecycle.js, gods-eye-view/src/layers/uav/index.js, gods-eye-view/src/ui/uavMissionPanel.js

### [gev-ui] Render the sensor camera PIP with simGetDetections bounding boxes
- plan: `PLAN.md §3.1 feed table L97 ('Sensor video PIP (Scene/IR) … /camera loop B') + acceptance 2 (L103 'detection boxes from simGetDetections rendered on PIP')` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: The bridge serves frames at godseye/mcp/godseye_uav/bridge.py:384-395 (`/camera/{veh}`) and a subscribe stub at bridge.py:279-281, but nothing in gods-eye-view/src requests them — grep for the bridge camera path across src/ returns zero hits, and there is no PIP element anywhere (grep for 'pip'/'picture-in-picture' in src/ui and src/app returns nothing). The `_detection*` identifiers in src/ui/applicationShell.js:179-189 belong to GEV's pre-existing synthetic world-overlay detection styling, not to AirSim detections. There is no /detections endpoint on the bridge at all, so DetectionInfo Box2D never reaches the browser.
- required: A PIP surface in the cockpit that pulls /camera/{vehicle} at 2-5 Hz only while a drone is tracked (subscriber-gated per the plan's budget), plus a new bridge endpoint exposing uav_get_detections Box2D + track_id so boxes can be drawn in the PIP's image coordinate frame.
- files: godseye/mcp/godseye_uav/bridge.py, gods-eye-view/src/ui/uavSensorPip.js, gods-eye-view/src/ui/cockpitCoordinator.js, gods-eye-view/src/ui/applicationShell.js
- scope correction: Gap stands as "missing" and is in fact slightly LARGER than claimed. Remaining work: (1) a GEV PIP surface (cockpit/mission-panel image element) that polls /camera/{vehicle}?cam=&type= at 2-5 Hz only while a drone is tracked, and calls nothing when untracked so loop B's subscriber gate releases — note the bridge never unsubscribes on client disappearance (subscribe_camera at bridge.py:387 has no TTL; unsubscribe_camera at bridge.py:212 is never called from any route), so a cache/subscription expiry is also needed; (2) an overlay drawing Box2D min/max in the PIP's image coordinate frame; (3) contrary to the prior auditor's framing, a bridge endpoint alone is NOT enough — uav_get_detections does not exist as an MCP tool (server.py:170 is only a backend method) and Track/salute_report (targets.py:40-53, 129-145) drop box2D, so a detections MCP tool plus box2D+track_id serialization must be added before any bridge route can expose it. Optional: /camera returns PNG (bridge.py:394) where PLAN.md:97 specifies JPEG — cosmetic, not blocking.

### [gev-ui] Surface safety alarms as toasts + HUD banner over an SSE channel
- plan: `PLAN.md §3.1 feed table L99 (SSE /events) + acceptance 5 (L106) + DoD L113 ('≥3 alarm types demonstrated')` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: No SSE producer: godseye/mcp/godseye_uav/bridge.py registers only /health, /snapshot, /snapshot/{name}, /mission-overlay, /camera/subscribe, /control/*, /tracks, /camera/{veh} (bridge.py:253-395) — no /events. No consumer: grep for 'EventSource' across gods-eye-view/src matches only unrelated Bhote-Koshi event fixtures. No alarm surface either: grep for 'bingo' across the whole GEV src tree hits exactly one line, gods-eye-view/src/ui/uavMissionPanel.js:549, a plain-text readout.
- required: FastAPI SSE /events emitting BINGO warnings, geofence proximity, lost-link plan activation and detection events; a browser EventSource client that raises a toast (the shell already has `_showToast`, gods-eye-view/src/ui/shellFeedback.js:181) and paints a persistent HUD banner in cockpit mode.
- files: godseye/mcp/godseye_uav/bridge.py, gods-eye-view/src/sources/live/uavEvents.js, gods-eye-view/src/ui/shellFeedback.js, gods-eye-view/src/ui/cockpitInstruments.js
- scope correction: Gap stands as written, and is in fact broader: beyond the missing SSE /events endpoint and missing browser EventSource/toast/banner client, the alarm PRODUCERS themselves do not exist. safety.py offers only pure predicates (check_point:145, geofence_margin_m:164, bingo_fuel_pct:242) with no per-tick monitor; server.py:306 logs only a plan-time preflight_reject; lost-link is entirely unimplemented. Full remaining work: (1) an in-flight safety monitor that ticks geofence margin / fuel-vs-BINGO / link state and emits typed events; (2) a server->bridge event path (tail audit.jsonl or an MCP notification), since bridge.py only proxies tool calls; (3) FastAPI SSE /events on the bridge; (4) a GEV EventSource client wiring events into the existing shellFeedback.js:181 _showToast and shellFacade.js:1295 _showGlobalStatusNotice primitives, plus a persistent cockpit-mode HUD banner.

### [gev-ui] Put mission phase, progress % and fuel-vs-BINGO in the HUD, fed by /snapshot.missions[]
- plan: `PLAN.md §3.1 feed table L96 ('/snapshot.missions[] (new field)'), implementation note L109, acceptance 2 (fuel %, ETA-to-BINGO) and 3 (progress % + phase label in HUD panel)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: The bridge /snapshot payload is only `{sim_state, observedAtMs, count, vehicles}` (godseye/mcp/godseye_uav/bridge.py:258-266) — no `missions[]`. Task progress exists server-side but is never published to the UI (godseye/mcp/godseye_uav/tasking.py:48 `progress_pct`, tasking.py:58). The cockpit HUD writes callsign, speed, altitude and heading only (gods-eye-view/src/ui/cockpitInstruments.js:25-105) — grep for 'fuel' across src/ui/cockpit*.js returns nothing. The only fuel/BINGO text in the product is a 1 Hz left-rail readout at gods-eye-view/src/ui/uavMissionPanel.js:546-550, which is outside the HUD and is itself broken (see the /control/status unwrap gap). ETA-to-BINGO does not exist anywhere in either repo.
- required: /snapshot must gain `missions[]` (phase, active tool, progress %, fuel % vs bingo_fuel_pct, safety/geofence state); the UAV layer must carry it through getTrackedInfo(); and the cockpit HUD must render fuel %, ETA-to-BINGO and a phase/progress line for the tracked drone. `getTrackedInfo()` already forwards fuelPct (gods-eye-view/src/layers/uav/index.js:232) — no HUD consumer reads it.
- files: godseye/mcp/godseye_uav/bridge.py, gods-eye-view/src/sources/live/uav.js, gods-eye-view/src/layers/uav/index.js, gods-eye-view/src/ui/cockpitInstruments.js

### [skill-harness-demo] Author the godseye-uav SKILL.md (does not exist at all)
- plan: `PLAN.md:184-188 (§5 Harness Skill), PLAN.md:4, PLAN.md:14` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: nothing — `find ObraMaestra -name SKILL.md` returns only godseye/.agents/skills/beads/SKILL.md, gods-eye-view/.agents/skills/community-pr/SKILL.md and a vendored fastapi one. `grep -rn godseye-uav` over the repo hits only PLAN.md:4/14/184, PLAN_DRAFT.md:12/194 and the MCPServer *server name* string at mcp/godseye_uav/server.py:215. No .claude/skills/ dir exists (godseye/.claude/ contains only settings.json).
- required: Two co-located copies (or one + symlink) so every harness finds it: godseye/.claude/skills/godseye-uav/SKILL.md (Claude Code project-skill path) AND godseye/.agents/skills/godseye-uav/SKILL.md (portable path the repo already uses for beads, referenced from AGENTS.md; add agents/openai.yaml alongside it mirroring .agents/skills/beads/agents/openai.yaml). Frontmatter must follow .agents/skills/beads/SKILL.md:1-4 (`name:`, `description:` naming the triggers: fly a drone/UAV mission, ISR recon, area/route recon, target track, threat assessment, SALUTE/INTREP). Body must contain, keyed to the ACTUAL tool names (server.py:317-539), not PLAN §4 names: (1) CONNECT — streamable-HTTP MCP at http://127.0.0.1:8791/mcp, Authorization: Bearer $TOKEN (default dev-token), started by ./start.sh (start.sh:57-63,78); (2) DOCTRINE PATTERNS mapped to calls — area recon = uav_mission(kind='grid_search', params={polygon, alt_m}); route recon = uav_mission(kind='recon_route', params={waypoints}); point surveillance = uav_mission(kind='orbit_poi', params={lat,lon,radius_m,alt_m,sun_azimuth_deg}); target track = uav_mission(kind='track_target') then uav_handoff_target(track_id, from_vehicle, to_vehicle); SAR expanding square and BDA re-look have NO backing kind (see separate gap) and must be flagged unsupported rather than hallucinated; (3) altitude<->resolution: state the actual camera model (missions.py:22-24 HFOV 60 deg, VFOV 45 deg, 20% overlap) and that lane spacing is server-derived (missions.py:39-43) and lanes are capped at 12 (missions.py:28) so a large AO is silently thinned — the skill must re-check sweep_spacing_m returned at server.py:397-398; (4) standoff from threat rings: read uav_assess_threat (server.py:454) then never plan inside the reported envelope; (5) BINGO BEFORE TASKING (M4): call uav_get_telemetry (server.py:358) for fuel_pct/bingo_fuel_pct before every task and treat a {'rejected': True, 'gate': ...} return (server.py:333,342,389) as a hard stop, never a retry-with-smaller-numbers loop without re-reading the gate; (6) WORKFLOW task->plan->dry-run->execute->monitor->report, with the dry-run step written against whatever dry-run affordance is added (see separate gap) and the monitor step polling uav_task_status (server.py:539); (7) REPORTING: uav_scan_targets (server.py:404) -> uav_identify_target (server.py:419, SALUTE) -> uav_target_report (server.py:430, INTREP) — fill fields, never free-form; (8) ABORT CRITERIA: fuel at/below bingo_fuel_pct, geofence violation in gate['envelope_violations'] (server.py:302-304), task state 'failed', watchdog timeout — action is uav_abort (server.py:352) then uav_return_to_home; (9) ROE: the skill may only be STRICTER than the server envelope, never overrides it, and no kinetic reasoning — the catalog is ISR-only (server.py:8) and there is no strike tool to request.
- files: godseye/.claude/skills/godseye-uav/SKILL.md, godseye/.agents/skills/godseye-uav/SKILL.md, godseye/.agents/skills/godseye-uav/agents/openai.yaml, godseye/AGENTS.md, godseye/CLAUDE.md
- scope correction: Gap is real and the scope is essentially accurate; two minor trims. (1) Only ONE real file is needed plus a symlink, not two independently authored copies: write godseye/.agents/skills/godseye-uav/SKILL.md (the portable path AGENTS.md:108 already advertises for beads) and symlink godseye/.claude/skills/godseye-uav -> ../../.agents/skills/godseye-uav so Claude Code finds it too. The agents/openai.yaml sidecar mirroring .agents/skills/beads/agents/openai.yaml is optional polish, not a blocker. (2) Authoring effort is modestly reduced by existing material the author should transcribe rather than invent: godseye/scripts/demo_mission.py:80-134 is an already-verified end-to-end call sequence (sim_spawn_target -> uav_takeoff -> uav_mission grid_search -> uav_scan_targets -> uav_assess_threat -> uav_identify_target -> uav_return_to_home -> uav_land), and PLAN.md:184-188 supplies the doctrine bullets. Note the demo is NOT a substitute and does not model the harness path: it bypasses MCP entirely by reaching into the in-process registry at demo_mission.py:75-78 (tools = srv.mcp._tool_manager._tools; await tools[_tool].fn(**kw)), so the skill must still document the streamable-HTTP connect at http://127.0.0.1:8791/mcp with Authorization: Bearer $TOKEN (default dev-token, start.sh:24,57-63,78). Everything else in the claimed required content stands, including flagging SAR expanding square and BDA re-look as unsupported (plan_mission at missions.py:143-170 implements only recon_route, grid_search, orbit-style and assess kinds).

### [skill-harness-demo] Ship an MCP connection manifest + operator README so a harness can reach the server
- plan: `PLAN.md:17, PLAN.md:49 (Streamable HTTP + Bearer, loopback), PLAN.md:198 (Phase 2 exit: harness flies GPS route via MCP)` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: nothing declarative. `find . -maxdepth 3 -name .mcp.json` over ObraMaestra returns nothing; godseye has no README.md (only AGENTS.md, CLAUDE.md, PLAN.md, PLAN_DRAFT.md, REVIEW_*.md). The URL/token exist only as runtime echoes at start.sh:78 and launch.py:94, and as defaults at launch.py:52-54 and server.py:546-547.
- required: (a) godseye/.mcp.json declaring server `godseye-uav`, type streamable-http (or `http`), url http://127.0.0.1:8791/mcp, headers {Authorization: Bearer ${GODSEYE_TOKEN:-dev-token}} — this is what makes `claude mcp` / Claude Code pick the server up without hand-configuration; (b) a README section stating transport=Streamable HTTP, path=/mcp, auth=static Bearer (server.py:28-37 StaticBearerVerifier), stateless_http=True (server.py:547), loopback-only bind, and the launch command. (c) Fix the hardcoded auth metadata: server.py:216-221 pins issuer_url and resource_server_url to 'http://127.0.0.1:8791' regardless of the --mcp-port passed at launch.py:52, so launching on any other port advertises wrong OAuth resource metadata to a discovery-capable client. Thread the actual host:port in.
- files: godseye/.mcp.json, godseye/README.md, godseye/mcp/godseye_uav/server.py

### [skill-harness-demo] Create scripts/demo_laptop.sh (the one-command demo does not exist)
- plan: `PLAN.md:222 (§8.1 one-command demo), PLAN.md:113 (Phase 3 definition of done)` | state: **missing** | verdict: OVERSTATED | effort: M | blocks_goal: True
- exists: nothing named demo_laptop — `find . -name 'demo_laptop*'` over the whole ObraMaestra tree returns nothing. godseye/scripts/ contains only ci.sh and demo_mission.py. start.sh:57-73 boots sim+MCP+bridge+vite but stops there: it never opens a browser (no `open`/`xdg-open` anywhere in start.sh:1-86) and never runs a harness or a scripted mission.
- required: scripts/demo_laptop.sh that satisfies PLAN.md:222 end to end: start fake-or-real sim + MCP + bridge + GEV (it can delegate to start.sh), wait for http://127.0.0.1:8790/health (bridge.py:253) and the MCP port to answer, open the browser at http://localhost:4173, then run the scripted recon mission **through the MCP HTTP endpoint** so the operator watches it live in the command center. Must exit non-zero when a stage fails to come up, and must accept the same env overrides start.sh:12 documents.
- files: godseye/scripts/demo_laptop.sh, godseye/start.sh
- scope correction: The remaining work is roughly two focused edits, not "write the one-command demo from scratch":

1. NEW scripts/demo_laptop.sh (~30-40 lines): delegate to start.sh in the background honoring the same env overrides it already documents at start.sh:12; poll http://127.0.0.1:${BRIDGE_PORT:-8790}/health (bridge.py:253) and the MCP port until ready with a bounded timeout, `exit 1` on timeout; `open`/`xdg-open` http://localhost:${UI_PORT:-4173} (no such call exists anywhere today); then invoke the mission driver against the LIVE stack; propagate its exit code.

2. EDIT scripts/demo_mission.py to add a "attach to running stack" path: replace the in-process shim at demo_mission.py:77-78 with an MCP HTTP client pointed at http://127.0.0.1:${MCP_PORT}/mcp (bearer TOKEN), and skip the self-owned sim/server/store construction at demo_mission.py:59-60 and :73-74 when attaching. The mission body at demo_mission.py:80-136 needs no logic change — only the `call()` transport and the setup guard change. Default --sim-port 45100 (demo_mission.py:43) should also align with the stack's 41451 in attach mode.

Everything else the claim demands (spawn/recon/ID/threat/SALUTE/RTH sequencing, BINGO-safe grid sizing, ISR-only discipline) is already implemented and needs no new authoring.

### [skill-harness-demo] demo_mission.py bypasses the MCP transport and is invisible in the command center
- plan: `PLAN.md:222, PLAN.md:113 (operator watches the mission live in GEV), PLAN.md:47-49` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: godseye/scripts/demo_mission.py:75-78 reaches into private internals — `tools = srv.mcp._tool_manager._tools` then `await tools[_tool].fn(**kw)` — so no MCP protocol, no Streamable HTTP, no Bearer auth is exercised. demo_mission.py:45 defaults --sim-port 45100 and line 59-60 starts its OWN FakeAirSim, so it does not attach to the sim start.sh launched on 41451; it never constructs the bridge (no import of godseye_uav.bridge) and never touches GEV. Result: the demo proves nothing about the path a real harness uses and shows nothing on the globe.
- required: Rewrite as an MCP client: connect to http://127.0.0.1:8791/mcp with the Bearer token, call tools/list, then drive the same script via tools/call against the already-running launch.py stack (so the bridge /snapshot at bridge.py:258 and the GEV uav layer see it). Keep the in-process variant only as a test helper, not as the §8.1 demo. Also fix demo_mission.py:73 `Store(":memory:")` — store.py:59-60 does `Path(root).mkdir(parents=True)`, so this literally created godseye/:memory:/ on disk (audit.jsonl 6561 bytes, tasks.jsonl 5849 bytes).
- files: godseye/scripts/demo_mission.py
- scope correction: Verdict stands as CONFIRMED; this is a scope refinement, not a downgrade. The fix is materially cheaper than "write an MCP client" implies, because two pieces already exist:

1. bridge.py:292-318 `_mcp_call` is already a working MCP Streamable-HTTP client — POSTs {"jsonrpc":"2.0","method":"tools/call","params":{name,arguments}} with `Authorization: Bearer`, and `_unwrap` at bridge.py:320-330 parses the SSE `data:` line into the tool's JSON payload. Liftable nearly verbatim into the demo (or better, factored into a shared helper both use).
2. server.py:547 serves with `stateless_http=True`, so no `initialize` handshake or session-id management is needed — a bare POST works. `tools/list` is optional discovery and not required for the demo.

Actual remaining work: (a) replace the 2-line body of `call()` at demo_mission.py:77-78 with the HTTP POST helper; (b) delete the local stack construction at demo_mission.py:58-74 (FakeAirSim, MultirotorClient, UavBackend, SafetyEnvelope, GodseyeUavServer, Store) and instead take --mcp-url/--token pointing at the launch.py stack, so the bridge /snapshot (bridge.py:258) and the GEV uav layer (src/sources/live/uav.js:102) observe the same sim; (c) drop the Store(":memory:") line entirely (the server-side store now belongs to launch.py, which uses --db .godseye/store at launch.py:57) and delete the stray godseye/:memory:/ directory; (d) keep the in-process variant only as a test helper. The mission choreography at demo_mission.py:80-134 (spawn OB, takeoff, grid search, scan, assess, SALUTE, RTB/land) and the tool names it uses are unchanged and all exist as registered MCP tools in server.py.

### [skill-harness-demo] No dry-run: the skill's plan->dry-run->execute workflow cannot be performed
- plan: `PLAN.md:187 (workflow: task -> plan -> dry-run (fuel/geofence/sun) -> execute), PLAN.md:145 (grid returns derived footprint, waypoints, est_time_s, est_fuel_pct, bingo gate result *as a plan product*)` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: every gating tool gates and commits in the same call. server.py:378-399 `uav_mission` plans (line 383), gates (line 387), and on pass immediately `self._submit(...)` (line 390) — the vehicle flies. Same shape at server.py:331-336 (uav_goto_gps) and server.py:340-345 (uav_fly_route). No `dry_run` parameter exists on any tool (grep for dry_run/preview over mcp/ hits only docstrings at safety.py:10 and safety.py:209). The estimator is already written and reusable — safety.py:208 `estimate_route` and safety.py:249 `preflight_gate` are pure functions — but are exposed to a harness only as a side effect of committing the flight. The returned handle also omits est_time_s/est_distance_m: server.py:396 copies only gate['required_pct'].
- required: Add `dry_run: bool = False` to uav_mission / uav_fly_route / uav_goto_gps (or a separate `uav_plan_mission` tool) that returns the full plan_mission output plus the complete preflight_gate dict (safety.py:260-270: plan_fuel_pct, return_fuel_pct, reserve_pct, required_pct, available_pct, est_time_s, est_distance_m) and envelope_violations, WITHOUT calling _submit. Without this the skill's dry-run step degrades to 'submit and hope it gets rejected', which flies the drone whenever the plan is legal-but-wrong.
- files: godseye/mcp/godseye_uav/server.py, godseye/tests/test_missions.py

### [skill-harness-demo] Fuel integrator never runs in the server — fuel_pct is a constant 100.0, so BINGO doctrine monitors nothing
- plan: `PLAN.md:167 (T5 fuel integrator: each telemetry tick recompute phase and persist), PLAN.md:186 (BINGO check before tasking, M4), PLAN.md:96 (fuel % vs BINGO line in mission status)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: `FuelModel.tick()` is defined at safety.py:198 and is the ONLY place fuel_pct is ever decremented (safety.py:204), but it has zero call sites in production code: `grep -rn '\.tick(' mcp/ tests/ scripts/` returns only tests/test_safety.py:76,77,84. Nothing in server.py, bridge.py, tasking.py or launch.py ticks it. Consequently server.py:362 `tele['fuel_pct'] = round(fm.fuel_pct, 2)` always reports 100.0 and the preflight gate at safety.py:266 always compares against 100.0 available. Corroborating evidence on disk: store.log_fuel (store.py:76) also has zero production call sites (grep returns only tests/test_store.py:40), and fuel.jsonl in the completed demo run directory godseye/:memory:/fuel.jsonl is 0 bytes while audit.jsonl is 6561 bytes.
- required: Drive FuelModel.tick() from a telemetry loop (the bridge already runs one at bridge.py:175 telemetry_loop) or from server.py:360 uav_get_telemetry, feeding speed_mps/vz_mps/landed_state that telemetry already computes (server.py:108,115-117), and call store.log_fuel on each tick. Then implement the un-cancellable force-RTB at BINGO (PLAN.md:166). Until this lands, the single most load-bearing doctrine rule in the skill ('BINGO check before tasking') is checked against a number that never moves, and a long mission never runs out of fuel.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/safety.py

### [skill-harness-demo] progress_pct is never written and there are no progress notifications — the skill's monitor step is blind
- plan: `PLAN.md:187 (monitor progress notifications), PLAN.md:119 (long ops return task_id + notifications/progress, progress derived server-side from telemetry), PLAN.md:151 (mission_status returns progress_pct, waypoint, eta_s)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: `grep -rn progress mcp/godseye_uav/*.py` returns exactly two lines: tasking.py:48 `progress_pct: float = 0.0` (dataclass default) and tasking.py:58 which serializes it into the handle. There is no assignment anywhere, so every uav_task_status (server.py:539-544) and every task handle reports progress_pct 0.0 until the state flips to completed. No progressToken / notifications/progress machinery exists at all (same grep). There is also no current-waypoint index or eta_s in the handle (tasking.py:52-61).
- required: Derive progress server-side from telemetry as PLAN.md:119 specifies (distance-to-target vs plan length — the leg loop is at server.py:261-263 and the arrival wait at server.py:149-161 already has the distance), write it onto Task.progress_pct, and add waypoint index + eta_s to the handle. Emit notifications/progress with the MCP progressToken so a harness can monitor without polling. Otherwise the skill can only poll a boolean done/not-done and cannot make mid-mission decisions.
- files: godseye/mcp/godseye_uav/tasking.py, godseye/mcp/godseye_uav/server.py

### [realism-realdata] Implement moving targets / convoys (sim_move_target, mobile_route)
- plan: `PLAN.md:157 sim_move_target(target_id, waypoints, speed); PLAN.md:156 mobile_route?; PLAN.md:202 Phase 6 'moving convoys' + exit criterion` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: Only sim_spawn_target (server.py:440-448 -> UavBackend.spawn_object server.py:185-191). Spawned targets are stored as immutable GeoPoints in FakeAirSim._objects (fake_airsim.py:87, 488-495) and the physics loop (fake_airsim.py:133-180) iterates only _vehicles — objects never move. simSetObjectPose exists on the wire (fake_airsim.py:500-507) but nothing calls it: grep for move_target/convoy/mobile/simSetObjectPose across mcp/ and tests/ returns only that handler definition.
- required: A sim_move_target MCP tool plus object motion integration, so a convoy can traverse a route at a speed. Without it the entire moving-contact half of the system is untestable: Track.update's speed/heading estimator (targets.py:55-68), the 'moving NNN° at X m/s' activity line (targets.py:148-153), the pattern-of-life history (targets.py:72-74) and the movement-derived intent term in threat assessment (threat.py:1-8 docstring, assess_track) can never see a real moving target. The Phase-6 exit criterion 'tracking moving convoy' is unreachable.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py, godseye/tests/test_realism.py

### [realism-realdata] Add real terrain for AGL, LOS and geofence (M19 flag + GEV /api/terrain/heights)
- plan: `PLAN.md:202 Phase 6 'Cesium-for-Unreal real terrain behind feature flag (M19)'; PLAN.md:139 uav_los_check; REVIEW_MILITARY.md:69-70` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: Flat-earth everywhere. bridge.py:129 `agl=max(0.0, -ned.z)` is height above the home plane, not above ground. server.py:63-67 llh_agl_to_ned converts a waypoint 'AGL' by adding the typed home altitude. safety.py:145-154 check_point compares that pseudo-AGL against ceiling_m_agl=120 / min_agl_m=3 (safety.py:140-142). fake_airsim.py:530-531 simTestLineOfSightToPoint returns True unconditionally. No uav_los_check MCP tool exists (full catalog at server.py:314-544). grep for cesium/terrain/elevation across mcp/, scripts/ and start.sh returns only two comment strings (geo.py:10, fake_airsim.py:413).
- required: M19 wants real terrain behind a feature flag; the user's goal wants real elevation for LOS and AGL. GEV already ships exactly that service and godSeye ignores it: server/providers/terrain.js:159 mounts /api/terrain/heights (Re:Earth quantized mesh + EGM2008 geoid, disk-cached, serve-stale) and src/services/terrainHeights.js:40,99,158 is a batched 64-point client. Consequences today: at iran-isfahan (home alt typed 1550 m, launch.py:35) and indo-pak-loc (1600 m, launch.py:33) a mission's default 60 m 'AGL' waypoint (missions.py:155,163) is hundreds to thousands of metres below the real Zagros/Kashmir relief that GEV's photorealistic tiles render, so the drone flies underground on the globe; terrain masking cannot be modelled, so M5's 'verified vs uav_los_check' standoff derivation has nothing to verify against. (Compounding datum bug: launch.py:61 builds GeoPoint(*t['home']) whose contract is altHae (geo.py:24-29), then bridge.py:119-120 re-treats that same home-derived altitude as MSL and converts it to HAE again.)
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/safety.py, godseye/mcp/godseye_uav/geo.py

### [realism-realdata] Consume God's Eye View real-world data (installations as OB/targets, air traffic as deconfliction, vessels)
- plan: `PLAN.md:4 'flying … missions on real-world maps'; PLAN.md:173 §4.6 order-of-battle library; user goal 'uses ACTUAL real-world data'` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: Nothing. grep across godseye/mcp, tests, scripts and start.sh for opensky|adsb|aisstream|vessel|flights|military-installations|api/terrain returns exactly two incidental hits: the CORS origin list at bridge.py:235 and the substring keyword "vessel": "naval" at targets.py:24. Targets come only from hand-called sim_spawn_target (server.py:440-448); classification is a 13-entry substring keyword dict (targets.py:18-35); the order-of-battle library is a 9-entry hardcoded dict of weights and envelope radii (threat.py:20-30). The GEV UAV source reads only the bridge (src/sources/live/uav.js:102,109) and the UAV layer renders only those records plus /tracks (src/layers/uav/index.js:173-179).
- required: GEV already exposes real feeds that map one-to-one onto ISR inputs, registered in the app's own source catalog (src/app/constructCatalog.js:27-49): real OSM military installations via installations.getMappedSites/searchNearby (src/layers/installations/source.js:16, backed by the /api/military-installations proxy at server/providers/military-installations.js:80) as mission targets and order-of-battle seed; live air traffic via OpenSky/adsb.lol (src/sources/live/index.js:17-21 createOpenSkySource/createAdsbLolSource, normalizers in src/sources/live/aircraft.js) as deconfliction contacts for the safety envelope; live AIS vessels (createAisStreamSource, src/sources/live/vessels.js) for maritime theaters; real elevation via /api/terrain/heights. None of these are wired into the MCP server, the safety envelope, the track store or the threat pipeline, so 'realistic mission in an environment using real data' is currently a synthetic sandbox drawn on top of a real-looking globe.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/targets.py, godseye/mcp/godseye_uav/threat.py, godseye/mcp/godseye_uav/safety.py

### [realism-realdata] Reconcile the two THEATER tables — the default theater's demo mission is geofence-rejected
- plan: `PLAN.md:222 'One-command demo'; PLAN.md:166 geofence/AO; launch.py:30-31 self-declared sync requirement` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: Two independent hand-typed tables that the code itself admits must be kept in sync: launch.py:30-45 THEATERS (comment at launch.py:30-31: 'single source of truth would be ideal; keep these in sync with src/ui/uavMissionPanel.js THEATERS') and gods-eye-view/src/ui/uavMissionPanel.js:11-80 THEATERS. No test compares them (only src/ui/uavMissionPanel.test.mjs references THEATERS).
- required: They have already drifted, and the default path is broken. (a) iran-isfahan: launcher home (33.72, 51.72, 1550) with AO lat 33.68-33.76 / lon 51.66-51.78 (launch.py:35-36) vs panel home (32.6546, 51.668, 1570) with AO 32.63-32.68 / 51.63-51.71 (uavMissionPanel.js:22-31) — ~115 km apart and fully disjoint; the launcher's 'Isfahan' coordinates are in fact Natanz (panel iran-natanz = 33.7243, 51.7286, uavMissionPanel.js:42-44). (b) start.sh:19 defaults THEATER=iran-isfahan, so the out-of-the-box demo is: select 'Iran — Isfahan', seed POI 'Isfahan Center' (32.655, 51.670; uavMissionPanel.js:103), launch orbit_poi -> every waypoint fails point_in_polygon (safety.py:89-110) in check_route (safety.py:156-162, called from server.py:302) -> {'rejected': True} (server.py:388-389). (c) taiwan-strait is disjoint the same way: launcher AO 24.10-24.20/119.24-119.36 (launch.py:37-38) vs panel AO 24.44-24.56/119.44-119.56 (uavMissionPanel.js:61-68), and home alt 0.0 vs 200. (d) ukraine-donbas home alt differs 200 vs 250. (e) Panel-only theaters iran-fordow and iran-natanz have no launcher entry, so `--theater iran-fordow` is an argparse error (launch.py:50 choices=list(THEATERS)); launcher-only red-sea-hormuz (launch.py:41-42) has no panel entry. (f) Changing the theater in the panel only flies the camera (uavMissionPanel.js:561-565 onTheater?.(t.home)); it never reconfigures the server's geofence/home, which are frozen at launch (launch.py:60-61, 75). One table must be the source of truth, served to the panel by the bridge.
- files: godseye/mcp/godseye_uav/launch.py, godseye/mcp/godseye_uav/bridge.py, gods-eye-view/src/ui/uavMissionPanel.js


## MAJOR (71)
### [tools-flight-sensors] Add uav_set_gimbal — no sensor pointing exists anywhere in the system
- plan: `PLAN.md §4.1 (uav_set_gimbal: vehicle, camera, pitch_deg, yaw_deg, track_geo_point?)` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: nothing — `grep uav_set_gimbal mcp/` = 0 hits; `grep simSetCameraPose` = 0 hits repo-wide. fake_airsim.py:257-531 has no simSetCameraPose handler. The camera is permanently nadir/identity: fake_airsim.py:432 returns _q_identity() as camera_orientation for every frame.
- required: `uav_set_gimbal(vehicle, camera, pitch_deg, yaw_deg, track_geo_point?)` over simSetCameraPose, with a fake_airsim handler holding per-camera pose. This is also the prerequisite for the `camera_track` parameter of uav_orbit_poi (PLAN §4.1) and for any 'point the sensor at the track while orbiting' doctrine — with no gimbal the drone can only look wherever the airframe happens to look.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py

### [tools-flight-sensors] Add uav_los_check — standoff and terrain masking cannot be verified
- plan: `PLAN.md §4.2 (uav_los_check: vehicle, lat, lon, alt_m → LOS bool + first obstacle); §4.3 mission_track_target 'verified vs uav_los_check' (M5)` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: False
- exists: no MCP tool and no backend method — `grep uav_los_check mcp/` = 0 hits; UavBackend (server.py:40-191) never calls simTestLineOfSightToPoint. The fake has a stub only: fake_airsim.py:530-531 `def simTestLineOfSightToPoint(self, point, vehicle_name=""): return True` — unconditionally true, no obstacle model, no first-obstacle return.
- required: `uav_los_check(vehicle, lat, lon, alt_m)` wrapping simTestLineOfSightToPoint and returning both the boolean and the first obstacle. The fake must gain at least a coarse terrain/obstacle model, otherwise the tool returns True for every query and the M5 standoff verification it is supposed to back is vacuous UE-free.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py

### [tools-flight-sensors] uav_orbit_poi: no direction, laps, camera_track or sun_side rule — and unknown params are silently swallowed
- plan: `PLAN.md §4.1 (uav_orbit_poi: vehicle, lat, lon, radius_m, alt_m, direction(cw/ccw), laps, camera_track, sun_side rule — M3/M6); REVIEW_MILITARY.md:13 (M3 CRITICAL), :22 (M6)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: No tool named uav_orbit_poi (0 hits in mcp/); the executor has no branch for it either — server.py:239-272 handles only takeoff/land/rth/goto_gps/fly_route/hover/abort and server.py:273 raises `no executor for tool {tool}` for anything else (tests/test_tasking.py:82 submits the string "uav_orbit_poi" purely as a queue fixture, and would fail if ever executed). The ring itself exists via uav_mission(kind="orbit_poi") (server.py:372-399) → missions.py:161-169 → orbit_waypoints (missions.py:107-127). That function takes only (lat, lon, alt_m, radius_m, points=12, sun_azimuth_deg): direction is fixed CCW-by-construction (angle always increases, missions.py:119), laps is fixed at exactly one ring of 12 points (missions.py:118), camera_track does not exist (no gimbal control anywhere), and the 'sun-side' handling at missions.py:115-117 only rotates the *starting vertex* of a closed ring — the vehicle still flies every azimuth, so it changes nothing about glare. plan_mission takes **kw (missions.py:143) and reads named keys only, so a harness passing direction/laps/camera_track gets a silent no-op rather than an error.
- required: Either a real uav_orbit_poi tool or uav_mission orbit params honoring direction(cw/ccw), laps, camera_track, and an actual sun_side rule: M3/M6 require the standoff arc to be biased so the sensor looks away from the sun (and the standoff derived from the threat ring, not operator taste), not merely a rotated start index. plan_mission must reject unknown params instead of dropping them. Naming deviation worth noting separately: a harness written against PLAN §4.1 will call `uav_orbit_poi` and get tool-not-found.
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py

### [tools-flight-sensors] uav_get_telemetry omits attitude, wind and est_range_km
- plan: `PLAN.md §4.2 (uav_get_telemetry → lat/lon/altHae, attitude, velocity, fuel_pct, bingo_fuel_pct, est_range_km, landed_state, wind)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: server.py:358-365 returns backend.telemetry() plus fuel_pct/bingo_fuel_pct/queue. backend.telemetry (server.py:99-119) returns only vehicle, lat, lon, alt_hae, ned, speed_mps, vz_mps, landed_state, gps — no attitude, no wind, no est_range_km. The data is available and simply not forwarded: the quaternion is fetched at server.py:101-102 (state.kinematics_estimated) and the fake fills it at fake_airsim.py:362, and the bridge already converts it (bridge.py:51-65 _attitude_from_quat, used at bridge.py:115). Wind is settable via sim_set_environment (server.py:509-536) and stored at fake_airsim.py:212-215, but is never read back into telemetry.
- required: Add attitude (roll/pitch/yaw deg), the current wind vector, and est_range_km (derivable from fuel_pct + cruise burn rate via safety.py:39-47) to the uav_get_telemetry payload. `alt_hae` vs the plan's `altHae` is a snake_case deviation only, not a gap. Without wind in telemetry the skill's M15 'wind in fuel math' doctrine has no input, and without attitude the harness cannot reason about sensor pointing.
- files: godseye/mcp/godseye_uav/server.py
- scope correction: Gap is real; if anything the wind portion is larger than claimed. Actual remaining work: (1) attitude — small: call _attitude_from_quat (bridge.py:51-65) on state.kinematics_estimated.orientation already fetched at server.py:101 and add roll/pitch/yaw (plus heading) to the dict at server.py:108-118; (2) est_range_km — small: derive from fuel_pct and the cruise burn rate at safety.py:39-40 and add to server.py:358-365; (3) wind — NOT a one-line forward: sim_set_environment (server.py:509-536) currently keeps no wind state (only the audit line at server.py:535) and fake_airsim exposes no getter (only simSetWind at fake_airsim.py:518-521, write-only setter at fake_airsim.py:212-215), so a server-side wind field or a simGetWind readback must be added first, then surfaced in telemetry; (4) while doing so, pass headwind_mps through _gate (server.py:301) into preflight_gate so the existing penalty at safety.py:192-195 stops being dead code in the server path.

### [tools-flight-sensors] uav_get_detections: DetectionInfo[] is never returned — only a count
- plan: `PLAN.md §4.2 (uav_get_detections: vehicle, camera → DetectionInfo[] + persistent track_ids, M11); §2 DetectionInfo{name, geo_point, Box2D, Box3D, relative_pose}` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: No tool named uav_get_detections (0 hits in mcp/). The nearest is uav_scan_targets (server.py:401-414), which returns `"detections": len(dets)` (server.py:412) — an integer count — plus SALUTE-shaped tracks (targets.py:129-145). The raw DetectionInfo is truncated one layer earlier: UavBackend.get_detections (server.py:170-183) keeps only name and geo_point and discards box2D, box3D and relative_pose, even though the fake supplies all of them (fake_airsim.py:459-465). Detection-filter setup is also absent: `simSetDetectionFilterRadius` and `simAddDetectionFilterMeshName` have zero call sites in mcp/, so the radius is whatever the fake hard-codes (500 m, fake_airsim.py:448) and cannot be scoped per mission.
- required: Return the full DetectionInfo array (name, geo_point, box2D, box3D, relative_pose) alongside the persistent track_ids, and expose the detection filter (radius + mesh-name) so a mission can scope what the sensor reports. box2D in particular is what PLAN §3.1 needs to draw detection bounding boxes on the GEV PIP feed; today it is dropped at server.py:178-182 and can never reach the UI. Naming deviation (uav_scan_targets vs uav_get_detections) would also confuse a plan-written harness.
- files: godseye/mcp/godseye_uav/server.py
- scope correction: Gap stands as written. Scope is marginally larger than the prior auditor stated: (a) serializing box2D/box3D/relative_pose at server.py:178-182 requires unpacking AirSim objects (Box2D.min/max Vector2r, Box3D.min/max Vector3r, Pose.position/orientation), not a passthrough; (b) exposing the detection filter needs `simSetDetectionFilterRadius`/`simAddDetectionFilterMeshName`/`simClearDetectionMeshNames` added to fake_airsim.py too (it currently has none, and hard-codes 500 m at fake_airsim.py:448); (c) the plan-named `uav_get_detections` tool must be registered in server.py (aliasing or renaming `uav_scan_targets`, which should keep returning tracks) so a PLAN-written harness does not hit an unknown-tool error.

### [tools-flight-sensors] uav_fly_route / uav_goto_gps return the plan's required fuel under the name bingo_fuel_pct
- plan: `PLAN.md §4.1 (uav_fly_route returns task_handle + bingo_fuel_pct); §4.5 BINGO definition (M4)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: server.py:344 (and identically server.py:335, server.py:396) sets `handle["bingo_fuel_pct"] = gate["required_pct"]`. But `required_pct` is defined at safety.py:262 as plan_fuel + return_fuel + RESERVE_PCT — the total fuel the plan consumes, not the BINGO line. The actual BINGO line is a different quantity computed by safety.py:242-247 bingo_fuel_pct() = return leg + reserve, and that is what uav_get_telemetry publishes under the same key (server.py:363).
- required: One meaning per field name. Return the true BINGO line as `bingo_fuel_pct` on the flight tools and surface the plan cost under its own key (e.g. `required_fuel_pct`), or the harness comparing the route's `bingo_fuel_pct` against telemetry's `bingo_fuel_pct` is comparing two unrelated numbers and will mis-time its RTB decision.
- files: godseye/mcp/godseye_uav/server.py
- scope correction: None - scope is accurate as filed. Remaining work is exactly: (1) set `handle["bingo_fuel_pct"] = fm.bingo_fuel_pct(...)` (the real line) at server.py:335, server.py:344, server.py:396; (2) surface the gate budget under its own key, e.g. `handle["required_fuel_pct"] = gate["required_pct"]` (ideally the whole gate breakdown on success, not only on rejection at server.py:333/342/389); (3) tighten tests/test_server.py:93 and tests/test_server.py:126-133, which currently accept either key name and never compare a route handle against telemetry, so they cannot catch the collision. Optional follow-up, separate gap: PLAN.md:166's "Reaching BINGO = force-RTB" has no implementation anywhere in mcp/.

### [tools-missions] Fix the two contradictory meanings of bingo_fuel_pct in mission returns
- plan: `PLAN §4.3 mission_status returns / §4.5 M4` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: server.py:396 (and identically 335, 344) sets handle['bingo_fuel_pct'] = gate['required_pct'], which safety.py:262 defines as plan_fuel + return_fuel + 20% reserve (the cost of the whole mission). server.py:363 sets tele['bingo_fuel_pct'] = fm.bingo_fuel_pct(...), which safety.py:242-247 defines as return leg + reserve only (the actual BINGO line).
- required: One name, one meaning. A harness comparing live fuel_pct against the bingo_fuel_pct its mission handle returned will treat the mission's total cost as the RTB threshold and abort far too early — or, on a cheap mission, far too late relative to what it thinks it measured. Return required_pct/est_fuel_pct as their own fields and reserve bingo_fuel_pct for the return+reserve line.
- files: godseye/mcp/godseye_uav/server.py
- scope correction: Scope is accurate as claimed. Concrete remaining work: at server.py:335, 344, 396 stop assigning gate["required_pct"] to handle["bingo_fuel_pct"]; instead emit handle["est_fuel_pct"] = gate["plan_fuel_pct"], handle["required_pct"] = gate["required_pct"] (or attach the whole gate dict, which also satisfies PLAN.md:145's "bingo gate result"), and set handle["bingo_fuel_pct"] from fm.bingo_fuel_pct(...) exactly as server.py:363 does, so the key means return-leg+reserve everywhere. Optionally add fuel_pct/bingo_fuel_pct to the uav_task_status return (server.py:538-544 / tasking.py:52-61) per PLAN.md:151.

### [tools-missions] Gate from the vehicle's current position and feed wind into the fuel estimate
- plan: `PLAN §4.5 M4 pre-flight gate / M15 wind in fuel math` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: server.py:300 `start = self.envelope.home[:2] if self.envelope.home else (waypoints[0]['lat'], waypoints[0]['lon'])` — the dry run always starts at home, never at the drone's actual position, so the transit leg from where the drone is to the first mission waypoint is never costed. server.py:301 calls fm.preflight_gate(waypoints, start, speed) with no headwind, although safety.py:249 accepts headwind_mps and safety.py:192-196 applies it. sim_set_environment (server.py:509-536) pushes wind to the sim and the audit log but stores it nowhere the FuelModel can read.
- required: M4 requires the gate to bound the real plan: start from live telemetry position and include the current wind field, since PLAN §4.5 specifies rate(phase, wind). A mission tasked from a distant orbit point is currently gated on a fuel figure that omits the whole ingress leg and any headwind penalty.
- files: godseye/mcp/godseye_uav/server.py

### [tools-missions] Give missions an identity: mission_id, mission journal, and overlay publication
- plan: `PLAN §4.3 mission_handle / §4.8 uav://mission/{id} / §3.1 mission overlays` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: server.py:390-392 submits every mission as tool 'uav_fly_route', so the task record loses the kind (only the transient return dict carries handle['kind'], server.py:393). No mission_id is generated anywhere. store.log_mission (store.py:70) and missions.jsonl have zero production callers (grep: only tests/test_store.py:36-38). bridge.py:172 initialises self.mission_overlay to an empty FeatureCollection and nothing ever writes to it, so GET /mission-overlay (bridge.py:275-277) is permanently empty.
- required: A mission handle distinct from a task id, a missions.jsonl lifecycle record (planned/started/progress/completed) for replay and resume (§4.8), and publication of the planned geometry (route polyline, grid pattern, waypoints, threat rings) to /mission-overlay so the GEV command center can draw the mission as §3.1 requires. Also, uav_mission returns no waypoints at all, so neither the harness nor the UI can see the plan it just approved.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/missions.py

### [tools-missions] Return the planned footprint, waypoints, est_time_s and est_fuel_pct from mission_grid_search
- plan: `PLAN §4.3 mission_grid_search returns` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: server.py:393-398 returns only {kind, doctrine, waypoint_count, bingo_fuel_pct, sweep_spacing_m} on top of the task handle. The gate dict already computes est_time_s and est_distance_m (safety.py:271-272) and plan_fuel_pct (safety.py:266) but they are dropped — only required_pct survives, and only under the wrong name (see the bingo_fuel_pct finding).
- required: Plan specifies returns: mission_handle, derived footprint + lane spacing, waypoints, est_time_s, est_fuel_pct, bingo gate result. Footprint (missions.footprint_m) is never surfaced, the waypoint list is never surfaced, and the gate result is discarded on success — so a harness cannot dry-run, cannot explain the plan, and cannot check coverage before committing, which is the PLAN §5 'plan → dry-run → execute' workflow.
- files: godseye/mcp/godseye_uav/server.py

### [tools-missions] Add the expanding-square pattern and the camera param to grid_search
- plan: `PLAN §4.3 mission_grid_search pattern(lawnmower/expanding-square) / §5 SAR expanding square` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: missions.py:153-160 grid_search accepts only polygon/alt_m/hfov_deg/overlap/speed_mps and always calls lawnmower_waypoints (missions.py:67-104). No 'pattern' parameter exists and no expanding-square generator exists in the module. No 'camera' parameter on any mission kind.
- required: pattern(lawnmower|expanding-square) per §4.3, since PLAN §5 lists 'SAR expanding square' as a required harness doctrine pattern; and a camera selector so footprint math and captures bind to a named camera rather than the module-level DEFAULT_HFOV_DEG (missions.py:22).
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py
- scope correction: The expanding-square half stands in full and is the bulk of the work: add a `pattern` kwarg to the grid_search branch (missions.py:153) dispatching lawnmower vs a new expanding_square_waypoints generator seeded on the polygon centroid (_centroid, missions.py:53) with leg growth = grid_sweep_spacing_m (missions.py:39), subject to the same MAX_GRID_LANES-style bound (missions.py:28) so the BINGO gate at server.py:387 stays satisfiable; surface the accepted values in the uav_mission description (server.py:376). The camera half is smaller than claimed: footprint math is already per-call tunable via hfov_deg (missions.py:156), so what is actually left is (a) a named-camera -> {hfov,vfov} lookup so the harness names a sensor instead of passing raw degrees, and (b) fixing missions.py:160 to pass the caller's hfov_deg/overlap into grid_sweep_spacing_m so the reported sweep_spacing_m matches the flown geometry. Item (b) is a one-line correctness fix and is independently worth doing regardless of the pattern work.

### [tools-missions] Use the handoff's derived standoff and require positive ID before custody transfer
- plan: `PLAN §4.3 mission_handoff_track / M10, M5` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: threat.py:148-159 plan_handoff derives standoff_m = envelope_m * 1.1 from the OB library but returns it only inside a human-readable `reason` string. server.py:493-495 then plans the receiver's orbit with hardcoded alt_m=60.0, radius_m=150.0, discarding the derived standoff entirely — for a 'sam' track (threat.py:21, envelope 15000 m) the receiver is sent to orbit 150 m from it. Acceptance (threat.py:150-157) tests only receiver fuel >= 30% and range.
- required: M10: sensor cue + positive-ID confirm before custody transfer, and M5 standoff honoured in the receiver's route. Today the handoff accepts on fuel/range alone with no ID confirmation step, and flies the receiving drone deep inside the threat ring the code just computed.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/threat.py

### [tools-sim-admin] Add sim_set_time (M6 sun-angle) — no time-of-day control or sun model exists
- plan: `PLAN §4.4 sim_set_time(datetime / clock_speed, M6); §5 doctrine "EO day / IR night, sun-glare avoidance"` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: No tool (server.py:317-544). The fake's `simSetTimeOfDay` handler is a no-op stub: godseye/mcp/godseye_uav/fake_airsim.py:512-516 accepts the args and `return True` without storing anything. The only sun-aware code is `orbit_ring(..., sun_azimuth_deg)` in missions.py:108-117, consumed via missions.py:166 `kw.get("sun_azimuth_deg")`.
- required: A `sim_set_time` tool that sets sim datetime/clock speed AND a server-side solar model so the sun azimuth/elevation can be derived and returned. Today the harness has no way to set day/night and no way to learn the sun angle — it must invent its own solar math and hand `sun_azimuth_deg` in as a raw mission param, so M6 sun-side orbit doctrine is unverifiable and the Phase-6 "night IR mission" acceptance is unreachable. `uav_capture_image`'s planned "+ sun angle" return (PLAN §4.2) has no source either.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py, godseye/mcp/godseye_uav/missions.py

### [tools-sim-admin] Add sim_set_weather precipitation/obscurants (rain/snow/fog/dust)
- plan: `PLAN §4.4 sim_set_weather(rain/snow/fog/dust ...); M18 sensor-degradation weather` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: nothing. No weather params on any tool (the closest, `sim_set_environment`, server.py:509-513, takes only wind_north/east/down, gps_denied, gps_noise_m, det_false_neg, det_false_pos). The fake's `simSetWeatherParameter` is a no-op stub: godseye/mcp/godseye_uav/fake_airsim.py:509-510 `return True` with no state stored.
- required: Weather params exposed as a tool, stored in the fake, and coupled to something observable: the fake's detection path (fake_airsim.py:437-486) uses a fixed 500 m filter radius and the image path (fake_airsim.py:407-434) returns a fixed solid-colour PNG per image type — neither degrades with weather. Without this a harness cannot exercise or reason about degraded-sensor conditions, and the INTREP "sensor conditions" field (PLAN §4.7) has nothing to report.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py
- scope correction: Gap CONFIRMED as missing; two scope refinements to the prior auditor's "what_is_required", which slightly over-describes the effort. (1) Do NOT include the wind vector -- PLAN.md:159's sim_set_weather row bundles wind with rain/snow/fog/dust, but the wind half is already implemented and working under the renamed sim_set_environment (server.py:509-511 -> fake_airsim.py:517-521 -> set_wind fake_airsim.py:212-215, affecting physics at fake_airsim.py:143-147). Only precipitation/obscurants are missing. (2) The "couple to something observable" work is smaller than described: the sensor-degradation pipeline already exists end-to-end -- set_detection_realism (fake_airsim.py:227) is wired through server.py:529-532 and consumed in the detection loop at fake_airsim.py:447-449 (false-neg drop) and :466-468 (false-pos phantom). Actual remaining work: (a) add rain/snow/fog/dust float params to the existing sim_set_environment tool (server.py:509-513) rather than authoring a new tool, keeping one environment surface for the harness; (b) store them in _FakeSim alongside _wind/_gps_noise_m (fake_airsim.py:94-100) with a setter beside set_detection_realism (fake_airsim.py:227), and make simSetWeatherParameter (fake_airsim.py:509-510) write that state instead of returning True; (c) derive an effective visibility scalar from those params and use it in two places -- scale the hardcoded 500.0 detection radius (fake_airsim.py:445) and bias the existing _det_fn_rate (fake_airsim.py:447) -- reusing the proven false-neg path rather than building new degradation logic; (d) surface the current values in telemetry/report output so INTREP's "sensor conditions" field (PLAN.md:176) has a source. Degrading the image path (fake_airsim.py:406-434, fixed _png_solid) is optional polish, not required for the harness goal, since the fake already returns flat synthetic colour frames that carry no imagery signal a harness reasons over.

### [tools-sim-admin] Add sim_reset that preserves track store and pattern-of-life (M12)
- plan: `PLAN §4.4 sim_reset; §4.8 "Track store + pattern-of-life persist across sim_reset (M12)"` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: No tool (server.py:317-544). The fake has an unexposed RPC `reset()` at godseye/mcp/godseye_uav/fake_airsim.py:269-276 which resets vehicles (ned/vel/armed/landed/task) and leaves `_objects` intact — but nothing calls it from the MCP layer.
- required: An MCP `sim_reset` plus the state discipline M12 demands. Beyond the missing tool, the state it must protect is not durable: `TrackManager` is a plain in-memory dict created per server instance (server.py:212-213, targets.py:79-82) and `Store` journals only tasks/missions/audit/fuel (store.py:62-65) — there is no tracks or pattern-of-life journal, so tracks do not survive a server restart either. Conversely the per-vehicle `FuelModel` instances (server.py:225-230) and the task queues WOULD need explicit reset, which no code does. As written the M12 invariant cannot even be tested.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/store.py, godseye/mcp/godseye_uav/targets.py

### [tools-sim-admin] sim_spawn_target takes a raw mesh string instead of an OB class key, and has no heading/mobile_route
- plan: `PLAN §4.4 sim_spawn_target(class — OB library key, lat, lon, heading, mobile_route?); Phase 4 "sim_spawn_target + OB classes"` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: godseye/mcp/godseye_uav/server.py:443-448 `sim_spawn_target(name, mesh, lat, lon, alt_m=0.0)`. Classification is substring matching on the free-text `name` — targets.py:18-26 `_CATEGORY_KEYWORDS`, targets.py:29-35 `classify()`, applied to the detection name at targets.py:106-113. The threat OB table is a separate dict at threat.py:20-30 `_OOB`.
- required: A `class` param keyed to a published OB library, plus `heading` and `mobile_route`. Today: (1) there is no enumerable OB library the harness can read — `_CATEGORY_KEYWORDS` (targets.py:18) and `_OOB` (threat.py:20) are private and unexposed (no `uav://targets` resource per §4.8); (2) the `mesh` argument is semantically inert in the zero-GPU path — the fake stores only object_name -> GeoPoint and discards mesh_name (fake_airsim.py:488-495) — so class is decided entirely by whether the caller happens to embed a magic substring in `name`, and any other spelling silently yields category "unknown" (targets.py:35) which the threat pipeline scores at weight 0.1 / envelope 1000 m (threat.py:29) with no warning; (3) no `heading`, so `Track.heading_deg` (targets.py:47) stays None on a static target; (4) no `mobile_route`. A harness following PLAN/skill wording will call this tool wrong on the first try.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/targets.py, godseye/mcp/godseye_uav/threat.py
- scope correction: Gap stands as written. Only rhetorical trim: the MCP tool schema is self-describing and its description already says "name should hint the category" (server.py:441-442), so a schema-driven harness will call the correct SHAPE on the first try — it just cannot know which magic substrings are valid, and gets no feedback when it guesses wrong. Remaining work is unchanged in size: (a) accept a `class` param validated against a published enumerable OB library (merge targets.py:18-26 keywords with threat.py:20-30 envelopes into one public table), echo the resolved class in the return and reject/warn on unknown; (b) add `heading` and plumb orientation through spawn_object (server.py:185-191) into Track.heading_deg for static targets; (c) add `mobile_route` plus the missing PLAN §4.4 `sim_move_target`, wiring the already-present fake simSetObjectPose (fake_airsim.py:500-508) through a UavBackend method; (d) publish the OB library as the `uav://targets` resource per PLAN §4.8 so a harness can enumerate valid class keys.

### [tools-sim-admin] GPS degradation is global and never affects the position the harness reads (M16)
- plan: `PLAN §4.4 sim_set_gps_degradation(vehicle, error_m / denial, M16); §5 "GPS-degraded fallback = INS/terrain nav"` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: Implemented under a different name: server.py:506-536 `sim_set_environment(..., gps_denied, gps_noise_m, ...)` -> fake `set_gps_denied` (fake_airsim.py:217-220), `set_gps_noise` (:222-225), applied in `_reported_geo` (:233-249) which feeds `getMultirotorState.gps_location` (:358-372) and `getGpsData` (:382-400). Tests exercise the fake directly (tests/test_realism.py:75-107).
- required: Per-vehicle degradation that actually degrades the navigation picture. Three concrete shortfalls: (1) NOT per-vehicle — `_gps_denied`/`_gps_noise_m` are single sim-wide fields (fake_airsim.py:97-101) set globally (fake_airsim.py:217-225), so you cannot GPS-deny one drone during a two-drone handoff (M10); (2) the degradation is invisible where it matters — `UavBackend.telemetry` derives lat/lon/alt_hae from the ground-truth NED `kinematics_estimated.position` (server.py:100-119, fake_airsim.py:361) and only the nested `"gps"` sub-dict (server.py:118) carries the degraded fix, and the bridge snapshot the GEV UI consumes does the same (bridge.py:111-127), so denial/noise never moves the reported position, never perturbs mission geometry, and never triggers any INS/terrain fallback; (3) silent no-op in `--real` mode — the hooks require `self.backend.sim` (server.py:523, set only when the fake runs in-process, launch.py:64-74) yet the tool still returns `{"ok": True}` (server.py:536). Also `error_m` vs `gps_noise_m` naming, and calling `sim_set_environment(gps_denied=True)` silently zeroes any previously set wind because wind_* default to 0.0 (server.py:509-511).
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py, godseye/mcp/godseye_uav/bridge.py
- scope correction: Gap stands as written. One mitigation worth recording for scoping: the plumbing exists end-to-end (MCP tool server.py:506-536 -> fake setters fake_airsim.py:217-225 -> `_reported_geo` :233-249), so remaining work is moderate, not greenfield: (a) key `_gps_denied`/`_gps_noise_m` by vehicle name and add a `vehicle` param to the tool (or a `sim_set_gps_degradation(vehicle, error_m, denial)` alias for PLAN.md:161 naming); (b) surface degradation where the harness reads it — add `gps_valid`/`eph`/`fix_type` and a degraded/INS-dead-reckoned position to `UavBackend.telemetry` (server.py:109-119), and have the safety gate (server.py:293) grow the geofence margin from that quality; (c) make wind_north/east/down default to None so a GPS-only call does not zero wind (server.py:509-519); (d) return `{"ok": False, "unsupported": [...]}` instead of `{"ok": True}` when `self.backend.sim` is None under `--real` (server.py:523-536); (e) add a test that drives `sim_set_environment` through the MCP tool and asserts the telemetry position (not just `gps_location`) reflects denial.

### [safety-fuel] Publish the real BINGO line on mission handles, not the plan's total required fuel
- plan: `PLAN.md §4.5 ("`bingo_fuel_pct` published in mission state"), §4.1 `uav_fly_route` returns task_handle + bingo_fuel_pct` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: True
- exists: Two different quantities are published under the same name. server.py:335, server.py:344 and server.py:396 set `handle["bingo_fuel_pct"] = gate["required_pct"]`, which safety.py:262 defines as `plan_fuel + return_fuel + 20% reserve` — the total fuel the whole mission needs. server.py:363 publishes `fm.bingo_fuel_pct(...)` which safety.py:242-247 defines as `return-leg fuel + reserve` — the actual abort line. A harness comparing telemetry `fuel_pct` against a mission handle's `bingo_fuel_pct` therefore compares against the wrong threshold, and on a long plan `required_pct` can exceed 100.
- required: One definition of `bingo_fuel_pct` (return leg + reserve at current position) everywhere; the plan-cost number should be surfaced under a distinct name such as `required_pct`.
- files: mcp/godseye_uav/server.py
- scope correction: Not "publish the real BINGO line" (it exists and works at safety.py:242-247, published at server.py:363, consumed correctly by uavMissionPanel.js:549). What is actually left is a 4-line rename so one name does not carry two meanings: at server.py:335, 344, 396 change `handle["bingo_fuel_pct"] = gate["required_pct"]` to `handle["required_pct"] = gate["required_pct"]`, and (optionally) add `handle["bingo_fuel_pct"] = round(fm.bingo_fuel_pct(start, alt), 2)` so the handle carries the same definition as telemetry. No change to safety.py, no change to the telemetry path, no change to the gate logic, no new capability. Severity is cosmetic/contract-clarity, not a safety defect: the currently published value is always <= 100 and always >= the true bingo, so it errs conservative.

### [safety-fuel] Enforce max_speed_mps in the safety gate
- plan: `PLAN.md §4.5 ("Geofence polygon around AO; ceiling / max speed / min AGL")` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: mcp/godseye_uav/safety.py:141 declares `max_speed_mps: float = 20.0`, but nothing ever reads it — grep for `max_speed` across mcp/godseye_uav/*.py matches only that declaration. `SafetyEnvelope.check_point` (safety.py:145) and `check_route` (safety.py:156) take no speed argument, and `GodseyeUavServer._gate` (server.py:298-307) passes `speed` only into the fuel estimate. `uav_goto_gps(speed_mps=500)` (server.py:330) passes the gate.
- required: The gate must reject (or clamp) any commanded speed above the envelope's max_speed_mps.
- files: mcp/godseye_uav/safety.py, mcp/godseye_uav/server.py

### [safety-fuel] Close the ceiling / min-AGL bypasses on takeoff and ground-altitude waypoints
- plan: `PLAN.md §4.5 (ceiling / min AGL)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: Three holes. (1) safety.py:152 guards min AGL with `if 0 < alt_agl < self.min_agl_m`, so alt_agl of exactly 0 (or negative) never violates. (2) safety.py:159 defaults a waypoint with no altitude key to 0, so such a waypoint always passes both checks. (3) `uav_takeoff` (server.py:317-319) and `uav_return_to_home` (server.py:325-327) call `self._submit(...)` directly with no `self._gate(...)` at all — `uav_takeoff(vehicle, alt_m=5000)` is never ceiling-checked and reaches backend.takeoff at server.py:240.
- required: min-AGL violation on any altitude below the floor including 0; a missing altitude should be rejected rather than defaulted to 0; takeoff altitude must pass the ceiling check.
- files: mcp/godseye_uav/safety.py, mcp/godseye_uav/server.py

### [safety-fuel] Add in-flight geofence enforcement and breach response
- plan: `PLAN.md §4.5 (geofence, server-enforced) and §3.1 (geofence-proximity alarms)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: Geofence is checked only at plan-submission time, via `SafetyEnvelope.check_route` called from `_gate` (server.py:302). `SafetyEnvelope.geofence_margin_m` (safety.py:164-168), the function built for proximity warning, has zero production callers (only tests/test_safety.py:61-62). The bridge telemetry loop (bridge.py:175-185) has no SafetyEnvelope reference, so the live position is never tested against the polygon, and no breach action (hover/RTB) or alarm exists. A vehicle blown outside the AO by the M15 wind model (fake_airsim.py:140-147) is never detected.
- required: Per-tick geofence margin check on live position, with a proximity warning and an autonomous breach response; PLAN.md:106 also expects it as an alarm event.
- files: mcp/godseye_uav/bridge.py, mcp/godseye_uav/server.py, mcp/godseye_uav/safety.py

### [safety-fuel] Expose the safety envelope to the harness
- plan: `PLAN.md §4.8 resource `uav://safety/geofence`; §5 skill workflow "plan → dry-run (fuel/geofence/sun)"` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: Nothing reads the envelope out. The geofence/ceiling/max-speed/min-AGL/home values are set once at launch.py:75 (`SafetyEnvelope(geofence=ao, home=t["home"])`, AO polygons at launch.py:33-45) and are never returned by any tool — none of the 19 tools at server.py:317-538 exposes them, and no MCP resources are registered. The only way a harness learns the AO is by submitting a plan and reading the `envelope_violations` list back out of a rejection (server.py:303, 332-333).
- required: A tool or resource returning the active geofence polygon, ceiling, max speed, min AGL and home, so the harness can plan inside the AO instead of discovering it by trial rejection.
- files: mcp/godseye_uav/server.py
- scope correction: Gap stands as written, with two refinements to the fix. (a) max_speed_mps (safety.py:141) is a DEAD field — check_point/check_route (safety.py:145-162) never test speed and grep finds no other reference in mcp/ or tests/; exposing it as an active limit would advertise an unenforced constraint, so either enforce it or omit it from the payload. (b) The fix is small in lines because the underlying math is already written and tested: one read-only tool (e.g. uav_get_envelope) returning geofence polygon, ceiling_m_agl, min_agl_m, home, plus geofence_margin_m(lat,lon) (safety.py:164-168, already covered by tests/test_safety.py:59-62). Ideally also have launch.py:32-45 serve the AO so gods-eye-view/src/ui/uavMissionPanel.js:11-80 stops duplicating it.

### [safety-fuel] Populate fuel_pct (and mission/track_id) in the bridge snapshot
- plan: `PLAN.md §3.1 (HUD "fuel %, ETA-to-BINGO"; mission status object with "fuel % vs BINGO line")` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: mcp/godseye_uav/bridge.py:46-48 declares `fuel_pct: float = 100.0`, `mission: str = ""`, `track_id: str = ""` on VehicleSnapshot, but `AirSimAdapter.snapshot()` (bridge.py:123-138) never assigns any of the three, so `/snapshot` (bridge.py:258-266, which serialises `vars(s)`) always reports 100% fuel. The GEV UAV layer consumes exactly this field — gods-eye-view/src/sources/live/uav.js:90 `fuelPct: finite(row?.fuel_pct)` rendered into the HUD label at gods-eye-view/src/layers/uav/index.js:89-92 — so the operator's fuel readout is a constant fiction. The mission panel path (gods-eye-view/src/ui/uavMissionPanel.js:549 "fuel …% bingo …%") reads `uav_get_telemetry`, which returns the equally static server.py:362 `fm.fuel_pct`. No `missions[]`/`contacts[]` sections exist in /snapshot either.
- required: The bridge snapshot must carry the live integrated fuel_pct (and the BINGO line) so the command-center HUD shows real fuel state.
- files: mcp/godseye_uav/bridge.py, mcp/godseye_uav/launch.py

### [safety-fuel] Feed wind into the BINGO gate and the live burn rate
- plan: `PLAN.md §4.5 ("fuel -= rate(phase, wind)*dt"); REVIEW_MILITARY.md:55-56 (M15)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: False
- exists: `FuelModel._burn` (safety.py:192-196) and `estimate_route`/`preflight_gate` (safety.py:208, 249) all accept `headwind_mps`, but every production call site passes the default 0.0: `GodseyeUavServer._gate` calls `fm.preflight_gate(waypoints, start, speed)` with no wind (server.py:301), and `uav_get_telemetry` calls `fm.bingo_fuel_pct(...)` with no wind (server.py:363). `sim_set_environment` (server.py:509-536) pushes wind into the sim over RPC and into the audit log, but never stores it on the server, so nothing can retrieve it. The fake sim does drift the vehicle with wind (fake_airsim.py:140-147), so the gate systematically under-estimates burn in wind.
- required: Persist the commanded wind vector on the server and resolve the headwind component per leg into preflight_gate, bingo_fuel_pct and the live tick.
- files: mcp/godseye_uav/server.py, mcp/godseye_uav/safety.py
- scope correction: NOT overstated — UNDERSTATED. The prior auditor's "partial" is correct for the gate/BINGO half (state b: params exist, call sites pass 0.0), but the "live burn rate" half is state (a) MISSING, not partial: FuelModel.tick has zero production call sites (`grep "\.tick(" mcp/` = no matches) and Store.log_fuel has zero production call sites, so there is no live integrator to feed wind into and fuel_pct is permanently 100.0. Real remaining work is therefore LARGER than the claimed fix: (1) persist the commanded wind vector on GodseyeUavServer in sim_set_environment (server.py:520) instead of discarding it; (2) resolve the per-leg headwind component from that vector and pass it into preflight_gate (server.py:301) and bingo_fuel_pct (server.py:363); AND (3) build the live tick that does not exist — call FuelModel.tick from the telemetry path or a background task with measured speed/vz/landed plus the resolved headwind, and persist via Store.log_fuel per PLAN.md:167. Without (3), (1) and (2) alone still leave the BINGO gate comparing against a tank that never drains.

### [safety-fuel] Add hover-recovery to the watchdog timeout path
- plan: `PLAN.md §4.5 ("Watchdog: future not resolved in timeout → fail handle → hover-recovery (T2)"); REVIEW_TECHNICAL.md T2` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: The watchdog fails the handle but never recovers the vehicle. tasking.py:166-170 catches `asyncio.TimeoutError`, sets FAILED and cancels the runner — no hover is issued and no recovery callback exists. The cancellation surfaces in the executor at server.py:274-282, which calls `self.backend.cancel_last(v)` only; `self.backend.hover(v)` (server.py:163-165) is never called on this path (it is called only by the explicit `uav_abort` executor branch at server.py:268-271). A vehicle whose command times out is therefore left under whatever the sim was last doing, with no command in flight.
- required: After a watchdog timeout, put the vehicle through cancelLastTask + hover before marking the handle failed.
- files: mcp/godseye_uav/tasking.py, mcp/godseye_uav/server.py
- scope correction: Remaining work is one line plus a test, not a new recovery mechanism.

Add `await self.backend.hover(v)` immediately after the existing `await self.backend.cancel_last(v)` inside the `except asyncio.CancelledError:` handler at godseye/mcp/godseye_uav/server.py:276-280 (inside the same try/except Exception guard so a hover RPC failure cannot mask the cancellation). That single edit satisfies PLAN.md:169 and the plan's "abort = cancelLastTask + hover" contract (PLAN.md:129) for the timeout path, and it also covers harness-initiated cancels (tasking.py:180-187) since both funnel through the same handler.

Already satisfied, do NOT redo:
- cancelLastTask on the timeout path (server.py:277-278) - proven to fire.
- Ordering "recover before marking the handle failed" - proven: recovery runs while state is EXECUTING; tasking.py:167 sets FAILED only after the runner finishes unwinding. No re-plumbing of tasking.py and no recovery callback parameter is needed.
- Audit trail of the event (server.py:281 log_task "cancelled").

Add a regression test (e.g. in tests/test_tasking.py or tests/test_server.py) using a stub backend + small watchdog_s asserting `hover` appears in the backend call list after a timeout; note that in the fake sim hover is physically near-inert (fake_airsim.py:317-319 sets a "hover" task with no matching branch in the _integrate chain), so assert on the call, not on motion.

### [safety-fuel] Make ceiling/min-AGL terrain-relative, not height-above-home
- plan: `PLAN.md §4.5 (min AGL / ceiling); user goal: realistic missions over ACTUAL real-world data` | state: **partial** | verdict: OVERSTATED | effort: L | blocks_goal: True
- exists: "AGL" is really height above the home datum everywhere: server.py:63-67 `llh_agl_to_ned` computes `abs_alt = self.home_geo.altitude + alt_agl_m`, and bridge.py:129 sets `agl=max(0.0, -ned.z)`. There is no terrain/DEM source anywhere in mcp/godseye_uav (grep for terrain/elevation/dem matches only a comment at fake_airsim.py:413). The shipped theaters are mountainous real-world AOs — launch.py:33 indo-pak-loc home 1600 m, launch.py:35 iran-isfahan home 1550 m — so `ceiling_m_agl=120.0` (safety.py:140) and `min_agl_m=3.0` (safety.py:142) are being applied against the home plane, not the ground. On real terrain the min-AGL floor can sit hundreds of metres underground.
- required: Terrain elevation lookup (DEM or AirSim ground query) so ceiling and min AGL are evaluated against ground height at each waypoint.
- files: mcp/godseye_uav/safety.py, mcp/godseye_uav/server.py, mcp/godseye_uav/geo.py
- scope correction: Do NOT build a DEM/terrain-elevation service in the MCP server — the plan schedules that as Phase 6 / M19 (PLAN.md:196,202) and the shipped fake sim has no terrain for it to read. What is actually left is small and splits in two:

(a) Naming/semantics so the harness is not misled (the only part that touches the agentic goal, ~30 min). `alt_m` is documented as "AGL" in the tool surface — server.py:317 ("Arm and take off to alt_m (AGL)"), safety.py:142 `min_agl_m`, safety.py:211 — while server.py:64-66 defines it as height above the launch datum. Rename/redocument to `alt_m_above_launch` (or state "AGL above launch datum; flat-world sim, terrain relief not modelled — see PLAN Phase 6/M19") in the uav_takeoff / uav_goto_gps / uav_fly_route tool descriptions and in SafetyEnvelope's docstring. That removes the one real harness-facing hazard: a skill reading "AGL" and assuming terrain-following.

(b) Display clamping in GEV, if wanted before Phase 6 (optional, and browser-side — not server-side). The reference machinery already exists in the repo the uav layer was cloned from: src/layers/transit/height.js (`services.ground` — `cachedGroundFloor`, `warmGroundFloor`, `coarseFloorCoord`) samples the rendered mesh floor with camera-altitude gating. The uav layer (src/layers/uav/index.js:96-100) currently sets ellipsoid height directly and sets no `heightReference`, which also misses PLAN.md:83 ("GEV sets entity heightReference explicitly"). That is a separate, smaller finding about the GEV layer, not about the server's safety envelope.

If anything in this dimension deserves a real finding, it is a different defect the auditor did not raise: launch.py:61 feeds the theater's MSL ground elevation into `GeoPoint(...)`, whose `altitude` field is documented as altHae (geo.py:25,29) — a ~45 m datum slip in the Kashmir/Isfahan theaters that is larger than the min-AGL limit itself and is genuinely in Phase-0/T1 scope.

### [bridge-command-center] Add SSE /events channel for BINGO, geofence-proximity, lost-link and detection alarms
- plan: `PLAN §3.1 feed table row 6 + 'Implementation notes' bullet 2 + Phase-3 DoD (≥3 alarm types)` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: False
- exists: nothing. The bridge registers exactly ten routes (bridge.py:253, 258, 268, 275, 279, 358, 362, 366, 370, 384) and none is /events; grep for StreamingResponse / text/event-stream across mcp/ returns no hits, and grep for EventSource across gods-eye-view/src returns no hits. There is also no event bus to feed it: safety/audit events are only appended to JSONL (store.py:73-74, called e.g. at server.py:355, 408-410) with no in-process publisher, and safety.py exposes geofence_margin_m (safety.py:164-168) and bingo_fuel_pct (safety.py:242-247) but nothing watches them per tick to raise an alarm.
- required: A one-way SSE endpoint (browser EventSource) publishing BINGO warnings, geofence-proximity warnings, lost-link/LOAL activation and detection events as they happen, fed by an in-process event bus that the fuel integrator, geofence check and track ingest push to.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/safety.py, godseye/mcp/godseye_uav/server.py, gods-eye-view/src/ui/uavMissionPanel.js

### [bridge-command-center] Loop A never publishes fuel, mission or track_id — /snapshot reports a constant fuel 100%
- plan: `PLAN §3.1 operator experience #2 (HUD fuel %, ETA-to-BINGO) + feed table row 3` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: VehicleSnapshot declares fuel_pct=100.0, mission="", track_id="" as defaults (bridge.py:46-48) and AirSimAdapter.snapshot() constructs it at bridge.py:123-138 without ever setting any of the three; a grep for `fuel_pct` in bridge.py hits only line 46. The real tick-reconciled integrator lives on the MCP side (safety.py:173-206, exposed at server.py:358-366) and is never joined to the telemetry cache. Downstream the false value is rendered as truth: gods-eye-view/src/sources/live/uav.js:84 maps it to status.fuelPct, src/layers/uav/index.js:89-92 paints it into the globe label and index.js:232 feeds it to the cockpit HUD — so the operator sees 100% fuel for the whole flight.
- required: Loop A (or the missions[] serializer) must carry the server-side fuel_pct, bingo_fuel_pct, active mission id and assigned track id into every /snapshot vehicle row, so the HUD's fuel/BINGO readout reflects the integrator rather than a dataclass default.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/server.py, godseye/tests/test_bridge.py

### [bridge-command-center] contacts[] absent from /snapshot; the /tracks substitute lacks confidence and is polled at 5 Hz instead of on change
- plan: `PLAN §3.1 feed table row 5 ('/snapshot.contacts[] — persistent track IDs, class, confidence, last SALUTE, on change')` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: False
- exists: There is no `contacts` key in /snapshot (bridge.py:258-266). A near-equivalent exists at a different endpoint: bridge.py:370-382 proxies the MCP uav_list_tracks tool (server.py:434-438), which returns salute_report() rows (targets.py:129-144) carrying track_id, unit (= OB category), location, time, equipment, sightings, speed/heading — but NO confidence field anywhere in targets.py. Delivery is also wrong-shaped: gods-eye-view/src/sources/live/uav.js:105-110 calls /tracks inside every getSnapshot, and the layer polls at 200 ms (src/layers/uav/index.js:10, 137), so the bridge fires ~5 blocking MCP JSON-RPC round-trips per second (bridge.py:292-318 uses urllib with a 15 s timeout) instead of publishing contacts on change.
- required: contacts[] serialized into /snapshot (or pushed on change via SSE) with persistent track ID, class, a confidence value, and the last SALUTE — the 5 Hz MCP proxy poll replaced by change-driven publication.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/targets.py, gods-eye-view/src/sources/live/uav.js

### [bridge-command-center] Loop B is never un-gated: camera subscribers are added but never released, and POST /camera/subscribe is a no-op stub
- plan: `PLAN §1 loop B (T3b) 'subscriber-gated … start when the first client opens the PIP, stop when the last closes'; §8.1 budget '1 camera subscriber max'` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: Lazy start works — GET /camera/{veh} calls state.subscribe_camera (bridge.py:387) and camera_loop only issues simGetImages when the subscriber set is non-empty (bridge.py:196-206). But BridgeState.unsubscribe_camera (bridge.py:212-215) has no caller anywhere (grep: defined at 212, referenced nowhere else), there is no unsubscribe route among the ten registered endpoints, and no TTL/last-seen expiry on _cam_subs — so a single PIP request pins a 3 Hz simGetImages readback for the life of the process. POST /camera/subscribe (bridge.py:279-281) worsens it: it ignores the request body and returns {"subscribed": True} without touching state, so a client that uses the documented subscribe endpoint registers nothing. No max-subscriber cap is enforced anywhere (create_app takes no budget config, bridge.py:226-229).
- required: Subscriber release on client close or idle-TTL so loop B actually stops, a working (or removed) /camera/subscribe + /camera/unsubscribe pair, and the §8.1 one-subscriber cap enforced in bridge config.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/tests/test_bridge.py

### [bridge-command-center] Command-center panel's tactical overlays are dead code — it reads a /snapshot shape the bridge does not emit
- plan: `PLAN §3.1 'Live data feeds' / operator experience #3 (mission geometry visible live)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: gods-eye-view/src/ui/uavMissionPanel.js:476-478 does `const out = await get('/snapshot'); const snap = out?.result ?? out; const rec = (snap?.records || [])[0];` and returns early at line 479 when rec is undefined. The bridge's /snapshot returns `{sim_state, observedAtMs, count, vehicles:[…]}` with snake_case fields (bridge.py:261-266, vars(VehicleSnapshot)); `records` with nested position/velocity is the normalized *source* shape produced later by src/sources/live/uav.js:121. Because the panel bypasses the source and hits the bridge directly (uavMissionPanel.js:336-342), rec is always undefined, so the path-ahead polyline (lines 484-511) and the ground-track polyline (lines 513-534) never render — the only overlay geometry the command center draws today is inert.
- required: The panel must consume the bridge's actual /snapshot contract (vehicles[] with latitude/longitude/alt_hae/speed_ms/heading_deg) or go through the normalized source, so its tactical overlays actually draw.
- files: gods-eye-view/src/ui/uavMissionPanel.js, gods-eye-view/src/ui/uavMissionPanel.test.mjs

### [bridge-command-center] Loop C is missing its LOS half — no line-of-sight capability exists anywhere
- plan: `PLAN §1 loop C (T3c) 'on-demand captures/detections/LOS'; §4.2 uav_los_check; §4.3 M5 standoff 'verified vs uav_los_check'` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: Nothing outside the fake. On-demand capture exists (bridge.py:388-392 falls back to adapter.camera_png) and on-demand detections exist under a different name (MCP uav_scan_targets, server.py:401-414 → UavBackend.get_detections, server.py:170-184 — a naming deviation from the plan's uav_get_detections, not a gap). LOS has no implementation: a repo-wide grep for simTestLineOfSightToPoint / los_check returns only the fake's stub at mcp/godseye_uav/fake_airsim.py:530, with no caller in bridge.py or server.py and no uav_los_check tool in the catalog (server.py:314-545).
- required: An on-demand LOS path (bridge endpoint and/or uav_los_check MCP tool) calling simTestLineOfSightToPoint and returning the boolean plus first obstacle, so standoff/track missions can verify masking as the plan's M5 doctrine requires.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/bridge.py, godseye/tests/test_server.py
- scope correction: CONFIRMED — not a downgrade. Scope refinement only, and the real work is marginally LARGER than the prior auditor claimed, not smaller. The claim assumed a caller could simply invoke the existing simTestLineOfSightToPoint. It cannot satisfy the contract: PLAN.md:139 requires "LOS bool + first obstacle", but fake_airsim.py:531 returns a bare unconditional True with no obstacle payload and no terrain model behind it. So the work is three parts, not one: (1) extend fake_airsim.py:530 to return {los: bool, first_obstacle: {...}} against some terrain/object model so the fake is testable without a GPU host; (2) add UavBackend.los_check in server.py (alongside get_detections at server.py:170-184) plus a uav_los_check MCP tool in the catalog at server.py:314-544, and/or an on-demand bridge endpoint next to /camera/{veh} at bridge.py:384; (3) wire the M5 consumer — missions.py:161-169 track_target currently takes a free radius_m (default 80.0) with no masking check, and threat.py:148-149 derives standoff from the weapon envelope only for plan_handoff. Blast radius on the user's goal: this blocks the M5 standoff/terrain-masking doctrine and any mission that must verify sensor LOS against real terrain — which is a meaningful loss in an environment built on actual real-world elevation data — but it does NOT block basic flight, recon_route, grid_search, or the detection/track pipeline, which are wired end to end.

### [geo-datum] Add the T1 datum test (NED 0 -> expected HAE) and unpin the two tests that lock in the wrong geoid constant
- plan: `PLAN.md:196 (Phase 0) 'datum test (T1): drone at NED 0 → bridge HAE ≈ expected'` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: No test asserts an absolute expected HAE at NED 0. The nearest is godseye/tests/test_bridge.py:53-58, which asserts only the DIFFERENCE alt_hae - alt_msl ≈ -28.8 ± 3.0 — circular, since -28.8 is exactly what the coarse fallback emits. godseye/tests/test_geo.py:109-113 is named test_pyproj_accuracy_when_available and calls pytest.importorskip('pyproj'), but pyproj IS installed, so the test runs and passes WITHOUT ever exercising pyproj — it asserts the fallback's own output. godseye/tests/test_geo.py:36-40 (test_origin_roundtrip) checks NED 0 but only against ned_to_geodetic, never the HAE conversion.
- required: An absolute assertion: drone at NED (0,0,0) with a declared-MSL origin -> bridge publishes HAE = origin_msl + N(φ,λ) for an independently sourced N. Both existing tests actively block the fix: true EGM96 at Seattle is -22.21, so |−22.21 − (−28.8)| = 6.59 > 3.0, meaning test_bridge.py:58 AND test_geo.py:113 both FAIL the moment the geoid is corrected. They must be re-pinned to independently-sourced EGM96 values, not to the fallback's output, or the suite will punish the correct fix.
- files: godseye/tests/test_geo.py, godseye/tests/test_bridge.py
- scope correction: Gap stands as written; if anything it is understated. Required work, unchanged: (1) add the T1 datum test asserting an ABSOLUTE HAE at NED (0,0,0) — origin declared MSL 122.0 -> expected HAE = 122.0 + N(47.641468,-122.140165) = 99.65 m for an independently sourced N; (2) re-pin tests/test_bridge.py:58 and tests/test_geo.py:113 from -28.8 to the independently-sourced EGM96 value (-22.35), since both fail at 6.45 > 3.0 the moment the geoid is corrected. Two additions that make the fix durable and were not in the claim: (a) geo.py:151-152's bare `except Exception: pass` must stop silently masking the broken grid path — it is why the wrong constant was believable; either let the error surface or assert in-test which branch ran, otherwise the same silent-fallback trap recurs. (b) An independent N source is already on hand — the `egm96` package is installed in .venv and vendored at godseye/egm96-0.3.0-py3-none-any.whl — so the T1 expected value needs no new dependency; this trims effort but does not shrink the gap.

### [geo-datum] Declare and validate OriginGeopoint as MSL — no settings.json exists and the home altitude is typed HAE but consumed as MSL
- plan: `PLAN.md:66 (§2 Geo) 'OriginGeopoint in settings.json (entered as MSL, T1)'; PLAN.md:196 Phase 0 'Blocks env + OriginGeopoint'; REVIEW_TECHNICAL.md:10` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: No AirSim settings.json exists anywhere in the repo (searched; only .claude/settings.json). The origin is a bare 3-tuple duplicated in three places with contradictory datum labels: godseye/mcp/godseye_uav/geo.py:25 and :29 declare GeoPoint.altitude as 'altHae metres'; godseye/mcp/godseye_uav/bridge.py:25 builds DEFAULT_HOME = GeoPoint(47.641468, -122.140165, 122.0) using that HAE-typed field, then bridge.py:119 reads the derived altitude back out as `alt_msl`; godseye/mcp/godseye_uav/fake_airsim.py:24 duplicates the same constant; godseye/mcp/godseye_uav/launch.py:33-44 defines six theater homes as bare tuples (e.g. indo-pak-loc 1600.0, iran-isfahan 1550.0) with no datum stated anywhere; godseye/mcp/godseye_uav/safety.py:143 comments its home tuple as 'alt_msl'. Three different datum claims for one value.
- required: T1 requires the origin altitude be entered as MSL and validated once in Phase 0, plus a recorded datum for the chosen origin (REVIEW_TECHNICAL.md:38). Needed: a settings.json (or equivalent config artifact) carrying OriginGeopoint, a single MSL-typed home field, and a startup assertion. As written, GeoPoint.altitude means HAE in geo.py's contract and MSL at bridge.py:119 — the type system actively asserts the wrong thing, which is how the 28.8 m divergence above went unnoticed.
- files: godseye/mcp/godseye_uav/geo.py, godseye/mcp/godseye_uav/launch.py, godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/safety.py

### [geo-datum] Stop calling height-above-takeoff 'AGL' — there is no terrain model, so ceiling and min-AGL safety limits are meaningless over real terrain
- plan: `PLAN.md:164-165 (§4.5 Safety envelope) 'ceiling / max speed / min AGL'; PLAN.md:145 (M1) swath = 2·alt·tan(HFOV/2); §3 Datum altitude-semantics requirement` | state: **partial** | verdict: OVERSTATED | effort: L | blocks_goal: True
- exists: 'AGL' in this system is height above the takeoff point on a flat-earth assumption, never above ground. godseye/mcp/godseye_uav/bridge.py:129 sets agl = max(0.0, -ned.z) — pure NED down-from-home. godseye/mcp/godseye_uav/server.py:63-66 llh_agl_to_ned() computes abs_alt = home_geo.altitude + alt_agl_m, with the docstring itself conceding 'AGL above home'. godseye/mcp/godseye_uav/safety.py:145-153 checks ceiling_m_agl (120.0) and min_agl_m (3.0) against that same home-relative number. godseye/mcp/godseye_uav/missions.py:31-42 derives sensor swath and grid lane spacing from alt_agl_m, so coverage math inherits the same assumption. Grepped the entire Python tree for terrain/DEM/elevation/sampleTerrain/simGetGroundLevel: zero hits — no ground-elevation source of any kind. The value is then surfaced to the operator labelled as AGL in the GEV HUD at gods-eye-view/src/layers/uav/index.js:91-92.
- required: Either a ground-elevation lookup so AGL means AGL, or an honest rename to height-above-takeoff (HAT) with the limitation documented. This matters specifically for the user's goal: launch.py ships Srinagar (home 1600 m, mountainous valley with >1000 m relief) and Isfahan (1550 m). A 120 m 'ceiling AGL' and a 3 m 'min AGL' CFIT guard computed off the takeoff plane are both wrong by the local relief — the min-AGL floor is a safety control that cannot do its job, and M1 grid swath/lane spacing is wrong by the same ratio, silently corrupting claimed coverage percentage.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/safety.py, godseye/mcp/godseye_uav/missions.py
- scope correction: Real remaining work is a naming/labeling fix, not a geo capability. Rename the field and its harness-facing descriptions so a harness is not misled:
- godseye/mcp/godseye_uav/bridge.py:35 (`agl: float`) and :129 -> `height_above_takeoff_m` (keep `agl` as an alias key in the snapshot JSON so GEV src/sources/live/uav.js:76 keeps working).
- server.py:317 tool description "Arm and take off to alt_m (AGL)" -> "alt_m above launch point (flat-earth; no DEM)". Same for the goto/route tool descriptions at server.py:329 and the `llh_agl_to_ned` name at server.py:63.
- safety.py:140,142 `ceiling_m_agl`/`min_agl_m` -> `_hat_m`, with a one-line docstring noting the limits are launch-plane relative until the Phase 6 terrain flag lands.
- GEV HUD label at src/layers/uav/index.js:91-92 -> suffix "HAT" instead of bare metres.
A DEM/ground-elevation lookup should stay where the plan already puts it (PLAN.md:202, Phase 6 / M19) and should not be counted against the current goal.

Separately, and NOT what this gap describes: `SafetyEnvelope.check_point` is only ever reached from `check_route` (safety.py:159), which is called once at pre-flight (server.py:302). No live telemetry tick ever evaluates ceiling or min-AGL — grep for `check_point` across mcp/ finds only safety.py:145,159 and tests. That is a genuine in-flight safety-enforcement gap and is materially larger than the datum-naming issue, but it belongs in the safety dimension, not geo-datum.

### [tasking-persistence-auth] Register the §4.8 uav:// MCP resources over the data that already exists
- plan: `PLAN §4.8 Resources & persistence` | state: **missing** | verdict: OVERSTATED | effort: M | blocks_goal: True
- exists: Zero `@mcp.resource` registrations in server.py (grep for 'resource' in mcp/ hits only `resource_server_url` at server.py:219). The SDK supports it (.venv/.../mcp/server/mcpserver/server.py:779 `def resource`). Backing data by URI: uav://{vehicle}/telemetry -> FULLY BACKED (UavBackend.telemetry server.py:99-119 + fuel/bingo at server.py:358-365, also bridge.py:258-273). uav://tracks -> FULLY BACKED (TrackManager targets.py:77-127, tool at server.py:434-438). uav://safety/geofence -> data exists (safety.py:136-143), unexposed (see previous gap). uav://reports/{id} -> partially backed (intrep_summary targets.py:156-169) but unkeyable, no mission id. uav://{vehicle}/camera/{name}/{type} -> backed only in the bridge (AirSimAdapter.camera_png bridge.py:144-160, /camera/{veh} bridge.py:384-395); the MCP server has NO image tool at all (PLAN §4.2 uav_capture_image is absent from server.py:317-544). uav://targets -> UNBACKED: sim_spawn_target (server.py:440-448) keeps no registry, it only audit-logs. uav://mission/{id} -> UNBACKED (no mission id). uav://pattern-of-life/{poi} -> UNBACKED: Track.history breadcrumbs exist (targets.py:52-53,72-74) but there is no POI aggregation anywhere.
- required: Priority order for a harness running missions: (1) uav://safety/geofence — nothing else lets the skill plan legally; (2) uav://{vehicle}/telemetry — trivial, wraps an existing tool; (3) uav://tracks — trivial, wraps an existing tool; (4) uav://mission/{id} — needs the mission registry first; (5) uav://{vehicle}/camera/... — needs a capture tool on the MCP side, only the bridge has pixels today. uav://targets and uav://pattern-of-life/{poi} have no backing store and are the largest builds.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/targets.py
- scope correction: Do not scope this as "register the 8 §4.8 uav:// resources". Four of them (telemetry, tracks, reports, mission) are already readable by a tool-calling harness via uav_get_telemetry (server.py:358-365), uav_list_tracks (server.py:434-438), uav_target_report/uav_identify_target (server.py:416-432) and uav_task_status (server.py:538-544) — that is a protocol-shape deviation, not a capability gap, and it blocks no mission. The real remaining work is: (1) expose the geofence polygon + margin over MCP (safety.py:139,164-168 configured live at launch.py:75) — already tracked as a separate gap, do not double-count it here; (2) add an MCP-side uav_capture_image tool, since pixels exist only in the bridge (bridge.py:144-160, 384-395); (3) optional, non-blocking: a pattern-of-life accessor over the breadcrumbs already collected at targets.py:52-53,72-74, and a ground-truth target registry behind sim_spawn_target (server.py:443-448). Mission-keying is ~5 lines, not a build: store.log_mission/mission_events already exist and are tested (store.py:70-80, tests/test_store.py:36-42) with zero production callers. If MCP resources are wanted for discoverability, they are thin wrappers over those existing tools and should be filed as polish, not as the blocking gap.

### [tasking-persistence-auth] Implement restart replay -> resume-or-abort-and-RTH
- plan: `PLAN §4.8 (T4c); Phase 2 exit criterion 'restart-recovery test resumes/aborts correctly'` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: False
- exists: The append-only half is real: `JsonlJournal` opens with mode 'a' and flushes per record (store.py:21-34), `Store` wires four journals (store.py:56-65), and `read_all` tolerates a corrupt tail (store.py:36-47). The recovery half does not exist. grep for replay|recover|resume across mcp/ and tests/ returns zero hits. `launch.py:76` does `store = Store(args.db)` and immediately constructs the server (launch.py:77) with no state reconstruction. `TaskingService.__init__` (tasking.py:222-226) starts from an empty `_queues` dict every time.
- required: PLAN §4.8: 'restart = replay -> resume-or-abort-and-RTH'. Today a crash mid-mission leaves the drone airborne under the sim's last command with the server having no memory that a task was ever in flight — on restart it reports the vehicle idle. The Phase-2 exit criterion names a restart-recovery test; no such test exists in tests/ (13 files, none touching recovery).
- files: godseye/mcp/godseye_uav/store.py, godseye/mcp/godseye_uav/tasking.py, godseye/mcp/godseye_uav/launch.py, godseye/tests/test_store.py
- scope correction: Not overstated — if anything understated; the remaining work is LARGER than the prior auditor described, for two reasons found in code. (1) The journal is not replay-sufficient: every write goes through `self.store.log_task(task.handle(), ...)` (server.py:237, 281, 284, 287, 311) and `Task.handle()` (tasking.py:52-61) emits only task_id/tool/vehicle/state/progress_pct/submitted_at/error — it deliberately omits `Task.params`, so tasks.jsonl never records the waypoints, lat/lon, alt_m or speed_mps of an interrupted uav_fly_route/uav_goto_gps. Resuming is impossible from the current record shape; T4c must first widen the task record (or log params on "submitted") before any replay can work. (2) missions.jsonl is dead in production: `log_mission` (store.py:70) has no caller anywhere in mcp/ — `uav_mission` (server.py:372-399) submits via `self._submit(...)` and never logs a mission lifecycle event, so the mission-level context a resume-or-abort decision needs is not persisted at all. Full remaining work: widen the task journal to carry params, emit mission lifecycle events from uav_mission, add a startup replay that reconstructs per-vehicle in-flight state into TaskingService before serve(), add the resume-or-abort-and-RTH decision (reconciled against live telemetry, since the vehicle may have moved while the server was down), and add the Phase-2 restart-recovery test.

### [tasking-persistence-auth] Write the mission and fuel journals that T4c says to replay
- plan: `PLAN §4.8 'append-only JSONL mission log (commands, task transitions, fuel integrals, LOAL)'; §4.5 fuel integrator (T5)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: Only two of the four journals are ever written in production. `log_task` is called at server.py:237, 281, 284, 287, 311 and `log_audit` at server.py:285, 306, 355, 409, 425, 447, 464, 468, 487, 535 — good. But `Store.log_mission` (store.py:70-71) and `Store.log_fuel` (store.py:76-77) have ZERO production callers (grep across mcp/ finds them only at their own definitions and in tests/test_store.py:36-40). Root cause for fuel: `FuelModel.tick` (safety.py:198-206) is never called outside tests/test_safety.py:76-84 — so `fuel_pct` never moves off 100.0 (safety.py:176) and there is nothing to journal. `missions.jsonl` is empty because no mission id is minted (see mission-registry gap).
- required: PLAN §4.8 names 'fuel integrals' and the mission log as the two things replay depends on; both files are created (store.py:63,65) and stay empty forever, so even a correct replay implementation would recover nothing about missions or fuel. Note the knock-on: because tick() never runs, the BINGO force-RTB transition promised at safety.py:12 and PLAN §4.5 can never fire.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/safety.py, godseye/mcp/godseye_uav/store.py

### [tasking-persistence-auth] Accept and honor idempotency_key on the state-mutating non-queue tools
- plan: `PLAN §4 intro (T4b): 'All tools accept idempotency_key'` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: Seven queue-backed tools take it and it is genuinely deduplicated (tasking.py:118-122, proven by tests/test_tasking.py:43-52): uav_takeoff (server.py:318), uav_land (322), uav_return_to_home (326), uav_goto_gps (330), uav_fly_route (339), uav_hover (348), uav_mission (380). Twelve do not, including four that mutate state: uav_abort (server.py:352), sim_spawn_target (server.py:443), uav_handoff_target (server.py:477), sim_set_environment (server.py:509).
- required: T4b says all tools. The read-only ones (uav_get_telemetry, uav_list_tracks, uav_task_status, ...) are defensible omissions, but a retried `sim_spawn_target` duplicates an object in the world and corrupts the detection ground truth that TrackManager correlates on (targets.py:96-120), and a retried `uav_handoff_target` queues a second standoff orbit on the receiving vehicle (server.py:500-502). On a flaky HTTP connection the harness has no safe retry for either.
- files: godseye/mcp/godseye_uav/server.py
- scope correction: One tool, roughly a two-line change — not four tools and not a new dedup table.

Required: add `idempotency_key: str | None = None` to the `uav_handoff_target` signature (godseye/mcp/godseye_uav/server.py:477-478) and pass it through instead of the hard-coded `None` at server.py:500-502. The dedup machinery it needs already exists and is already exercised by tests (tasking.py:118-122; tests/test_tasking.py:43-52), so no new storage, table, or middleware is needed. Worth pairing with one regression test asserting that two handoffs sharing a key return the same `task_id`, since no test currently covers `uav_handoff_target`'s queueing side effect (tests/test_threat.py:78-91 only exercises the pure `plan_handoff` function).

NOT required (drop from the gap): sim_spawn_target, sim_set_environment, and uav_abort. All three are idempotent under retry as written — proven empirically above — so adding a key to them buys nothing but signature noise. If the literal wording of PLAN.md:119 ("All tools accept idempotency_key") is to be honored as a uniform API surface rather than as a safety property, that is a cosmetic consistency task and should be scoped and prioritized as such, not as a harness-blocking retry hazard. The four read-only tools the prior auditor already conceded (uav_get_telemetry, uav_list_tracks, uav_task_status, uav_list_vehicles, uav_target_report, uav_identify_target, uav_assess_threat) stay out.

Impact on the stated goal is correspondingly narrower: the entire flight-control surface an agentic harness drives for a single-vehicle mission (takeoff, land, RTH, goto, route, hover, mission) is already retry-safe. The residual exposure is a retried multi-UAV custody handoff double-queuing an orbit on the receiving vehicle.

### [tasking-persistence-auth] Persist the track store and pattern-of-life history
- plan: `PLAN §4.8 'Track store + pattern-of-life persist across sim_reset (M12)'` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: False
- exists: `TrackManager` keeps everything in `self._tracks: dict[str, Track]` (targets.py:82) with a process-local counter `self._seq` (targets.py:83), and `Track.history` breadcrumbs are capped at 200 in-memory entries (targets.py:72-74). server.py:213 constructs `TrackManager()` with no store handle. Nothing serializes or reloads it — grep for log_mission/log_audit shows tracks reach the audit journal only as one flat line per sighting (server.py:409-411), which is not a reconstructable store.
- required: PLAN §4.8 requires the track store and pattern-of-life DB to survive sim_reset, and by implication a server restart, since M12 pattern-of-life deviation analysis is defined against accumulated history. Today track ids restart at TRK-001 on every launch and all prior pattern-of-life evidence is lost, which also leaves uav://pattern-of-life/{poi} permanently unbackable.
- files: godseye/mcp/godseye_uav/targets.py, godseye/mcp/godseye_uav/store.py, godseye/mcp/godseye_uav/server.py

### [intel-tracks-threat] Order-of-battle library has no capabilities, weapon-range breakdown, or mobility
- plan: `PLAN.md:173 §4.6(a) / M13(a)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: godseye/mcp/godseye_uav/threat.py:20-30 — `_OOB` = 9 coarse categories (sam, radar, armor, tel, vehicle, personnel, naval, aircraft, unknown), each carrying exactly two scalars: {"weight": float, "envelope_m": float}. No data file backs it (mcp/godseye_uav/data/ contains only us_nga_egm96_15.tif).
- required: PLAN §4.6(a): 'match each track against order-of-battle library (type -> capabilities, weapon ranges, mobility)'. Required per entry: capability list (what it can do/detect/shoot), weapon range(s) distinct from sensor/acquisition range, mobility class (fixed / towed / tracked / wheeled / self-propelled with a speed band), and platform-level rows (SA-6 vs SA-15 vs SA-22 are all just 'sam' today, so every SAM in every theater yields identical envelope 15000 m and weight 1.0). This single number also drives M5 standoff derivation at threat.py:149, so standoff realism is capped by the table's fidelity.
- files: godseye/mcp/godseye_uav/threat.py, godseye/mcp/godseye_uav/data/

### [intel-tracks-threat] Evidence is a prose string, not cited per element
- plan: `PLAN.md:173 §4.6(c)-(d) / M13` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: godseye/mcp/godseye_uav/threat.py:89 returns `rationale` built by `_rationale` (threat.py:95-105), which string-joins phrases like 'sam (oob 1.00); observer INSIDE sam envelope; closing on asset (intent 0.42)'. It cites no observation: not sightings count, not first_seen/last_seen, not which detection frame or audit record produced the fix. threat.py never reads track.history, track.sightings, first_seen or last_seen (grep for those names in threat.py returns nothing).
- required: PLAN §4.6(c)-(d): each element of the assessment must carry cited evidence and each score component must be traceable. Needs a structured `evidence: [{element, value, source}]` list referencing concrete observations (track sighting timestamps, detection names, observer geometry, POL baseline record) rather than a human-readable sentence the harness must re-parse.
- files: godseye/mcp/godseye_uav/threat.py
- scope correction: Gap is real and, if anything, slightly understated rather than overstated. The prior auditor framed it as evidence-citation only; clause (c)'s confidence tier (confirmed/probable/possible) is not merely unstructured, it is entirely absent from the codebase (no `confidence` key in threat.py:81-92, no tier constant anywhere). Remaining work: (1) add structured `evidence: [{element, value, source}]` to the assess_track return citing concrete observations available on Track (targets.py:47-53: sightings count, first_seen/last_seen, history breadcrumbs) plus the OOB library row used (threat.py:20-30); (2) add a `confidence` tier derived from sightings/last_seen age; (3) surface `capability_weight` and `envelope_factor` as numeric fields so all three score terms in threat.py:78 are machine-traceable without parsing `rationale`; (4) update tests/test_threat.py:37 which currently asserts on the prose string.

### [intel-tracks-threat] Score is an additive weighted sum, not capability x intent, and its components are not all returned
- plan: `PLAN.md:173 §4.6(d) / M13(d)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: godseye/mcp/godseye_uav/threat.py:78 — `score = min(1.0, 0.5 * base + 0.3 * envelope_factor + 0.2 * intent)`. The output dict (threat.py:81-92) returns `intent` but never `base`/capability or `envelope_factor`, so two of the three terms cannot be recomputed by the caller.
- required: PLAN §4.6(d): 'score = capability x intent, each component traceable'. Verified behaviour of the current formula: a stationary SAM co-located with the observer and intent exactly 0.0 scores 0.8 -> threat_level 'critical' (run against threat.py, category 'sam', speed_mps None). Under the plan's multiplicative rule zero intent must collapse the score. Intent can move the result by at most 0.2 and can never gate it, which is the exact doctrinal error M13 was written to prevent.
- files: godseye/mcp/godseye_uav/threat.py
- scope correction: Gap stands; two precision refinements to the prior auditor's wording, neither reducing the work. (1) `base` is not "never returned" — threat.py:96 embeds it in the free-text `rationale` as "sam (oob 1.00)". It is still not a structured field, and prose-parsing is not traceability for an agentic harness, so the substance holds. (2) `envelope_factor` is worse than claimed, not better: threat.py:97-100 omits the number entirely in the `in_env` branch, so the term is unrecoverable exactly when it drives the score hardest. Remaining work is unchanged: replace threat.py:78 with a multiplicative capability x intent form so zero intent collapses the score, and add `capability` and `envelope_factor` (or whatever the final component decomposition is) as structured keys in the dict at threat.py:81-92 alongside the existing `intent`, plus a test pinning "zero intent => non-critical" that test_threat.py:46-54 currently does not provide.

### [intel-tracks-threat] Only 1 of 4 intent indicators implemented; pattern-of-life and emissions never consulted
- plan: `PLAN.md:173 §4.6(b) / M13(b)` | state: **partial** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: godseye/mcp/godseye_uav/threat.py:60-76 implements movement-toward-asset only (bearing-to-asset vs track heading, >0.5 m/s, cone < 30 deg). No posture evaluation (emplaced vs march vs dispersed vs radar-up), no pattern-of-life deviation, no emissions. threat.py contains no reference to track.history, and `assess_track`/`assess_area` take no pattern-of-life argument (threat.py:41-42, 116-117).
- required: PLAN §4.6(b): 'intent indicators (posture, movement toward asset, pattern-of-life deviation from M12 store, emissions if modeled)'. Three of four indicators are absent, and the one wired input the plan names explicitly — the M12 store — does not exist to be wired.
- files: godseye/mcp/godseye_uav/threat.py, godseye/mcp/godseye_uav/targets.py

### [intel-tracks-threat] sim_reset tool does not exist, so the M12 'survives sim_reset' guarantee is unimplementable and untested
- plan: `PLAN.md:162 §4.4 / PLAN.md:180` | state: **missing** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: nothing. Repo-wide grep for 'sim_reset' matches only PLAN.md:162, PLAN_DRAFT.md:157 and REVIEW_MILITARY.md:42 — no Python file defines or registers it. The sim-admin tools actually registered are `sim_spawn_target` (godseye/mcp/godseye_uav/server.py:440-448) and `sim_set_environment` (server.py:506-536).
- required: PLAN §4.4 row: `sim_reset` -> ok, 'does NOT wipe track store / pattern-of-life DB (M12)'. Because the tool is absent, a harness cannot reset a scenario between missions at all, and the one behavioural invariant the plan names for M12 (state survives reset) has no code path and no test to exercise it.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py, godseye/tests/test_targets.py
- scope correction: Not overstated — if anything understated. Scope of real remaining work is LARGER than the finding says: (a) add a `sim_reset` MCP tool in server.py:_register_tools that drives adapter/fake_airsim's existing `reset` handler (fake_airsim.py:269), which is currently dead code; (b) make the track store durable — add a tracks/pattern-of-life journal to Store (store.py:57-63) and wire TrackManager (targets.py:77-82, server.py:213) to load/append through it, since today both the track dict and the Track.history breadcrumbs (targets.py:52-53) are in-memory only; (c) add the missing M12 test asserting tracks + pattern-of-life survive sim_reset. Note: PLAN §4.4 also lists sim_move_target, sim_set_time, sim_set_link_state and sim_set_gps_degradation, which are likewise absent from the 19 registered tools (sim_set_environment at server.py:506 partially subsumes weather/wind only).

### [intel-tracks-threat] Reports are not mission-scoped — no mission_id exists anywhere in the running system
- plan: `PLAN.md:176 §4.7 (`uav://reports/{mission_id}`)` | state: **missing** | verdict: OVERSTATED | effort: M | blocks_goal: True
- exists: `Store.log_mission(mission_id, ...)` is defined at godseye/mcp/godseye_uav/store.py:70-71 and `mission_events(mission_id)` at store.py:79-80, but grep shows neither is called by any non-test code — missions.jsonl is written only by tests. `uav_mission` returns a task handle with kind/doctrine/waypoint_count/bingo_fuel_pct and no mission id (server.py:390-399). `intrep_summary` takes `since: float` as its only scoping (targets.py:156), so a harness must guess a unix timestamp window to approximate 'this mission'.
- required: PLAN §4.7 keys the report artifact by mission_id (`uav://reports/{mission_id}`), and PLAN §4.3 has every mission primitive return a `mission_handle`. Without a mission identity the harness cannot request the report for the mission it just flew, cannot correlate tracks/LOAL/coverage to a mission, and the JSONL mission journal stays empty.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/targets.py, godseye/mcp/godseye_uav/store.py
- scope correction: Not "build mission identity from scratch" — the id, the journal, the query API and the report generators all already exist and are tested. The actual remaining work is threading the existing `task_id` (tasking.py:42/52-61) through three call sites:
1. Stamp it on tracks: pass the active task_id into `TrackManager.ingest` / `Track` (targets.py:96-120) or keep a mission→track_id set, so tracks correlate to the mission that observed them.
2. Add a `mission_id` (i.e. task_id) parameter to `uav_target_report` (server.py:428-432) and a matching filter in `intrep_summary` (targets.py:156-158) alongside the existing `since`.
3. Call the already-implemented `store.log_mission(...)` at the three points that currently only call `log_task`: mission submit (server.py:390-399), and completion/failure in `_execute` (server.py:284-287), so missions.jsonl stops being empty.
Optionally (cosmetic, for harness clarity): echo the same value as `mission_id` in the `uav_mission` return dict next to `task_id` (server.py:393-399), and populate `VehicleSnapshot.mission` at bridge.py:123-138 so the GEV HUD shows the active mission. The `uav://reports/{mission_id}` MCP resource is a separate, genuinely missing item (no resource registry exists at all) — but the tool-level `uav_target_report(mission_id=...)` fix above already unblocks the harness goal without it.

### [intel-tracks-threat] OB classification collides on real-world names: Motorcycle/Tractor -> sam, Satellite/Hotel -> tel
- plan: `PLAN.md:173 §4.6(a) (OB match feeds the score) / M13(a)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: godseye/mcp/godseye_uav/targets.py:18-35 — `_CATEGORY_KEYWORDS` is an unanchored substring table including "tor": "sam" and "tel": "tel", matched by `if kw in n` (targets.py:32-34). Verified by running classify(): 'Motorcycle' -> sam, 'Tractor_01' -> sam, 'Satellite_dish' -> tel, 'Hotel_Building' -> tel; conversely 'BM-21_Grad' -> unknown, 'Pickup' -> unknown, 'Civilian_Bus' -> unknown, 'School_Bus' -> unknown. Categories flow straight into `_OOB` lookup at threat.py:48, so a motorcycle is scored with the SAM weight 1.0 / 15 km envelope.
- required: PLAN §4.6(a) requires a sound type match, and the user goal is missions in a real-world-data environment where `sim_spawn_target` accepts arbitrary asset names (server.py:440-448). Needs token/word-boundary or explicit-designator matching plus an OB keyword set with artillery/air-defence/C2 granularity, and an explicit 'unclassified' path that does not silently inherit a weapons envelope.
- files: godseye/mcp/godseye_uav/targets.py, godseye/tests/test_targets.py

### [gev-ui] Admit UAV drones and target tracks into the Contacts roster / SALUTE side panel
- plan: `PLAN.md §3 L79 ('Contacts roster' free win) + §3.1 feed table L98 ('/snapshot.contacts[] (new field)') + acceptance 3 ('SALUTE card in side panel') and 4 ('contact card shows TRACKING: UAV-2')` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: The awareness (Contacts) layer is constructed without the uav layer: gods-eye-view/src/app/constructCatalog.js:128-133 passes only `{flights, military, vessels, installations}`, and the engine hard-whitelists cohort ids `['flights','military','ais-live-vessels','military-installations']` at gods-eye-view/src/data/militaryAwarenessEngine.js:129-138 — 'uav' can never appear. gods-eye-view/src/app/layers/uav.js is 6 lines and registers no world-overlay paint lane, unlike every roster-bearing layer (e.g. src/app/layers/cctv.js, src/app/layers/aisLiveVessels.js). Target tracks render as bare orange points (gods-eye-view/src/layers/uav/index.js:97-129) with no card. Grep for 'SALUTE' in GEV src matches one string literal, uavMissionPanel.js:89.
- required: Pass the uav layer into createApplicationAwareness, widen the engine whitelist to 'uav', add a bridge `/snapshot.contacts[]` (persistent track ID, class, confidence, last SALUTE) and render SALUTE cards plus a 'TRACKING: <vehicle>' custody card in the side panel so acceptance items 3 and 4 have somewhere to land.
- files: gods-eye-view/src/app/constructCatalog.js, gods-eye-view/src/data/militaryAwarenessEngine.js, gods-eye-view/src/app/layers/uav.js, godseye/mcp/godseye_uav/bridge.py
- scope correction: The gap is real and blocking, but the prior auditor's `what_is_required` is wrong in BOTH directions — correct it before filing work.

OVERSTATED — the contacts data feed already exists, renamed. Do NOT build "/snapshot.contacts[]". It is PRESENT-RENAMED as `/tracks` + `snapshot.targets[]`, end to end:
- godseye/mcp/godseye_uav/bridge.py:370-382 — `GET /tracks` proxies the MCP `uav_list_tracks` tool and unwraps the envelope.
- godseye/mcp/godseye_uav/server.py:434-438 — `uav_list_tracks` returns `[salute_report(t) for t in self.tracks.tracks()]`.
- godseye/mcp/godseye_uav/targets.py:129-145 — `salute_report()` emits persistent track_id plus full Size/Activity/Location/Unit(=class)/Time/Equipment, observer, sightings, speed, heading.
- gods-eye-view/src/sources/live/uav.js:107-122 — the GEV source already fetches /tracks each poll and returns it as `targets[]` (best-effort, never breaks the vehicle snapshot).
- gods-eye-view/src/layers/uav/index.js:97-129, 179 — already renders one numbered marker per track, labelled `track_id unit activity`.
The only field genuinely absent from that feed is `confidence` (no "confidence" token anywhere in mcp/godseye_uav/*.py). A harness reading the plan literally WILL be confused by the name mismatch (`contacts[]` vs `targets[]`), so record it as a naming deviation to reconcile, not as data to build.

UNDERSTATED — "widen the engine whitelist to 'uav'" is not sufficient. militaryAwarenessEngine.js:130-138 is only the NAVIGATION whitelist; the roster itself comes from a hardcoded literal (src/layers/awareness/subject.js:61-101) plus hardcoded DEPENDENCIES and SOURCE_LABEL (src/layers/awareness/policy.js:1-11, 52-57).

Actual remaining work:
1. Give the uav layer the cohort-source interface it lacks: `getNearby(center, radiusM, limit, {includeHidden})`, plus `selectById`/`focusById` for click-through (model on src/layers/military/queries.js:326). Serve both vehicles (entities map) and target tracks (targetEntities map) from it.
2. Add a 5th cohort entry `{id:'uav', label:'UAV / tracks'}` in src/layers/awareness/subject.js:61-101, add 'uav' to DEPENDENCIES + SOURCE_LABEL in policy.js, and add 'uav' to the navigation whitelist in militaryAwarenessEngine.js:130-138.
3. Pass the uav layer through: add `uav` to the destructure in src/app/layers/militaryAwareness.js:6-11 and to the call site at src/app/constructCatalog.js:128-133.
4. Render the SALUTE card: the data is already on `snap.targets[]`; extend `rowHtml`/the card renderer in src/layers/awareness/panel.js to show the SALUTE fields for uav-cohort rows, and add the "TRACKING: <vehicle>" custody line (source it from the existing per-vehicle `status.trackId`, already normalized at src/sources/live/uav.js:92).
5. Add `registerPickOwner('uav', …)` (no src/layers/uav/lifecycle.js exists) so clicking a drone or track marker selects it as the awareness subject.
6. Optionally add `confidence` to Track/salute_report in mcp/godseye_uav/targets.py if the card must show it.

### [gev-ui] Replace per-poll ConstantPositionProperty with sampled properties + client interpolation
- plan: `PLAN.md §3 L81 ('Entity property writes capped at 2–5 Hz with client interpolation (T6)') + §3.1 acceptance 1 (L102 'drones flying on globe with 5 Hz smooth motion (T6 sampled properties + interpolation)'); REVIEW_TECHNICAL.md T6` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: Every poll tick overwrites the entity with a brand-new constant: gods-eye-view/src/layers/uav/index.js:74 `slot.entity.position = new Cesium.ConstantPositionProperty(cart)` (also at index.js:35 on create and index.js:126 for targets). There is no SampledPositionProperty, no interpolation, no per-frame animator — compare the reference implementation's bracketing dead-reckoner at gods-eye-view/src/layers/military/motion.js:56-127. The layer also never takes a render-governor hold (military does: gods-eye-view/src/layers/military/lifecycle.js:118 `holdContinuousRender('military')`), so with the scene in requestRenderMode (gods-eye-view/src/renderGovernor.js:46-47) an untracked drone repaints only on the layer's own `requestRender` (index.js:180) — 5 discrete frames per second.
- required: A rendering/motion module using Cesium.SampledPositionProperty (or an equivalent per-frame interpolator) rendered one poll interval behind, plus holdContinuousRender('uav') while the layer is enabled, so motion is continuous rather than a 5 Hz stutter. As written, acceptance item 1's 'smooth motion' is not met even when the bridge is at full rate.
- files: gods-eye-view/src/layers/uav/rendering.js, gods-eye-view/src/layers/uav/motion.js, gods-eye-view/src/layers/uav/lifecycle.js, gods-eye-view/src/layers/uav/index.js

### [gev-ui] Swap the 12px point for the mq9.glb model when zoomed / tracked
- plan: `PLAN.md §3 L75 ("rendering.js (billboards → 3D GLB on zoom; MQ-9 model already in 3D hangar; IR boost via Cesium.CustomShader)")` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: False
- exists: The layer renders a fixed 12px cyan point plus a text label and nothing else: gods-eye-view/src/layers/uav/index.js:36-52. No billboard, no Cesium.Model, no CustomShader. The asset the plan names is present and already mapped for the 'uav' aircraft class — gods-eye-view/public/models/mq9.glb, referenced at gods-eye-view/src/data/aircraftClass.js:501 `uav: { url: '/models/mq9.glb', bellyM: 2.02, radiusM: 12.0 }` with its visual anchor at src/data/modelVisualAnchor.js:68 — so the model is wired for the military layer and simply unused by the UAV layer. The reference distance gate is src/layers/military/policy.js:80 TRACKED_MODEL_MAX_PX.
- required: Load mq9.glb into a PrimitiveCollection and swap point→model on the same pixel-size regime the military layer uses, orienting by the drone's heading/pitch/roll (already carried in the record at src/sources/live/uav.js:83-86). Without it the operator watching a 'realistic drone simulation' sees a dot.
- files: gods-eye-view/src/layers/uav/rendering.js, gods-eye-view/src/layers/uav/policy.js, gods-eye-view/src/layers/uav/index.js

### [gev-ui] Fix cockpit exit leaving the drone invisible and untracked
- plan: `PLAN.md §3.1 acceptance 2 (click drone → cockpit) and the DoD's 'operator watches entire mission live' (L113)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: Cockpit entry hides the subject entity — gods-eye-view/src/ui/cockpitTrackingController.js:151-153 `entity.show = false` — and exit restores it only under `this.viewer.entities.contains(entity)` (cockpitTrackingController.js:244-245, and the tracking restore at 248-251). UAV entities are NOT in viewer.entities: they are added to a separate CustomDataSource at gods-eye-view/src/layers/uav/index.js:152-153 and index.js:33. The military and flights layers add their tracked entity to the viewer collection (src/layers/military/tracking.js:902, src/layers/flights/tracking.js:937), which is why the guard works for them. Consequence: exiting the cockpit on a drone leaves it permanently hidden (the poll loop at index.js:74 only rewrites position, never `show`) and never reaches the uav branch of restoreTrackingFrame at gods-eye-view/src/ui/cockpitCoordinator.js:108-111.
- required: Either add the tracked UAV entity to viewer.entities like the sibling layers, or widen the contains/restore guard to search data-source collections. Today an operator who enters and leaves the cockpit once loses the drone from the globe for the rest of the session.
- files: gods-eye-view/src/ui/cockpitTrackingController.js, gods-eye-view/src/layers/uav/index.js

### [gev-ui] Unwrap the MCP envelope on /control/status so the fuel/BINGO readout works
- plan: `PLAN.md §3.1 acceptance 2 (HUD fuel %, ETA-to-BINGO) — the only shipped fuel/BINGO surface` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: godseye/mcp/godseye_uav/bridge.py:366-368 returns `_mcp_call("uav_get_telemetry", …)` raw — the JSON-RPC envelope — while the sibling /tracks route deliberately unwraps `result.content[0].text` (bridge.py:370-382) and `_unwrap` exists for exactly this at bridge.py:320-330. The browser then does `const t = out?.result ?? out; if (t && t.lat != null)` at gods-eye-view/src/ui/uavMissionPanel.js:544-545; `out.result` is the FastMCP `{content:[…], structuredContent:{…}}` wrapper (tool returns a flat dict at godseye/mcp/godseye_uav/server.py:359-365), so `t.lat` is always undefined and the panel silently keeps showing 'telemetry: —'. Fuel % and bingo_fuel_pct never render anywhere in the product.
- required: Apply `_unwrap()` in the /control/status route (one-line change, matching /tracks), or have the panel parse result.content[0].text. Until then every fuel/BINGO figure the safety envelope computes is invisible to the operator.
- files: godseye/mcp/godseye_uav/bridge.py, gods-eye-view/src/ui/uavMissionPanel.js

### [gev-ui] Fix the tactical-overlay tick reading a snapshot shape the bridge never emits
- plan: `PLAN.md §3.1 acceptance 3 (live route/track overlays on the globe)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: gods-eye-view/src/ui/uavMissionPanel.js:476-481 fetches the bridge's /snapshot directly and reads `snap?.records` then `rec.position` / `rec.velocity`. The bridge emits `{sim_state, observedAtMs, count, vehicles:[…]}` with flat snake_case fields — `vehicles`, not `records`, and `latitude`/`longitude`/`speed_ms` rather than a nested `position`/`velocity` (godseye/mcp/godseye_uav/bridge.py:258-266, dataclass at bridge.py:29-48). The nested shape only exists AFTER normalization in gods-eye-view/src/sources/live/uav.js:58-96, which this panel bypasses. `(snap?.records || [])[0]` is therefore always undefined and overlayTick returns at line 478, so both shipped operator toggles — 'Path-ahead terrain sim' and 'Ground track + data capture' (uavMissionPanel.js:233-242) — draw nothing when ticked.
- required: Either route the panel through createUavSource().getSnapshot() (the normalizer that already produces this shape) or read `vehicles[]` with the bridge's flat field names. Two of the three tactical overlays the UI advertises are currently dead controls.
- files: gods-eye-view/src/ui/uavMissionPanel.js

### [skill-harness-demo] No test drives the MCP server over Streamable HTTP with Bearer auth
- plan: `PLAN.md:205 (T8 UE-free CI: bridge/MCP contract tests), PLAN.md:198 (Phase 2 exit: harness flies GPS route via MCP)` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: tests/test_server.py:22-39 builds GodseyeUavServer in-process and every test calls tool functions directly; tests/test_server.py:47-50 tests StaticBearerVerifier as a bare object, not over the wire. `grep -rn 'streamable|/mcp|ClientSession|httpx|TestClient' tests/*.py` matches only tests/test_bridge.py:7,31 (FastAPI TestClient against the *bridge*, not the MCP server). server.py:546-547 `serve()` / run_streamable_http_async is never executed by any test. So the exact path a harness uses — POST /mcp, Authorization: Bearer, initialize / tools/list / tools/call, stateless_http=True — has zero coverage, including the 401 path and the hardcoded auth metadata at server.py:216-221.
- required: An end-to-end test that starts the server on an ephemeral port, connects with an MCP streamable-HTTP client, asserts tools/list returns the 19 registered tools (server.py:317-539), asserts a wrong/absent Bearer is rejected, and flies takeoff -> uav_mission -> uav_task_status -> uav_return_to_home over the protocol against the fake sim. This is the one test that would prove the user's goal (harness + skill fly a mission) is achievable.
- files: godseye/tests/test_mcp_transport.py, godseye/scripts/ci.sh, godseye/.github/workflows/ci.yml
- scope correction: Gap stands as written, with one addition: the required work should also cover the bridge->MCP proxy path (bridge.py:292-317 and routes bridge.py:358-366), which is likewise untested and is the path the browser C2 UI uses to command the sim. Required: an end-to-end test binding GodseyeUavServer.serve() on an ephemeral port, connecting via mcp.client.streamable_http, asserting tools/list returns the 19 tools (server.py:317-539), asserting a wrong/absent Bearer yields 401 (exercising server.py:214-221), and flying uav_takeoff -> uav_mission -> uav_task_status -> uav_return_to_home over the protocol against FakeAirSim. Note that server.py:217-221 hardcodes port 8791 in AuthSettings, so an ephemeral-port test will likely expose that as a real defect rather than just a coverage gap.

### [skill-harness-demo] The zero-GPU fake-sim path is never exercised in CI (airsim client is an uncommitted sibling repo)
- plan: `PLAN.md:219 (§8.1: fake-AirSim mode --sim=fake -> entire MCP+GEV stack runs with zero GPU for CI, skill dev, doctrine iteration), PLAN.md:205 (T8)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: The fake backend itself is real and substantial (mcp/godseye_uav/fake_airsim.py, 598 lines) and start.sh:19,51-53 / launch.py:55,65-68 select it by default. But .github/workflows/ci.yml runs ONLY `pytest tests/test_geo.py tests/test_geo_gate.py` — the two files that do not import airsim (verified: `grep -ln 'import airsim' tests/*.py` lists test_fake_airsim, test_missions, test_realism, test_targets, test_server; test_geo.py:1-20 and test_geo_gate.py:1-19 import only godseye_uav.geo). Those five fake-sim test files all need `import airsim` (e.g. tests/test_server.py:4), which resolves only through PYTHONPATH to a sibling checkout: ci.yml sets `PYTHONPATH="mcp:$PWD/../airsim/PythonClient"`, a path that does not exist on a GitHub runner, and `airsim` is not a dependency in pyproject.toml:9-16 nor pip-installed in the venv (`ls .venv/.../site-packages | grep -i airsim` -> nothing). scripts/ci.sh:5 hardcodes the absolute developer path `airsim/PythonClient`, so it only runs on this laptop. No GEV/npm job exists in ci.yml at all, so the 'MCP+GEV stack' half is untested too.
- required: Make the airsim python client resolvable in CI (vendor the minimal client, add a git dependency, or check out the sibling repo in the workflow), then run the full fake-sim suite in ci.yml — test_fake_airsim, test_server, test_missions, test_targets, test_threat, test_realism, test_tasking, test_safety, test_store, test_bridge — not just the two geo files. De-hardcode scripts/ci.sh:5 to a relative/env path. Add a headless GEV job (the uav layer already has src/layers/uav/uav.test.mjs and src/ui/uavMissionPanel.test.mjs). Until then, a green CI badge says nothing about whether the zero-GPU stack a skill developer depends on still works.
- files: godseye/.github/workflows/ci.yml, godseye/scripts/ci.sh, godseye/pyproject.toml
- scope correction: Not overstated — UNDERSTATED. Scope correction upward: (a) the fix must also remove the hardcoded absolute path from tests/test_fake_airsim.py:10 and tests/test_bridge.py:9, not only scripts/ci.sh:5; (b) ten test files must be enabled, because test_bridge.py and test_threat.py also fail via lazy imports at bridge.py:149 and server.py:516 — without airsim only 56/106 tests pass, not the ~85 the five-file framing implies; (c) most importantly, mcp/godseye_uav/launch.py:70 must move `import airsim` inside the `--real` branch (or route the fake path through an in-repo client shim), because today the advertised zero-GPU `--sim=fake` launcher cannot start at all without the uncommitted sibling — this is a code fix, not just a CI fix; (d) prerequisite to all of it: mcp/, tests/, .github/, scripts/, start.sh and PLAN.md are untracked and no git remote is configured, so the workflow must first actually be committed and pushed before any CI claim means anything.

### [skill-harness-demo] SAR expanding-square and BDA re-look patterns have no implementation
- plan: `PLAN.md:185 (skill patterns include SAR expanding square, BDA re-look), PLAN.md:145 (mission_grid_search pattern(lawnmower/expanding-square))` | state: **missing** | verdict: OVERSTATED | effort: M | blocks_goal: True
- exists: missions.py:143-177 `plan_mission` accepts exactly five kinds — recon_route (147), grid_search (153), orbit_poi/track_target (161), assess (170) — and raises ValueError on anything else (missions.py:177). `grep -rni 'expanding|square|sar_' mcp/godseye_uav/*.py` matches only geo.py:20 ('eccentricity squared'). `grep -rni 'bda|re-look|relook'` matches nothing relevant. grid_search has no `pattern` parameter (missions.py:153-160 hardwires lawnmower_waypoints).
- required: Add an expanding-square generator (outward spiral from a datum, leg length stepping by the footprint-derived spacing from missions.py:39-43) selectable as grid_search pattern='expanding_square' or a new kind, and a BDA re-look kind that re-flies a stored track's last known geo point at assess geometry. Two of the six doctrine patterns PLAN §5 names for the skill are otherwise undeliverable — a skill that documents them would be instructing the harness to call tools that raise ValueError.
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py, godseye/tests/test_missions.py
- scope correction: Half the gap is real; the other half is already implemented. Scope should be cut to one item, not two.

STILL NEEDED (real): an expanding-square generator in mcp/godseye_uav/missions.py — an outward box spiral from a datum whose leg length steps by grid_sweep_spacing_m (missions.py:39-43, already exists and is tested), exposed either as a grid_search pattern='expanding_square' branch at missions.py:153-160 or as a new kind before the raise at missions.py:177, plus the kind name added to the uav_mission description string at server.py:372-377. This is one function plus one branch reusing an existing, tested spacing primitive — not a new subsystem. It does belong server-side: letting the harness compute the spiral and call uav_fly_route (server.py:339, accepts arbitrary waypoints) would return lane spacing to the LLM, the exact anti-pattern M1 forbids (PLAN.md:145 "NOT lane_spacing — server derives spacing"; PLAN.md:234).

DROP FROM THE GAP (already works): the "BDA re-look kind" is not needed. uav_list_tracks -> location{lat,lon} -> uav_mission(kind='assess') performs precisely the described behavior today, gates and queues correctly (verified by execution). A godseye-uav skill can document this composition as-is and it will run. If a convenience wrapper is ever wanted it is cosmetic, not blocking.

SECONDARY (small, worth folding in): make plan_mission reject or honor unknown kwargs instead of silently swallowing them (missions.py:143) — today pattern='expanding_square' silently yields a lawnmower with no error, which will mislead a harness more than a clean rejection would.

NOTE ON BLOCKING STATUS: the real blocker for the skill-harness-demo dimension is that no godseye-uav skill directory exists at all (only .agents/skills/beads/SKILL.md). Of the six doctrine patterns in PLAN.md:185, five are deliverable against today's tools (grid_search, recon_route, orbit_poi, track_target+uav_handoff_target, assess-based BDA re-look); exactly one — SAR expanding square — lacks code.

### [skill-harness-demo] No sensor tools (EO/IR capture, FOV, gimbal, LOS) — EO-day/IR-night and altitude<->resolution doctrine has nothing to call
- plan: `PLAN.md:186 (doctrine: EO day / IR night; altitude<->resolution tradeoff), PLAN.md:136-139 (uav_capture_image, uav_los_check), PLAN.md:130-131 (uav_set_gimbal, uav_set_fov wide<->narrow cross-cue M7)` | state: **missing** | verdict: CONFIRMED | effort: L | blocks_goal: True
- exists: The MCP catalog is the 19 tools at server.py:317-539; none of uav_capture_image, uav_set_fov, uav_set_gimbal or uav_los_check is registered (verified by `grep -n 'mcp.tool(name='`). The only image path is the *bridge REST* one — bridge.py:144 camera_png and bridge.py:384-385 `/camera/{veh}?cam=&type=` — which is for the GEV PIP, not callable by an MCP client, and exposes ImageType as a bare int with no EO/IR semantics. uav_scan_targets (server.py:404-414) defaults image_type=0 (Scene/EO) with no way to request Infrared. server.py:417-418 explicitly punts cross-cue to 'uav_mission assess', and missions.py:170-176 'assess' only tightens the orbit radius — no FOV change occurs anywhere.
- required: Register MCP tools for capture (with type scene/infrared and returned geo pose), FOV set (the M7 wide->narrow cross-cue), gimbal pointing, and LOS check (AirSim simTestLineOfSightToPoint per PLAN.md:60). Add a `camera`/`image_type` parameter to uav_scan_targets so night missions can run IR. Without these, a skill can only write 'use IR at night' as prose with no corresponding call, and the altitude<->resolution tradeoff reduces to changing alt_m and hoping.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py

### [skill-harness-demo] Sun-glare avoidance (M6) is unimplementable: no sun-angle source, and the sun-side orbit bias is cosmetic
- plan: `PLAN.md:186 (sun-glare avoidance M6), PLAN.md:187 (dry-run includes sun), PLAN.md:128 (uav_orbit_poi sun_side rule), PLAN.md:158 (sim_set_time for sun-angle planning), PLAN.md:136 (capture returns sun angle)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: orbit_waypoints accepts sun_azimuth_deg (missions.py:108) and plan_mission forwards it (missions.py:166), but nothing in the system computes or supplies a solar position: `grep -rn sun mcp/godseye_uav/*.py` returns only those missions.py lines plus fake_airsim.py:515 (a simSetTimeOfDay stub signature). No sim_set_time tool is registered (server.py:317-539), uav_get_telemetry (server.py:358-365) returns no sun angle, and no solar-ephemeris helper exists. Separately, the implementation is ineffective even when given an azimuth: missions.py:115-117 uses it only as the ring's STARTING phase (`start = radians(sun_azimuth_deg)`), and missions.py:118-127 then walks a full 360 degrees at constant radius, so the aircraft visits every aspect including the glare aspect regardless.
- required: Add a solar-position computation (lat/lon/UTC -> azimuth/elevation) surfaced on telemetry and in the dry-run result, expose sim_set_time so the harness can set the celestial clock, and make the sun rule actually constrain geometry — bias the arc (e.g. a partial ring or a per-waypoint gimbal aspect) rather than only rotating the start point. Until then the skill cannot honor M6 and the dry-run's 'sun' check has no input.
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py

### [skill-harness-demo] Wind never reaches the fuel gate and is absent from telemetry (M15 doctrine unusable)
- plan: `PLAN.md:186 (wind in fuel math, M15), PLAN.md:137 (uav_get_telemetry returns wind), PLAN.md:159 (sim_set_weather wind vector feeds fuel model)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: The physics is written but unplumbed. safety.py:192-195 applies a headwind penalty and safety.py:208/242/249 all accept headwind_mps — but server.py:301 calls `fm.preflight_gate(waypoints, start, speed)` with no headwind argument, so it always defaults to 0.0 (safety.py:249), and server.py:363 calls bingo_fuel_pct with no headwind either. sim_set_environment (server.py:509-536) pushes wind into the sim via simSetWind (server.py:518-519) but never stores it on the server or the FuelModel — `grep -rn headwind mcp/` shows every hit is inside safety.py. uav_get_telemetry (server.py:358-365) returns no wind field at all, so the skill cannot even read wind to reason about it manually.
- required: Persist the wind vector set by sim_set_environment on the server, project it onto each planned leg to a headwind component, and pass it into preflight_gate / bingo_fuel_pct / tick. Add `wind` to the uav_get_telemetry payload per PLAN.md:137. Otherwise 'wind in fuel math' is a doctrine line the skill cannot act on and the gate systematically under-estimates fuel into a headwind.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/safety.py
- scope correction: Gap is real and the claimed scope is if anything UNDERSTATED, not overstated. Claim is accurate on persisting wind on the server, projecting it per-leg to a headwind component, passing it into preflight_gate (server.py:301) and bingo_fuel_pct (server.py:363), and adding `wind` to the uav_get_telemetry payload (server.py:358-365) per PLAN.md:137. One correction that ENLARGES the work: the claim's "pass it into tick" is not a one-line change, because FuelModel.tick has NO production call site at all -- `grep -rn "\.tick("` returns only tests/test_safety.py:76,77,84. safety.py:204 is the only writer of self.fuel_pct and is unreachable in production, so fm.fuel_pct is a constant 100.0 (safety.py:176) and server.py:362 always reports 100.0. Wiring wind into tick therefore first requires building the per-telemetry-tick fuel integrator loop specified at PLAN.md:167. Minimal fix: add a wind field to GodseyeServer.__init__ (server.py:206-214) set by sim_set_environment (server.py:520), add a helper projecting the NED wind onto each leg bearing to a headwind component, thread it through _gate (server.py:298-307) and the telemetry tool (server.py:363), and add `wind` to the telemetry dict. Live-tick wind is blocked on the separate missing integrator loop and should be filed as its own issue.

### [skill-harness-demo] Theater presets diverge across three sources — the server geofence is ~120 km from what the UI plots
- plan: `PLAN.md:222 (one-command demo: harness mission visible live in the command center), PLAN.md:113 (Phase 3 DoD)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: Three independent tables for the same theater keys. launch.py:35-36 'iran-isfahan' home (33.72, 51.72, 1550.0) with ao [(33.68,51.66)...(33.76,51.66)]; gods-eye-view/src/ui/uavMissionPanel.js 'iran-isfahan' home [32.6546, 51.668, 1570] with ao [[32.68,51.63]...]; scripts/demo_mission.py:31 'iran-isfahan' home (33.7220, 51.7250, 1580.0). The latitudes differ by ~1.07 deg (~119 km). launch.py:30-31 carries the comment 'single source of truth would be ideal; keep these in sync with src/ui/uavMissionPanel.js THEATERS' — they are not in sync. The server enforces its own table as the geofence (launch.py:75 SafetyEnvelope(geofence=ao)), so coordinates a harness or operator takes from the UI panel land outside the envelope and every plan is rejected at server.py:302-304.
- required: One source of truth for theaters (a JSON/py module the bridge serves and the panel fetches, or generate the JS from the Python). Also reconcile demo_mission.py:29-33 to consume it rather than redefining it. This directly breaks the demo the user's goal depends on: the mission the operator sets up in the command center is gated against a different box than the one drawn on the globe.
- files: godseye/mcp/godseye_uav/launch.py, godseye/scripts/demo_mission.py, gods-eye-view/src/ui/uavMissionPanel.js

### [realism-realdata] Feed wind into the fuel integrator and the pre-flight BINGO gate
- plan: `PLAN.md:202 Phase 6 / §4.5 T5+M4; REVIEW_MILITARY.md:55-56 (M15)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: safety.py:192-196 FuelModel._burn accepts headwind_mps and applies a clamped +50% cruise/climb penalty; safety.py:208,242,249 plumb the param through estimate_route/bingo_fuel_pct/preflight_gate; server.py:509-519 sim_set_environment pushes a wind vector to the sim; tests/test_safety.py:88-93 proves the penalty math in isolation.
- required: M15: 'Feed wind into the fuel/burn model ... include wind in the pre-flight BINGO check (M4)'. No server-side wind state exists (grep for wind in server.py returns only the sim_set_environment tool). Every caller passes the default headwind_mps=0: server.py:301 fm.preflight_gate(waypoints, start, speed) and server.py:363 fm.bingo_fuel_pct((lat,lon), alt_hae). Worse, FuelModel.tick() is never called in production code at all — the only call sites are tests/test_safety.py:76,77,84 — so live fuel never burns, wind or no wind, and fuel_pct stays 100.0 (safety.py:174) for the whole mission. sim_set_environment must persist wind on the server and every gate/BINGO/tick call must pass it.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/safety.py, godseye/tests/test_realism.py

### [realism-realdata] Make wind affect a commanded vehicle (crab, ground speed, leg time)
- plan: `PLAN.md:202 Phase 6 wind (M15); REVIEW_MILITARY.md:55 'Crosswind changes orbit hold, ground speed, lane fidelity'` | state: **partial** | verdict: OVERSTATED | effort: M | blocks_goal: True
- exists: fake_airsim.py:142-148 adds the wind vector to NED position each tick, but only when the vehicle is airborne+armed; fake_airsim.py:212-215 set_wind; tests/test_realism.py:49-59 asserts drift during a hover.
- required: The drift is erased on the same tick for any vehicle under command: fake_airsim.py:162-179 drives straight at the target with step = min(speed*dt, dist) and recomputes the unit vector from the post-drift position, so a commanded leg arrives at the same time with the same ground speed in a 15 m/s crosswind as in dead calm. v.vel (fake_airsim.py:179) is never wind-corrected, so _update_attitude (fake_airsim.py:182-209) shows no crab angle and the GEV heading readout (bridge.py:122) is wrong. tests/test_realism.py:61-68 only checks the landed case, so the suite passes while route execution is wind-blind. Wind must be applied as an airspeed-vs-groundspeed difference on moving legs.
- files: godseye/mcp/godseye_uav/fake_airsim.py, godseye/tests/test_realism.py
- scope correction: Do NOT rewrite the motion model — wind already affects crab, ground speed and leg time correctly (p_dot = airspeed*u_hat + W). The real remaining work is a reported-telemetry fix, roughly one line plus a test:

1. fake_airsim.py:169,179 — keep the air-relative vector for attitude (so the 41.9 deg crab heading at fake_airsim.py:194 survives) but report GROUND velocity in telemetry. Simplest form: add a v.ground_vel field set to (u*sp + wind) on moving legs and to (wind) when airborne with no active task, and publish that at fake_airsim.py:363 (linear_velocity) and fake_airsim.py:395 (gnss.velocity). This makes bridge.py:121 speed_ms show 21.68 m/s instead of 20.00 on a crosswind leg, and 15.00 instead of 0.00 in a wind-drifted hover.

2. Optionally add a distinct course/ground-track field alongside bridge.py:122 heading_deg, so GEV can show nose heading and ground track separately (heading itself is already correct — it is the crabbed nose heading).

3. tests/test_realism.py — add a moving-leg test asserting leg time under crosswind/headwind exceeds the dead-calm time and that reported speed_ms differs from commanded airspeed. This is the only place the existing correct behavior is genuinely unguarded.

### [realism-realdata] Make degraded GPS actually degrade navigation, geofence and reporting
- plan: `PLAN.md:161 sim_set_gps_degradation; REVIEW_MILITARY.md:58-59 (M16)` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: fake_airsim.py:217-220 set_gps_denied, fake_airsim.py:233-249 _reported_geo freezes the fix at the last good value and adds gaussian jitter; it is returned by getMultirotorState.gps_location (fake_airsim.py:358,368-372) and getGpsData (fake_airsim.py:382-400); tests/test_realism.py:71-110 assert on those two RPCs.
- required: M16: 'geofence checks on fused position with growth margin during degradation; log GPS quality in telemetry and INTREP.' No consumer reads the degraded fix. bridge.py:116-117 derives lat/lon from kinematics_estimated.position (NED truth) and server.py:104 does the same; the GPS fix survives only as a secondary display key at server.py:118. So the geofence (safety.py:148-149 via check_route safety.py:156-162), the BINGO gate, the track store and the GEV globe (src/layers/uav/index.js:26-30) all run on ground truth during a denial. Telemetry (server.py:358-365) carries no eph/fix_type/denied/quality field, SafetyEnvelope has no degradation margin (safety.py:135-168), and intrep_summary (targets.py:156-169) has no sensor/GPS-condition section. The Phase 6 exit criterion requires a GPS-denied window in which the system behaves differently; today nothing does.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/safety.py, godseye/mcp/godseye_uav/targets.py
- scope correction: Gap is real, not overstated. One added deviation worth folding into the fix: PLAN.md:161 specifies `sim_set_gps_degradation | vehicle, error_m / denial`, but the implementation (server.py:509-530 sim_set_environment -> fake_airsim.py:97) is GLOBAL with no vehicle scoping, which breaks Phase 6's multi-vehicle GPS-denied window. Also fake_airsim.py:387-398 must stop hardcoding eph=1.0/fix_type=3/satellites_visible=12 before any telemetry or INTREP quality field can carry real meaning.

### [realism-realdata] Stop sim_set_environment reporting success for realism it did not apply
- plan: `PLAN.md:160-161 sim_set_link_state / sim_set_gps_degradation; REVIEW_MILITARY.md:58,61 (M16/M17)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: server.py:506-536 sim_set_environment. Wind goes over RPC (server.py:517-519) and works on both backends; GPS denial, GPS noise and detection FP/FN are applied only through a direct handle to the fake (server.py:523-534, guarded by `sim = getattr(self.backend, "sim", None)`).
- required: launch.py:64-67 only constructs a FakeAirSim when --real is absent, and launch.py:74 passes that (None in real mode) as UavBackend(..., sim=sim) (server.py:47,56). So on a real AirSim every gps_denied / gps_noise_m / det_false_neg / det_false_pos argument is silently discarded, yet the tool still returns {"ok": True, ...} (server.py:536) with those keys simply absent — a harness cannot tell that the GPS-denied window it just requested never happened, and will write it into its INTREP anyway. The tool must report which knobs were actually applied, or fail explicitly on an unsupported backend.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/launch.py

### [realism-realdata] Make detection noise non-trivial: jitter, range/weather/sun dependence, unlabelled phantoms
- plan: `PLAN.md:202 Phase 6 (M17); PLAN.md:233 risk 'Detection GT cheating'; REVIEW_MILITARY.md:61-62` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: fake_airsim.py:227-231 set_detection_realism; fake_airsim.py:450-451 drops a real contact at a flat false-negative probability; fake_airsim.py:467-485 invents a phantom at a flat false-positive probability; tests/test_realism.py:113-144 exercise both at probability 1.0/0.0.
- required: M17 asks for 'positional jitter + missed detections + false alarms (rate by sensor type, range, weather, sun angle)'. (a) Phantoms are named `phantom_{seq}` (fake_airsim.py:469-470), so a harness can filter every false alarm with one string prefix — the noise is self-labelling and trains exactly the oracle behaviour M17 exists to prevent; classify() then tags them 'unknown' (targets.py:29-35). (b) Real contacts are returned with exact ground-truth geo_point (fake_airsim.py:452-458) — no positional jitter at all. (c) Both rates are flat per-call probabilities with no dependence on slant range (the only range term is the hard 500 m cut at fake_airsim.py:448), on weather (which does not exist), or on sun angle (which does not exist). (d) As above, none of it is reachable on real AirSim.
- files: godseye/mcp/godseye_uav/fake_airsim.py, godseye/tests/test_realism.py

### [realism-realdata] Implement weather as sensor-degradation + fuel multiplier (sim_set_weather)
- plan: `PLAN.md:159 sim_set_weather; PLAN.md:202 Phase 6; REVIEW_MILITARY.md:64-65 (M18)` | state: **missing** | verdict: OVERSTATED | effort: M | blocks_goal: True
- exists: Nothing. fake_airsim.py:509-510 simSetWeatherParameter is a bare `return True` stub. No MCP tool sets weather: the full tool catalog in server.py:314-544 is takeoff/land/rth/goto/route/hover/abort/telemetry/list_vehicles/mission/scan_targets/identify/report/list_tracks/spawn_target/assess_threat/handoff/sim_set_environment/task_status, and sim_set_environment (server.py:509-513) takes only wind, gps and detection params.
- required: M18: 'Keep weather as sensor-degradation knobs + fuel multiplier.' Requires a weather state (rain/snow/fog/dust intensity) that (a) raises the detection false-negative rate and shrinks effective detection range in simGetDetections (fake_airsim.py:437-486), and (b) multiplies the burn rate in FuelModel._burn (safety.py:192-196) and therefore the BINGO gate. Today the fuel rate table (safety.py:41-47) has no weather term and detection generation ignores weather entirely.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py, godseye/mcp/godseye_uav/safety.py
- scope correction: Restate as PARTIAL, not MISSING: "sim_set_environment implements only the wind half of PLAN.md:159's sim_set_weather; add weather intensity and wire it to the sensor/fuel knobs that already exist." Remaining work is a mapping layer plus one fuel term, roughly 4 small edits, not a new sensor/fuel realism subsystem:
1. Add weather state to FakeAirSim (rain/snow/fog/dust intensity 0-1), e.g. a set_weather() sibling of set_wind (fake_airsim.py:212-231), and make the existing simSetWeatherParameter stub (fake_airsim.py:509-510) write it instead of returning True.
2. Derive sensor degradation from that intensity rather than adding new machinery: raise the already-implemented _det_fn_rate used at fake_airsim.py:450-452, and replace the hardcoded `dist <= 500.0` at fake_airsim.py:448 with a range scaled by intensity.
3. Add one clamped weather factor to safety.py:_burn alongside the existing WIND_PENALTY term (safety.py:192-196), plus a WEATHER_PENALTY constant near safety.py:48; it then reaches tick/estimate_route/bingo_fuel_pct/preflight_gate for free via the existing signatures.
4. Expose it as params on sim_set_environment (server.py:509-513) — or add a sim_set_weather alias so the harness sees the plan's name — and pass the resulting factor through at the two call sites that currently drop it: server.py:301 and server.py:363. Fixing those two call sites is also required to make the existing M15 wind-in-fuel path live, so it should be done in the same change.

### [realism-realdata] Implement time-of-day and sun position (sim_set_time) so night/IR missions exist
- plan: `PLAN.md:158 sim_set_time; PLAN.md:202 Phase 6 exit 'Night IR mission…'; REVIEW_MILITARY.md:22-24 (M6)` | state: **missing** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: fake_airsim.py:512-516 simSetTimeOfDay accepts every argument and returns True without storing anything. missions.py:108-117 orbit_waypoints has a sun_azimuth_deg parameter that biases the ring start angle, and missions.py:166 forwards kw.get('sun_azimuth_deg').
- required: The sun_azimuth_deg path is dead code: uav_mission (server.py:378-399) passes only the caller's params dict and no solar computation exists anywhere in the repo (grep for solar/sun_elev/sim_set_time across mcp/ and tests/ returns only those three missions.py lines). There is no sim_set_time MCP tool, no server-side clock, and no day/night effect on sensors: the fake returns a flat 120-grey PNG for ImageType 7 (Infrared) and a flat green PNG for Scene regardless of time or target heat (fake_airsim.py:412-419). The Phase-6 exit criterion's 'Night IR mission' half therefore cannot be demonstrated or tested.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/fake_airsim.py, godseye/mcp/godseye_uav/missions.py
- scope correction: Gap stands as real and missing. Accurate remaining work: (1) add a sim_set_time MCP tool (datetime + clock_speed) that stores a sim epoch server-side and forwards to client.simSetTimeOfDay; (2) give fake_airsim a time state + set_time() setter and make simSetTimeOfDay store it (fake_airsim.py:512-516 currently discards it); (3) add a solar position function (elevation/azimuth from datetime+lat/lon) — none exists in the repo — and surface sun angle in mission planning so the already-wired missions.py:166 sun_azimuth_deg is server-computed rather than harness-supplied; (4) make the fake's imagery respond to time/sensor type instead of the constant RGB table at fake_airsim.py:411-419, so a night IR frame differs from a day scene frame; (5) add the missing uav_capture_image MCP tool (PLAN.md:136) returning the frame + geo pose + sun angle — without it the harness has no MCP path to imagery at all; (6) add a Phase-6 night-IR scenario test alongside tests/test_realism.py. Only the orbit ring's sun-side bias (missions.py:107-127) and the stub signature at fake_airsim.py:512-516 pre-exist.

### [realism-realdata] Stop spawned targets being reported as vehicles (phantom drones on the globe)
- plan: `PLAN.md:94 '/snapshot (loop A)' drone positions; PLAN.md:156 sim_spawn_target; PLAN.md:202 Phase 6 multi-vehicle` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: fake_airsim.py:301-302 `def listVehicles(self): return list(d._vehicles.keys()) + list(d._objects.keys())` — spawned target objects are returned in the vehicle list.
- required: bridge.py:179-181 iterates adapter.vehicles() and calls adapter.snapshot(name) for each, which calls getMultirotorState(vehicle_name=<target>) (bridge.py:111); fake_airsim.py:550-555 _veh() then setdefault-creates a brand-new _Vehicle at NED (0,0,0), landed. Result: every sim_spawn_target call permanently adds a fake UAV parked on the home point to /snapshot (bridge.py:258-266), to the GEV globe (src/layers/uav/index.js:173-176 upserts every record as a cyan drone) and to uav_list_vehicles (server.py:367-370). The 'realistic mission view' fills with phantom drones as soon as targets are spawned, and multi-vehicle counts become meaningless.
- files: godseye/mcp/godseye_uav/fake_airsim.py, godseye/tests/test_fake_airsim.py
- scope correction: Gap stands as written (if anything understated — the target is also duplicated in listVehicles after the first telemetry poll because _veh() setdefault-promotes it into _vehicles). Remaining work is small but genuinely absent: in fake_airsim.py:301-302 return only `list(d._vehicles.keys())`, and make fake_airsim.py:550-555 `_veh()` raise/return None for an unknown name instead of setdefault-creating one, so a stray getMultirotorState on a target cannot mint a phantom vehicle. Optionally add a regression test asserting a spawned object never appears in /snapshot.vehicles or uav_list_vehicles.

### [realism-realdata] Anchor theaters to real data instead of hand-drawn boxes and synthetic POIs
- plan: `PLAN.md:4 'real-world maps'; PLAN.md:196 OriginGeopoint; user goal 'real theaters'` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: Theaters are real-place-named but entirely hand-typed: launch.py:32-45 and uavMissionPanel.js:11-80 give each theater a home [lat, lon, alt] and an axis-aligned 4-vertex lat/lon rectangle. Seed POIs are synthetic offsets from the home named '<Theater> North/Center/South' (uavMissionPanel.js:95-127, e.g. 'Isfahan North' 32.670/51.660, 'Donbas Center' 48.600/37.900).
- required: Nothing about a theater is derived from data. Home altitudes (1550, 1600, 950, 250, 200, 0.0) are typed constants rather than sampled ground height from GEV's /api/terrain/heights (server/providers/terrain.js:159), and they are the entire vertical datum — ned_to_geodetic returns home.geo.altitude - v.z (geo.py:74) and everything AGL/ceiling-related hangs off it (server.py:66, safety.py:150-153). AO polygons are rectangles, not real boundaries. Seed POIs are geometric offsets, not real mapped features, even though GEV can return the real OSM military installations inside a bbox via installations.getMappedSites (src/layers/installations/source.js:16 / server/providers/military-installations.js:80) — which is exactly the 'real installations as mission targets/OB' input the user described. Theater definitions should be resolved at launch: ground elevation from the terrain service, AO from a real bbox, POIs/OB seeded from the installations proxy.
- files: godseye/mcp/godseye_uav/launch.py, godseye/mcp/godseye_uav/server.py, gods-eye-view/src/ui/uavMissionPanel.js
- scope correction: Gap is real, but priority inside it should be reordered — two of the three sub-items block the goal, one is soft:

1. BLOCKING — ground elevation per theater must come from /api/terrain/heights (terrain.js:159) instead of the typed constants, and be resolved in ONE place both launch.py and the panel read. This is the only item that is actively wrong today (the iran-isfahan/Natanz 500 m and 115 km drift above), and it corrupts the vertical datum for every altitude in the system (geo.py:79, server.py:66, safety.py:150).
2. BLOCKING — seed POIs / order-of-battle should come from installations.getMappedSites (source.js:16 / military-installations.js:80) so missions target real mapped sites; today a harness must invent target coordinates for sim_spawn_target (server.py:443).
3. SOFT, not required for "done" — "AO polygons are rectangles, not real boundaries" overstates. A bbox around a real site is a legitimate AO in real ISR practice; the rectangle is only a problem because it is typed three times and drifts, which item 1's single-source fix already resolves.

Sequencing caveat the claim omits: both endpoints are Vite middlewares (terrain.js:216-217 configureServer/configurePreviewServer), served by the GEV dev server that start.sh:68-71 starts AFTER launch.py at start.sh:57-64 — so launch-time resolution needs either a startup-order change or a cached/offline theater manifest.

### [realism-realdata] Model multi-vehicle explicitly instead of relying on accidental auto-creation
- plan: `PLAN.md:202 Phase 6 'multi-vehicle'; PLAN.md:64 simAddVehicle L1083; PLAN.md:216 '1-2 vehicles max'` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: True
- exists: FakeAirSim._veh (fake_airsim.py:550-555) setdefault-creates a _Vehicle for any name on first reference, so uav_handoff_target (server.py:473-504) 'works' by materialising a second drone at NED (0,0,0) the moment it calls backend.telemetry(to_vehicle) (server.py:483). FuelModel instances are likewise created lazily per name (server.py:225-230).
- required: There is no way to declare a second vehicle: the fake has no simAddVehicle handler (grep count in fake_airsim.py = 0) despite PLAN.md:64 citing it, and no MCP tool adds or configures one (full catalog server.py:314-544). A receiving UAV therefore springs into existence at the home point with a fresh 100% fuel tank and no launch history, rather than being a pre-positioned asset — which invalidates the handoff doctrine M10/M5 is meant to teach. On real AirSim the vehicle set is whatever settings.json declares and nothing in the launcher writes it (launch.py:64-77). Needs an explicit per-vehicle roster (home, fuel capacity, sensor config) at launch plus a sim_add_vehicle path.
- files: godseye/mcp/godseye_uav/launch.py, godseye/mcp/godseye_uav/fake_airsim.py, godseye/mcp/godseye_uav/server.py
- scope correction: Gap is real; only the claimed remedy is slightly oversized. Minimum work actually required: (1) a launch-time vehicle roster (name, start lat/lon or NED offset, fuel capacity) threaded through launch.py:66-77 into FakeAirSim.__init__ (fake_airsim.py:82-85) and into GodseyeUavServer(fuels=...) (server.py:202,209), with a start.sh/--vehicles flag; (2) make fake_airsim.py:550-555 _veh() raise on an unknown name so it matches real AirSim instead of silently materialising drones; (3) fix fake_airsim.py:302 listVehicles() to stop returning _objects, and/or filter in bridge.py:97-104, to stop spawned targets rendering as phantom UAVs in GEV. A sim_add_vehicle MCP tool and per-vehicle sensor config are NOT required — the plan's own tool contract (PLAN.md:124-161) never lists sim_add_vehicle (it appears only in the AirSim API inventory at PLAN.md:64), and PLAN.md:216 caps the laptop profile at 1-2 vehicles, which a static roster covers. Secondary: gods-eye-view/src/ui/uavMissionPanel.js:362,379,543,558 hardcodes 'Drone1' and would need a vehicle selector for an operator to see the receiving UAV, though the harness drives MCP directly.

### [realism-realdata] Publish wind, GPS quality and est_range_km in uav_get_telemetry
- plan: `PLAN.md:137 §4.2 uav_get_telemetry returns '… fuel_pct, bingo_fuel_pct, est_range_km, landed_state, wind'` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: True
- exists: server.py:358-365 uav_get_telemetry returns vehicle, lat, lon, alt_hae, ned, speed_mps, vz_mps, landed_state, a gps{lat,lon} echo (server.py:109-119), fuel_pct, bingo_fuel_pct and queue status.
- required: The plan's contract names wind and est_range_km; neither is present, and no GPS-quality field is present either (M16 requires 'log GPS quality in telemetry'). The consequence is doctrinal, not cosmetic: the skill is supposed to do 'wind in fuel math (M15)' and 'GPS-degraded fallback = INS/terrain nav (M16)' (PLAN.md:186), but telemetry is the harness's only window onto vehicle state and it exposes neither the wind vector it should crab into nor any indication that the fix has been frozen or jittered. The GEV HUD has the same blind spot — the panel readout prints only lat/lon/alt/spd/fuel/bingo (uavMissionPanel.js:545-548).
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/bridge.py


## MINOR (17)
### [tools-missions] Scope threat assessment to an area_polygon and return per-element confidence
- plan: `PLAN §4.3 mission_threat_assessment (vehicle, area_polygon) / §4.6 M13` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: server.py:454-471 uav_assess_threat(vehicle, track_id?, defended?) — no area_polygon param; the area path (server.py:467) calls threat.assess_area over self.tracks.tracks(), i.e. every track ever seen anywhere, with no AO filter. threat.py:81-92 returns threat_score/threat_level/intent/rationale; there is no confirmed/probable/possible confidence field and no pattern-of-life term (M12) in the intent calculation (threat.py:62-76 uses instantaneous heading only).
- required: §4.3 params (vehicle, area_polygon) and §4.6's confidence taxonomy — confirmed/probable/possible with cited evidence per element. The missing polygon means a harness cannot ask 'what is the threat in this AO'; it can only ask about one track or about everything, which misreports the picture as soon as more than one AO has been surveyed in a session.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/threat.py
- scope correction: Gap is real but bounded — narrower than "build the M13 pipeline". §4.6 clauses (a) order-of-battle library (threat.py:20-30) and (d) traceable capability x intent score (threat.py:78, threat.py:95-105) ARE implemented, and M14's ROE removal is honored (threat.py:108-113). Remaining work is exactly two items: (1) add an `area_polygon` param to uav_assess_threat (server.py:454) and filter `self.tracks.tracks()` (server.py:467) through the existing `safety.point_in_polygon` (safety.py:89) before calling `threat.assess_area`, echoing the AO in the THREATREP header so a harness can tell which AO the roll-up covers; (2) add a confidence field (confirmed/probable/possible) to the threat.py:81-92 return, derivable from evidence already on Track — sightings (targets.py:49), first_seen/last_seen recency, history length (targets.py:52-53) — with the citing evidence named per element. The pattern-of-life intent term (M12, threat.py:62-76 uses instantaneous heading only) is a separate finding and should be scored under M12, not folded into this one.

### [tools-missions] Expose cancel-by-handle instead of only whole-queue abort
- plan: `PLAN §4.3 mission_cancel(mission_handle)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: server.py:351-356 uav_abort(vehicle) calls queue.abort() which drains every queued task and cancels the current one (tasking.py:189-204). VehicleQueue.cancel_current (tasking.py:180-187) is never exposed as a tool; nothing accepts a task_id/mission_handle to cancel.
- required: mission_cancel(mission_handle) that stops one mission. With FIFO queuing a harness can have a follow-on mission queued behind the active one; cancelling the active mission today also silently destroys the queued plan, and the harness gets no way to express 'abandon this leg, keep the rest'.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/tasking.py
- scope correction: Not a downgrade — the remaining work is slightly LARGER than the prior auditor stated, not smaller. Beyond wrapping `cancel_current` in a tool, a correct `mission_cancel(mission_handle)` also needs a handle-resolution fix: `VehicleQueue.get` (tasking.py:209-215) searches only `_current` and `_history`, never the pending `_queue`. Verified empirically: `q.get(<queued task id>)` returns None while that task is queued, so `uav_task_status` already returns `{"error": "unknown task_id"}` for a submitted-but-not-started mission (server.py:542-543). Consequently a naive `mission_cancel` built on `get()` + `cancel_current()` would either fail to find a queued handle or, worse, cancel the wrong (currently active) mission. Minimum fix: (a) make task lookup cover pending queue entries, (b) add a tool taking task_id that cancels the current task only when the handle matches `q.current.id`, and marks a queued task CANCELLED in place otherwise (leaving the rest of the FIFO intact), (c) keep the existing BINGO force-RTB transition un-cancellable per PLAN.md:166.

### [tools-missions] Normalize mission waypoint/vertex shapes so a malformed route fails cleanly
- plan: `PLAN §4.3 mission_recon_route params (harness-facing contract)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: missions.py:46-50 _pt accepts a polygon vertex as a (lat,lon) tuple OR a {'lat','lon'} dict, but missions.py:148-152 recon_route passes kw['waypoints'] straight through untouched. server.py:387 then hands them to _gate → safety.estimate_route which does float(wp['lat']) (safety.py:221). A tuple-shaped route therefore raises TypeError inside _gate, outside the try/except at server.py:382-385 which catches only ValueError/KeyError — it escapes as an unhandled tool error rather than a structured rejection.
- required: One accepted waypoint shape (or _pt normalization applied to recon_route too) and the gate call inside the guarded block, so a harness that reuses the polygon vertex shape for a route gets {'rejected': True, 'error': ...} instead of a server-side exception.
- files: godseye/mcp/godseye_uav/missions.py, godseye/mcp/godseye_uav/server.py

### [safety-fuel] Declare FuelModel.home as a field and stop silently dropping the return leg
- plan: `PLAN.md §4.5 BINGO doctrine (M4)` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: `FuelModel` (safety.py:171-179) declares only capacity_s_cruise/fuel_pct/rates/_last_ts/_last_alt — there is no `home` field — yet `bingo_fuel_pct` (safety.py:245-246) and `preflight_gate` (safety.py:255) both read `self.home`. Verified: `FuelModel().bingo_fuel_pct((47.6,-122.1),50.0)` raises `AttributeError: 'FuelModel' object has no attribute 'home'`. It only works because server.py:226-230 `fuel_for` monkey-patches `fm.home = self.envelope.home`. Second half: `SafetyEnvelope.home` defaults to None (safety.py:143) and `GodseyeUavServer.__init__` falls back to a bare `SafetyEnvelope()` (server.py:208), in which case `bingo_fuel_pct` silently degrades to a flat 20% (safety.py:245) and `preflight_gate`'s return leg becomes 0.0 (safety.py:255,261) — the M4 gate loses its return-leg component with no warning.
- required: Declare `home` as a real dataclass field, and reject (or loudly warn) when a BINGO gate is evaluated without a configured home instead of silently dropping the return leg.
- files: mcp/godseye_uav/safety.py, mcp/godseye_uav/server.py
- scope correction: Scope as claimed is accurate. Remaining work is small and exactly as stated: (1) add `home: tuple[float, float, float] | None = None` as a real field at safety.py:179 so FuelModel() is constructible-and-usable without the server.py:227-229 monkey-patch (and drop the hand-patching in tests/test_safety.py:98,113,127); (2) make the None-home case loud rather than silent — either raise in bingo_fuel_pct/preflight_gate (matching the existing server.py:246-248 precedent) or add an explicit `home_configured: false` / warning key to the gate dict at safety.py:264-272 so a harness cannot read ok=True from a gate that dropped its return leg.

### [safety-fuel] Return `busy` instead of queueing behind an in-flight command
- plan: `PLAN.md §1 hard rules ("Busy commands return `busy` + current handle — harness decides"); REVIEW_TECHNICAL.md T2 ("Reject, don't queue-behind-fail")` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: `VehicleBusyError` is defined at tasking.py:67-70 but is never raised anywhere. `VehicleQueue.submit` (tasking.py:116-125) unconditionally enqueues, and `TaskingService.submit` (tasking.py:286-290) forwards to it, so a `uav_takeoff` arriving mid-cruise is silently queued behind the route instead of returning `busy` with the current handle. No tool in server.py:317-538 inspects `queue_for(v).current` before submitting.
- required: Submission while a vehicle is executing should return `busy` plus the current task handle, leaving the decision to the harness.
- files: mcp/godseye_uav/tasking.py, mcp/godseye_uav/server.py
- scope correction: Scope as claimed is correct; adding two mitigating details that shrink the fix but do not make the capability present. (1) The one-in-flight FIFO half of T2 IS implemented (tasking.py:142-178 worker loop), so this is a missing rejection policy, not a missing queue. (2) The exact busy payload already exists: `TaskingService.status()` (tasking.py:292-299) returns `{vehicle, state, current: <current task handle>, queued}`. Remaining work is therefore single-site: in `MCPServer._submit` (server.py:309-312), before calling `self.tasking.submit(...)`, check `self.tasking.queue_for(vehicle).current` and, when non-None, return `{"busy": True, **self.tasking.status(vehicle)}` instead of enqueuing (raising `VehicleBusyError` from tasking.py:67-70 and translating it at the server boundary is the equivalent shape); `uav_abort` (server.py:351-356) must bypass the check since it aborts first. Also add the missing T2 contract test (REVIEW_TECHNICAL.md:38) asserting a mid-route `uav_takeoff` returns busy with the in-flight handle rather than a new `queued` handle. Separately worth noting for harness authors: `Task.handle()` (tasking.py:52-61) reports `state: "queued"` both for a task stacked behind an in-flight one and for one about to start immediately, so `"queued"` is a racy, unusable busy signal — and the handle returned is the new task's, not the blocking task's.

### [bridge-command-center] Camera PIP serves PNG, not JPEG, and has no quality control
- plan: `PLAN §3.1 feed table row 4 ('2–5 Hz JPEG'), T3 fix ('convert PNG→JPEG in the bridge at the requested quality'), §8.1 laptop budget ('camera JPEG compress=true, 640×480 @ 3 Hz')` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: AirSimAdapter.camera_png requests airsim.ImageRequest(camera, image_type, False, True) — compressed PNG (bridge.py:144-158) — and the endpoint returns it with media_type="image/png" (bridge.py:393-395). There is no jpeg_quality parameter on the route (bridge.py:384-385) and no re-encode step; tests assert the PNG magic bytes (tests/test_bridge.py:83-84). Rate itself is compliant: camera_loop defaults to 3 Hz (bridge.py:196, started at bridge.py:251).
- required: JPEG output at a requested quality (PNG→JPEG re-encode in the bridge) to meet the PIP bandwidth budget on the laptop profile.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/tests/test_bridge.py
- scope correction: Gap stands as written; if anything the prior auditor understated the work. Remaining work is not smaller than claimed: (1) add an imaging dependency to pyproject.toml:10-17 (no Pillow/opencv/simplejpeg is present, so a PNG->JPEG re-encode cannot be written today); (2) add a jpeg_quality (and optional width/height for the 640x480 budget) param to the route at bridge.py:384-385 and thread it into camera_png at bridge.py:144; (3) re-encode and switch bridge.py:395 to media_type="image/jpeg"; (4) update tests/test_bridge.py:83-84 and give fake_airsim.py:32-44 a JPEG-capable path so the 106-test suite still covers it. Only the priority is arguably lower than implied: no GEV PIP consumer exists yet (src/layers/uav/index.js has no camera code), so this is a prerequisite for the PIP rather than a live bandwidth problem.

### [bridge-command-center] Entity writes are capped at 5 Hz but use ConstantPositionProperty — no client-side interpolation
- plan: `PLAN §3 last line + §3.1 operator experience #1 ('5 Hz smooth motion (T6 sampled properties + interpolation)'), T6` | state: **partial** | verdict: CONFIRMED | effort: M | blocks_goal: False
- exists: The rate half of T6 is satisfied: gods-eye-view/src/layers/uav/index.js:10 sets DEFAULT_POLL_MS = 200 (5 Hz), exposed as refreshInterval at index.js:137 and armed by src/data/lifecycle.js:515-527, so exactly one entity write per 200 ms per vehicle. The interpolation half is absent: every tick allocates a fresh `new Cesium.ConstantPositionProperty(cart)` (index.js:74, and index.js:126 for targets), which steps the entity discretely instead of a SampledPositionProperty with an interpolation algorithm — motion visibly jumps at 5 Hz rather than being smoothed between samples.
- required: Push samples into a SampledPositionProperty (or equivalent) with client-side interpolation so the 5 Hz feed renders as smooth motion, per T6 and the Phase-3 acceptance.
- files: gods-eye-view/src/layers/uav/index.js, gods-eye-view/src/layers/uav/uav.test.mjs
- scope correction: Gap stands as PARTIAL. One correction, which enlarges rather than shrinks it: the prior auditor's prescribed fix ("push samples into a SampledPositionProperty") is necessary but not sufficient. Because src/renderGovernor.js:46-47 puts the scene into requestRenderMode=true whenever no layer holds continuous render, and src/layers/uav/index.js registers no hold while requesting a single frame per 200 ms tick (index.js:180), an interpolating property would still be sampled only 5x/sec. Remaining work: (1) buffer per-vehicle fixes and drive position from an interpolating property or a preRender-driven dead-reckoner, mirroring src/layers/military/motion.js:57-115; (2) register holdContinuousRender('uav') / releaseContinuousRender('uav') on enable/disable as every peer layer does (e.g. military/lifecycle.js:118); (3) apply the same to the four ConstantPositionProperty sites at index.js:35, :74, :105, :126.

### [bridge-command-center] /snapshot observedAtMs is stamped at request time, so freshness/stale can never detect a frozen telemetry loop
- plan: `PLAN §3.1 DoD ('motion ≤250 ms behind sim'); T9 ('expose sim_state in /snapshot so GEV shows it instead of silently going stale')` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: bridge.py:263 sets "observedAtMs": int(time.time()*1000) at serialization time, not from the cached samples (each VehicleSnapshot does carry a true sample time at bridge.py:137). The consumer computes staleness from that field: gods-eye-view/src/sources/live/uav.js:102 reads body.observedAtMs and line 118 computes `freshness: Date.now() - observedAtMs` — always ~0 — while `stale` (line 117) is derived solely from the sim_state string. If the telemetry loop thread dies or the adapter stops returning snapshots, /snapshot keeps serving the last cached positions with a fresh timestamp and the UI shows them as current.
- required: observedAtMs must be the newest sample timestamp in the cache (or a per-vehicle age), so the contract's stale/freshness handling and the ≤250 ms latency acceptance are actually measurable.
- files: godseye/mcp/godseye_uav/bridge.py, gods-eye-view/src/sources/live/uav.js
- scope correction: Real remaining work is one line in the bridge, plus an optional cosmetic contract alignment. It does not block the harness goal.

1. REQUIRED (1 line, godseye/mcp/godseye_uav/bridge.py:263): replace the request-time stamp with a rollup of the cached sample times —
   `"observedAtMs": max((s.timestamp_ms for s in snaps), default=None),`
   No consumer change is needed: uav.js:103 already falls back via `epoch(...) ?? Date.now()` and uav.js:119 becomes a true age automatically. Add a test asserting /snapshot's observedAtMs equals the newest cached `timestamp_ms` and does not advance when the cache is frozen.

2. OPTIONAL hardening for the only genuinely uncovered case (silent stall, not adapter failure): either lower the msgpack-rpc timeout at rpc_patch.py:23 (3600s is effectively infinite) so a hung RPC raises into the existing bridge.py:139-142 → sim_state "down" path that already works, or have bridge.py:92-95 report degraded when the newest cached `timestamp_ms` exceeds a staleness budget.

3. COSMETIC / separate from this gap: uav.js:119 emits `freshness` as a NUMBER, while the reference source contract emits a string enum — gods-eye-view/src/sources/live/vessels.js:64-68 yields 'stale' | 'unknown' | 'current'. Nothing currently reads uav's `freshness`, so this is a naming/shape deviation only; worth aligning if a harness is expected to read the field generically.

NOT required (contrary to the original finding): exposing a per-vehicle sample age — bridge.py:43,137,265 and uav.js:94 already carry it; and sim_state-based stale detection — bridge.py:92-95,262 and uav.js:115,117 already implement T9.

### [bridge-command-center] No bridge-config rate budgets; loop A runs at 10 Hz against the plan's 5 Hz laptop budget
- plan: `PLAN §8.1 'Budgets (enforced in bridge config): telemetry 5 Hz, camera 3 Hz max, 1 camera subscriber max'; §3.1 feed table row 1 ('5 Hz')` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: telemetry_loop defaults to hz=10.0 (bridge.py:175) and create_app starts it with no argument (bridge.py:250), so the live system polls AirSim at 10 Hz — within T3a's ≤10 Hz ceiling but double the §8.1/§3.1 budget. create_app's signature is (adapter, token, start_loops) only (bridge.py:226-227): there is no config object for telemetry Hz, camera Hz, subscriber cap or image size, and launch.py:87 passes none.
- required: A bridge config surface carrying the §8.1 budgets (telemetry 5 Hz, camera ≤3 Hz, 1 subscriber, 640×480) with the loops honouring it.
- files: godseye/mcp/godseye_uav/bridge.py, godseye/mcp/godseye_uav/launch.py
- scope correction: Two small, localized edits in godseye/mcp/godseye_uav/bridge.py — no config object, no launch.py change, no image-size work:
1. Change the telemetry default from 10.0 to 5.0 at bridge.py:175, or start it as `telemetry_loop, kwargs={"hz": 5.0}` at bridge.py:250, so loop A matches PLAN.md:220's 5 Hz laptop budget. (Optionally reconcile the bridge.py:5 docstring, which currently quotes the T3a <=10 Hz ceiling.)
2. Cap the camera subscriber set to 1 in `subscribe_camera` (bridge.py:208-210) — e.g. clear/replace the existing entry instead of `add`, or reject a second distinct key — to enforce "1 camera subscriber max".
Explicitly NOT needed: camera Hz (already 3.0 at bridge.py:196), the §3.1 5 Hz browser poll (already 200 ms at gods-eye-view/src/layers/uav/index.js:10), and a 640x480 bridge setting (belongs to AirSim settings per PLAN.md:216, not the bridge-config budgets row). If a config object is wanted for tidiness it is optional polish, since both loops already accept `hz`.

### [bridge-command-center] Loop A is not batched: listVehicles RPC every tick, one state call per vehicle, no sensor fields
- plan: `PLAN §1 loop A (T3a) 'telemetry ≤10 Hz batched'; T3 fix 'one batched call (getMultirotorState + sensors), single message on the wire'; §4.2 telemetry returns wind` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: telemetry_loop calls self.adapter.vehicles() on every iteration (bridge.py:179), which issues a listVehicles RPC each tick (bridge.py:97-104), then a separate getMultirotorState per vehicle (bridge.py:111). Nothing else is batched into the tick: no getGpsData, no wind, no simGetCollisionInfo — so the published row (bridge.py:123-138) carries no wind vector even though §4.2's telemetry contract and the M15 fuel model need it, and the fuel integrator's headwind argument (safety.py:199-206, 242) has no live source.
- required: One batched per-tick fetch per vehicle (state + the sensors the contract publishes, incl. wind) with the vehicle list cached rather than re-queried at loop rate.
- files: godseye/mcp/godseye_uav/bridge.py
- scope correction: Gap is real; trim one element and add one. NOT required: collapsing multiple vehicles into a single RPC (AirSim has no such API), and fixing "single message on the wire" (bridge.py:258-266 already sends one /snapshot payload for all vehicles; uav.js:102 consumes it).

Actually remaining work: (a) cache the vehicle list in AirSimAdapter with a refresh interval (e.g. 2-5 s) instead of calling listVehicles at 10 Hz (bridge.py:179 -> 97-104) — small, performance-only, does NOT block the agentic goal; (b) add wind + fuel to the per-tick fetch and to VehicleSnapshot (bridge.py:28-48, 123-138) and to uav_get_telemetry (server.py:358-364) so §4.2's contract and the M15 doctrine are observable by the harness — this DOES block the goal, since an agent can set wind via sim_set_environment and never observe it; (c) the load-bearing prerequisite the auditor missed: wire FuelModel.tick() into the telemetry path at all (safety.py:198 has no production caller) and persist via store.log_fuel (store.py:76-77) — without this, plumbing wind changes nothing because fuel_pct is a constant 100.0 in both the bridge row and uav_get_telemetry.

### [geo-datum] Set heightReference explicitly on GEV UAV entities and clamp target markers instead of defaulting them to ellipsoid zero
- plan: `PLAN.md:83 (§3 Datum) 'GEV sets entity heightReference explicitly'; REVIEW_TECHNICAL.md:10` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: godseye/../gods-eye-view/src/layers/uav/index.js:26-30 and :213-217 call Cesium.Cartesian3.fromDegrees(lon, lat, ellipsoidAltitude) with no heightReference on the entity. Grepped src/layers/uav, src/app/layers/uav.js and src/sources/live/uav.js for heightReference: zero hits. The drone case is implicitly correct (default HeightReference.NONE is ellipsoid-absolute, which matches HAE) so the vehicles render right — but the plan explicitly requires it not be left to a Cesium default. The target-marker case is an actual bug: src/layers/uav/index.js:100 does fromDegrees(loc.lon, loc.lat, loc.alt_m ?? 0), and track altitude comes from godseye/mcp/godseye_uav/targets.py:105 (gp.get('altitude', 0.0)), so any track lacking altitude is planted at ellipsoid zero — which at Srinagar is ~1600 m underground and at Seattle ~22 m off. For contrast, the reference layer the uav layer was cloned from does wire a real geoid service (src/layers/military/records.js:51-57, src/layers/military/tracking.js:671 baroM + geoidHeight(lat,lon)); the uav layer correctly does not, since the bridge is meant to be the one conversion point — that part is right.
- required: Explicit heightReference on UAV entities per PLAN.md:83, and CLAMP_TO_GROUND (or a real altitude) for ground-target markers so contacts sit on the terrain rather than on the ellipsoid. Low effort, and it is the last hop where a correct altHae can still be rendered wrong.
- files: gods-eye-view/src/layers/uav/index.js, godseye/mcp/godseye_uav/targets.py
- scope correction: Gap is CONFIRMED and if anything understated. Actual remaining work, in order: (a) set heightReference: Cesium.HeightReference.NONE explicitly on the drone point/label at src/layers/uav/index.js:36-52 so the HAE contract is stated rather than inherited from a Cesium default; (b) set heightReference: Cesium.HeightReference.CLAMP_TO_GROUND on the target point/label at src/layers/uav/index.js:106-122 and pass height 0 at :100 (with CLAMP the height argument is ignored, which is the desired behavior for a ground contact) — enableCollision:true is already set at src/maps/google3d.js:79 so this works immediately; (c) ALSO fix the second marker path missed by the prior auditor: src/ui/uavMissionPanel.js:406-428, same clamp, and note its click handler at :439-444 already picks the true terrain height and throws it away; (d) beyond the claim's scope but same root cause — convert track altitude MSL->HAE at the single bridge conversion point, i.e. apply msl_to_hae() to salute_report location.alt_m in bridge.py:370-375 (or inside targets.py ingest) so tracks obey PLAN.md:83's "ONE conversion point in the bridge"; without this a track with a real altitude is still rendered in the wrong datum even after clamping is added.

### [tasking-persistence-auth] Scope idempotency keys server-wide and persist them
- plan: `PLAN §4 intro (T4b); §4.8 persistence` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: The dedup map is `VehicleQueue._idem` (tasking.py:96), an in-memory dict per vehicle. Consequences visible in the code: (a) it is never written to any journal, so a restart loses all dedup state and a retried command re-executes; (b) it is per-vehicle, so the same key submitted for Drone1 and Drone2 via TaskingService.submit (tasking.py:286-290) creates two independent tasks; (c) it is unbounded — entries are added at tasking.py:121-122 and never evicted, growing for the life of the process; (d) tasking.py:118-119 returns the cached Task even when it is in a terminal FAILED state, so a retry after a genuine failure silently returns the old failure handle and never re-flies.
- required: T4b idempotency should be a server-scoped, persisted, TTL-bounded key->handle map so retries are safe across a reconnect or restart, which is exactly when a harness retries.
- files: godseye/mcp/godseye_uav/tasking.py, godseye/mcp/godseye_uav/store.py
- scope correction: Drop "scope server-wide" and "TTL" from the gap; keep only persistence, and recognize it is one slice of the wholly-absent T4c restart-replay rather than a standalone idempotency defect. Actually remaining work, ~20 lines: (1) add `idempotency_key` to the dict returned by Task.handle() (tasking.py:52-61) — or pass it as an extra field at the store.log_task call in server.py:311 — so tasks.jsonl carries it; (2) on startup in launch.py (near the Store(args.db) construction at launch.py:76), read tasks.jsonl via store.tasks.read_all() and repopulate each VehicleQueue._idem with the prior key->task mapping (or a key->terminal-handle map), so a post-restart retry returns the old handle instead of re-flying. Optionally cap the map with an insertion-ordered eviction. Do NOT re-key the map on (key) alone across vehicles and do NOT make a repeated key re-execute after FAILED — both would regress correct idempotent-replay semantics. Note the larger finding this sits inside: there is no restart/resume/recover code or test anywhere in mcp/ or tests/, so PLAN.md:198's Phase-2 exit criterion is unmet independently of idempotency.

### [tasking-persistence-auth] Derive AuthSettings URLs from the actual bind port and test auth over HTTP
- plan: `PLAN §1 Hard rules (T4e); §8.1 'token = env var'` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: Auth itself is fine: StaticBearerVerifier (server.py:28-37) rejects any non-matching token, and Origin/Host validation is on by default because serve() binds 127.0.0.1 (server.py:546-547) and the SDK auto-enables DNS-rebinding protection for loopback hosts (.venv/.../mcp/server/lowlevel/server.py:742-746) — T4e is substantially met. Two loose ends: (a) `AuthSettings(issuer_url='http://127.0.0.1:8791', resource_server_url='http://127.0.0.1:8791', ...)` is hardcoded at server.py:216-221 while launch.py:52 exposes `--mcp-port`, so on any other port the protected-resource-metadata URL and WWW-Authenticate pointer (.venv/.../lowlevel/server.py:804-827) advertise the wrong endpoint; (b) `validate_token_resource` is left unset with `resource_server_url` set, which the SDK warns will default to True in 3.0 (.venv/.../mcp/server/auth/settings.py:59-64) — at which point StaticBearerVerifier's AccessToken (server.py:37, no `resource=`) would fail every request.
- required: Build the auth URLs from the host/port actually passed to serve(), and set validate_token_resource explicitly. Also note the only auth test is a direct unit call on the verifier (tests/test_server.py:47-50) — nothing exercises a rejected tools/call over the real HTTP transport, so a regression in the wiring at server.py:214-222 would not be caught.
- files: godseye/mcp/godseye_uav/server.py, godseye/mcp/godseye_uav/launch.py, godseye/tests/test_server.py
- scope correction: Drop the headline item. Do NOT plumb host/port into AuthSettings — the URLs it would correct point at OAuth endpoints that return 404 on every port (no auth_server_provider is configured, so create_auth_routes never runs), so the change is cosmetic and buys a harness nothing. Nothing about the current auth blocks the user's goal: a bearer-token harness gets 200 and the full tool catalog on any port, verified live on 8999.

What is actually left, in priority order:

1. (Real, ~1 keyword) Set validate_token_resource=False explicitly at mcp/godseye_uav/server.py:217-221. This silences the MCPDeprecationWarning and, more importantly, defuses the mcp 3.0 flip that would otherwise 401 every request once the default becomes True (StaticBearerVerifier's AccessToken at server.py:37 carries no resource=). Pair it with an upper bound on pyproject.toml:16 ("mcp>=1.6,<3") so a fresh install cannot silently pick up the breaking default.

2. (Real, ~15 lines) Add an HTTP-level auth test for the MCP server, mirroring the existing bridge pattern at tests/test_bridge.py:44-47: assert 401 with no token, 401 with a wrong token, 200 with the right token against the streamable-HTTP app. This is regression safety for server.py:214-222, not a missing capability.

3. (Optional, cosmetic) If AuthSettings URL accuracy is ever wanted for its own sake, thread host/port through GodseyeUavServer.__init__ (server.py:197-205) so they match serve() (server.py:546). Low value while no authorization server exists; revisit only if real OAuth is ever added, at which point the resource identifier should also become ".../mcp" rather than the bare origin (today /.well-known/oauth-protected-resource/mcp returns 404 even on the default port).

### [intel-tracks-threat] Area threat assessment ignores the AO polygon — assesses every track globally
- plan: `PLAN.md:149 §4.3 `mission_threat_assessment(vehicle, area_polygon)` / §4.6` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: godseye/mcp/godseye_uav/threat.py:116-127 `assess_area(tracks, observer, defended)` has no polygon parameter and no spatial filter; the MCP tool `uav_assess_threat(vehicle, track_id, defended)` (godseye/mcp/godseye_uav/server.py:454-471) passes `self.tracks.tracks()` — the entire global store — when no track_id is given.
- required: PLAN §4.3 specifies `area_polygon` as the parameter of the threat-assessment mission. A harness running two AOs (or two theaters from launch.py:32-45) cannot scope an assessment; every INTREP/THREATREP mixes contacts from unrelated areas, which also corrupts `highest_threat` (threat.py:124).
- files: godseye/mcp/godseye_uav/threat.py, godseye/mcp/godseye_uav/server.py
- scope correction: Real remaining gap is narrow: no sub-AO/sector scoping of the threat roll-up WITHIN a single theater, i.e. successive sorties over different sectors of the same AO accumulate in one store and a later `uav_assess_threat` with no track_id blends them (threat.py:119) and so can mis-report `highest_threat` (threat.py:125) for the sector the harness cares about. It is NOT cross-theater or cross-AO contamination — that is prevented by one-theater-per-process (launch.py:50,60,75-77; server.py:213), by the 500 m detection radius (fake_airsim.py:447), and by the geofence route gate (server.py:298-302; safety.py:148).

Actual work left is roughly a 4-line change reusing an existing tested primitive, not a new capability:
1. threat.py:116 — add `area_polygon: list | None = None` and, before line 119, `if area_polygon: tracks = [t for t in tracks if point_in_polygon(t.lat, t.lon, area_polygon)]` (import from .safety), echoing the polygon in the THREATREP dict so the report states its scope.
2. server.py:454 — add `area_polygon: list | None = None` to `uav_assess_threat` and forward it at server.py:467; mention it in the tool description (server.py:450-453) so a harness discovers it.
3. Optionally accept the same kwarg on `uav_target_report` (server.py:429-432 / targets.py:156) for INTREP parity.

Naming deviation worth noting separately (not part of this gap): PLAN.md:149 specifies `mission_threat_assessment` returning a mission_handle (a flying mission), whereas the implementation is a pure analysis tool `uav_assess_threat` with no handle; a harness composes it as uav_mission(grid_search, polygon) -> uav_scan_targets -> uav_assess_threat.

### [intel-tracks-threat] SALUTE 'Size' is hardcoded to "1" and 'Unit' carries the OB category
- plan: `PLAN.md:176 §4.7 (Size/Activity/Location/Unit/Time/Equipment) / M8` | state: **partial** | verdict: CONFIRMED | effort: S | blocks_goal: False
- exists: godseye/mcp/godseye_uav/targets.py:129-145 — all six SALUTE fields are present and structured (good), but `"size": "1"` is a literal with the comment 'single platform per track' (targets.py:134) and `"unit": track.category` (targets.py:139). tests/test_targets.py:58-60 only asserts key presence and `rep["unit"] == "sam"`.
- required: Size must reflect the observed element (a 12-vehicle convoy, the Phase 6 moving-convoy scenario at PLAN.md:202, reports as twelve separate 'size 1' contacts with no aggregation). Unit should carry a unit/identity assessment distinct from equipment category, since Equipment already holds the platform name (targets.py:140). The artifact shape itself is correct — this is a fidelity gap inside an otherwise present structure.
- files: godseye/mcp/godseye_uav/targets.py

### [gev-ui] GEV issues flight commands, contrary to the plan's read-only God's Eye rule
- plan: `PLAN.md §1 Hard rules L47 ('MCP server is the only command path; GEV UI is read-only (God's Eye)') and §3.1 L89 ('GEV remains read-only for flight control')` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: The mission panel POSTs mission launches and aborts from the browser: gods-eye-view/src/ui/uavMissionPanel.js:361-363 `post('/control/mission', {vehicle:'Drone1', kind, params})` and uavMissionPanel.js:379 `post('/control/command', {tool:'uav_abort', …})`, served by godseye/mcp/godseye_uav/bridge.py:358-364, which forwards to the MCP tool endpoint. The bridge comment at bridge.py:283-285 acknowledges the tension. Commands do still transit the MCP server, so the safety envelope is not bypassed, but the browser is now a command originator the plan says it must not be.
- required: Decide explicitly: either amend the plan to sanction an operator command lane (and document that the harness and the operator can both task the same vehicle FIFO), or move mission launch out of GEV. Left as-is, an agentic harness and a human panel can race for the same per-vehicle queue with no UI indication of who owns the vehicle.
- files: godseye/PLAN.md, gods-eye-view/src/ui/uavMissionPanel.js
- scope correction: No architectural change and no plan rewrite of the command path is needed — the safety envelope, mission gates and FIFO already cover the racing concern the finding raises. What is actually left is three small items: (1) one-line doc reconciliation in PLAN.md §1 L47 / §3.1 L89 sanctioning an operator lane that transits the MCP server (or, if strict read-only demos are wanted, a config flag in gods-eye-view/src/app/controls.js:56 that hides the Launch/Abort buttons — the panel is already self-contained and destroy()-able at uavMissionPanel.js:571-579); (2) render the already-transmitted `queue.current.tool`/`task_id`/`state` in uavMissionPanel.js:546-549 so the operator sees who owns the vehicle — pure display, data is already on the wire from server.py:364; (3) replace the unvalidated `body.get("tool")` at bridge.py:344-350 with an allowlist (uav_abort, uav_hover, uav_cancel) plus a test, since today it forwards arbitrary tool names. Do NOT move mission launch out of GEV.

### [skill-harness-demo] GPS-degraded fallback (M16) is fake-sim-only and has no documented detection or fallback path
- plan: `PLAN.md:186 (GPS-degraded fallback = INS/terrain nav, M16), PLAN.md:161 (sim_set_gps_degradation)` | state: **partial** | verdict: OVERSTATED | effort: S | blocks_goal: False
- exists: sim_set_environment exposes gps_denied/gps_noise_m (server.py:510-512) but applies them only through `sim = getattr(self.backend, 'sim', None)` (server.py:523-530) — the FakeAirSim handle set at launch.py:74/server.py:57. With --real (launch.py:65, sim stays None) the parameters are silently accepted and dropped: the returned state dict (server.py:520) simply omits them, with no error. On the detection side, telemetry does return the NED-derived fix and the raw GPS fix separately (server.py:111-118 lat/lon vs `gps`), so divergence IS observable, but nothing documents that as the M16 signal and there is no INS/terrain-nav mode the skill can command.
- required: Either implement GPS degradation for the real backend or have the tool return an explicit unsupported/no-op flag instead of silently succeeding. Expose a gps_quality/fix-valid indicator on telemetry so the skill has a defined trigger, and define the fallback behavior (dead-reckon on the NED estimate, reduce speed, RTB) as either a server mode or an explicit skill procedure documented against the telemetry fields above.
- files: godseye/mcp/godseye_uav/server.py
- scope correction: Real remaining work is much smaller than claimed, and is two small code edits rather than a backend implementation plus a new fallback subsystem:

1. Emit a GPS quality indicator on telemetry so the harness has a defined trigger. Today server.py:109-119 returns no such field and fake_airsim.py:396-399 reports fix_type=3 / satellites_visible=12 / is_valid=True even while _gps_denied is True. Fix: have _reported_geo's denial/noise state drive fix_type/is_valid/eph in fake_airsim.py:382-400, and add a derived gps_quality (or fix_valid + gps_divergence_m between the NED-derived lat/lon and the raw gps fix) to the dict at server.py:109-119, then name it in the uav_get_telemetry description at server.py:358.

2. Make sim_set_environment stop lying when it cannot honor the request: at server.py:523-534, when sim is None, return the gps_denied/gps_noise_m keys with an explicit unsupported/no-op marker instead of omitting them from the {"ok": True} response at server.py:536.

NOT part of this gap (do not charge here):
- Implementing GPS degradation inside real AirSim. The default and demo path is the fake sim (start.sh:19,51-52); --real requires the operator to start Unreal themselves (start.sh:46-49) and real AirSim GPS error is configured in settings.json, not commandable at runtime.
- Writing the INS/terrain-nav fallback procedure (PLAN.md:186). That is skill content, and no godseye-uav skill exists at all - it belongs to the missing-skill gap, not to M16.

Optionally worth filing SEPARATELY (real, currently unreported): denial does not degrade the safety envelope, because the geofence/BINGO gates use NED-derived truth at server.py:293 and server.py:363, so REVIEW_MILITARY.md:59's "geofence checks on fused position with growth margin during degradation" is unimplemented.

